import{c as a,Q as t,j as n,m as s}from"./chunks/framework.DxgWTcNC.js";const g=JSON.parse('{"title":"Generic Reverse Engineering Techniques","description":"","frontmatter":{},"headers":[],"relativePath":"part5/27-generic-re.md","filePath":"part5/27-generic-re.md","lastUpdated":null}'),o={name:"part5/27-generic-re.md"};function i(r,e,c,d,l,p){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="generic-reverse-engineering-techniques" tabindex="-1">Generic Reverse Engineering Techniques <a class="header-anchor" href="#generic-reverse-engineering-techniques" aria-label="Permalink to &quot;Generic Reverse Engineering Techniques&quot;">​</a></h1><p>This chapter collects techniques that apply across every reverse engineering target — embedded or userland, ARM or x86, ELF or PE. None of them is r2-specific; all of them transfer to Ghidra, IDA, or any other disassembler. They are the kind of pattern recognition and recovery tricks that distinguish an experienced reverse engineer from someone who has learned the tool but not the craft.</p><h2 id="compiler-fingerprinting" tabindex="-1">Compiler fingerprinting <a class="header-anchor" href="#compiler-fingerprinting" aria-label="Permalink to &quot;Compiler fingerprinting&quot;">​</a></h2><p>Knowing the compiler that produced a binary tells you which calling-convention quirks to expect, which optimization idioms to recognise, and which signature databases to apply.</p><p><strong>Leaked compiler strings.</strong> Most compilers embed a version string somewhere in the binary unless aggressively stripped:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ strings binary | grep -iE &#39;gcc|clang|msvc|rustc|go1\\.|ldc|icc|tcc&#39; | head</span></span>
<span class="line"><span>GCC: (Ubuntu 11.2.0-19ubuntu1) 11.2.0</span></span>
<span class="line"><span>Linker: LLD 15.0.0</span></span>
<span class="line"><span>clang version 15.0.0</span></span>
<span class="line"><span>rustc 1.74.0 (79e9716c9 2023-11-13)</span></span>
<span class="line"><span>go1.21.5</span></span></code></pre></div><p>For ELF binaries, look in <code>.comment</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ readelf -p .comment binary</span></span>
<span class="line"><span>String dump of section &#39;.comment&#39;:</span></span>
<span class="line"><span>  [     0]  GCC: (GNU) 11.2.0</span></span>
<span class="line"><span>  [    14]  rustc version 1.74.0</span></span></code></pre></div><p>For Mach-O, look at <code>__LC_BUILD_VERSION</code> and <code>__LC_VERSION_MIN_*</code>.</p><p>For PE, look at the rich header (Microsoft&#39;s embedded compiler metadata) — tools like <code>pefile</code> or <code>CFF Explorer</code> decode it.</p><p><strong>Idiom-based detection.</strong> When strings are scrubbed, compiler idioms remain:</p><ul><li><strong>GCC</strong> likes <code>lea</code> for arithmetic on x86, uses <code>endbr64</code> prologue on modern x86_64 with CET, emits switch-table dispatchers via indirect <code>jmp</code>.</li><li><strong>Clang/LLVM</strong> often produces tighter code than GCC, uses <code>movabs</code> more aggressively for 64-bit constants, generates distinctive vectorisation patterns.</li><li><strong>MSVC</strong> emits stack-cookie probes (<code>__security_cookie</code>, <code>__security_check_cookie</code>), uses <code>__SEH_prolog</code> and <code>__SEH_epilog</code> for exception frames on x86, has its own characteristic vtable layouts.</li><li><strong>Rust</strong> binaries have distinctive panic-handling code, the <code>core::panicking::panic</code> chain, mangled symbol names starting with <code>_ZN</code> (Itanium) or <code>_R</code> (newer Rust v0 mangling).</li><li><strong>Go</strong> binaries have an unmistakable runtime: <code>runtime.main</code>, <code>runtime.morestack</code>, gigantic moduledata tables, every function ending in a stack-growth check. The <code>golang_loader_assist</code> Ghidra script (and r2&#39;s <code>aag</code> recovery) recovers Go function names from the moduledata.</li></ul><p>Knowing the language matters even more for higher-level analysis. Rust and Go binaries have rich runtime metadata you can mine for type information; C/C++ binaries are bare to the metal.</p><h2 id="recognising-cryptographic-primitives" tabindex="-1">Recognising cryptographic primitives <a class="header-anchor" href="#recognising-cryptographic-primitives" aria-label="Permalink to &quot;Recognising cryptographic primitives&quot;">​</a></h2><p>Cryptography uses public mathematical constants. Find them in the binary and you find the crypto.</p><p><strong>Constants worth memorising:</strong></p><table tabindex="0"><thead><tr><th>Constant</th><th>Algorithm</th></tr></thead><tbody><tr><td><code>0x67452301, 0xefcdab89, 0x98badcfe, 0x10325476</code></td><td>MD5 IV</td></tr><tr><td><code>0x6a09e667, 0xbb67ae85, 0x3c6ef372, 0xa54ff53a, ...</code></td><td>SHA-256 IV</td></tr><tr><td><code>0x428a2f98, 0x71374491, 0xb5c0fbcf, ... (64 entries)</code></td><td>SHA-256 round constants</td></tr><tr><td><code>0xcbbb9d5d, 0x629a292a, 0x9159015a, ...</code></td><td>SHA-384 IV</td></tr><tr><td><code>0x6a09e667f3bcc908, 0xbb67ae8584caa73b, ...</code></td><td>SHA-512 IV</td></tr><tr><td><code>0x9e3779b9</code> (golden ratio constant)</td><td>TEA, XTEA, many hashes</td></tr><tr><td><code>0xc6a4a7935bd1e995</code></td><td>MurmurHash3 64-bit</td></tr><tr><td><code>0x9747b28c</code>, <code>0xed5b9b91</code></td><td>xxHash seed</td></tr><tr><td><code>0x01000193</code></td><td>FNV-1a 32-bit prime</td></tr><tr><td><code>0x100000001b3</code></td><td>FNV-1a 64-bit prime</td></tr><tr><td>256-byte permutation that starts <code>0x63 0x7c 0x77 0x7b ...</code></td><td>AES S-box</td></tr><tr><td>256-byte permutation that starts <code>0x52 0x09 0x6a 0xd5 ...</code></td><td>AES inverse S-box</td></tr><tr><td>256-byte table starting <code>0x00000000 0x77073096 0xee0e612c ...</code></td><td>CRC32 (Ethernet poly)</td></tr><tr><td><code>0x428a2f98d728ae22, 0x7137449123ef65cd, ...</code> (80 entries)</td><td>SHA-512 round constants</td></tr><tr><td>Permutation <code>0xd3, 0x14, 0x0f, 0x5d, ...</code> (24 rounds)</td><td>KeccakF[1600] / SHA-3</td></tr><tr><td><code>0x243f6a88, 0x85a308d3, 0x13198a2e, ...</code> (π in hex)</td><td>Blowfish P-array</td></tr><tr><td><code>0x3a39ce37, 0xd3faf5cf, 0xabc27737, ...</code> (e in hex)</td><td>Various</td></tr></tbody></table><p>A 256-entry word table that looks random-ish but is the same in two unrelated binaries is almost always a CRC lookup; the specific polynomial determines the variant (Ethernet&#39;s <code>0xedb88320</code>, ZIP/PNG&#39;s <code>0x04c11db7</code>, USB&#39;s <code>0xa001</code>).</p><p><strong>Structural tells:</strong></p><ul><li>A function with a 32-bit IV, eight working variables, eight parallel arithmetic chains, and sixteen rounds of message schedule → SHA-256 compression function.</li><li>A function with a 4×4 byte state, ten rounds (or twelve, or fourteen) of <code>SubBytes</code>/<code>ShiftRows</code>/<code>MixColumns</code>/<code>AddRoundKey</code> → AES.</li><li>A function that does 24 iterations on a 1600-bit state with <code>θ</code>/<code>ρ</code>/<code>π</code>/<code>χ</code>/<code>ι</code> steps → Keccak/SHA-3.</li><li>A long lookup-then-XOR-then-permute loop, often with a 256-byte state → RC4 (still in older firmware).</li><li>Big-number arithmetic on multi-word integers, with <code>mulhi</code>/<code>mullo</code> results combined → RSA or ECC.</li></ul><p>The free <strong><code>findcrypt</code></strong> and <strong><code>signsrch</code></strong> tools (and the <strong>Ghidra script <code>findcrypt.py</code></strong>, and r2&#39;s <code>iz~AES</code>) scan binaries for known crypto constants and tables and report matches with algorithm names. Run them on any cryptographic-looking binary first; manually identify what they miss.</p><h2 id="c-vtables-and-rtti-recovery" tabindex="-1">C++ vtables and RTTI recovery <a class="header-anchor" href="#c-vtables-and-rtti-recovery" aria-label="Permalink to &quot;C++ vtables and RTTI recovery&quot;">​</a></h2><p>C++ binaries embed runtime polymorphism through <strong>vtables</strong>: each object&#39;s first word points to a per-class table of virtual function pointers. With RTTI, those tables also reference type-info structures that contain the original class name as a string.</p><p><strong>Finding vtables:</strong></p><p>A typical vtable in <code>.rodata</code> looks like:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x405200]&gt; pxw 64 @ 0x405200</span></span>
<span class="line"><span>0x405200  0x004052f0 0x004053a0 0x00405410 0x00405480</span></span>
<span class="line"><span>0x405210  0x004054f0 0x00405560 0x00000000 0x00000000</span></span>
<span class="line"><span>0x405220  ...</span></span></code></pre></div><p>Each word that resolves to a function in <code>.text</code> is a vtable entry. A vtable typically has 4–20 entries followed by zero terminators or another vtable.</p><p><strong>RTTI:</strong></p><p>With RTTI enabled (the default in most builds), the word just before the vtable&#39;s first entry is a pointer to a <code>type_info</code> structure. That structure contains a pointer to a mangled class name (typically beginning with <code>N</code> for a namespaced name or just <code>9MyClass</code> style for unnamespaced).</p><p>To demangle:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ c++filt _ZN9MyClass5fnEi</span></span>
<span class="line"><span>MyClass::fn(int)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>$ rabin2 -D ./binary _ZN9MyClass5fnEi</span></span>
<span class="line"><span>MyClass::fn(int)</span></span></code></pre></div><p>R2 demangles automatically when you set <code>e bin.demangle=true</code>. The demangled names show up in function lists and disassembly.</p><p><strong>Recovering class hierarchies:</strong></p><p>Each vtable&#39;s RTTI links to its base class&#39;s RTTI. By walking those references you can reconstruct the inheritance tree:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; pxr 64 @ &lt;vtable - 16&gt;     # walk back to type_info</span></span>
<span class="line"><span>[0x...]&gt; ps @ &lt;name_addr&gt;</span></span>
<span class="line"><span>[0x...]&gt; pxw 64 @ &lt;type_info&gt;       # base class pointer</span></span></code></pre></div><p>For non-trivial codebases, <strong>Ghidra&#39;s <code>ClassRecovery</code> plugin</strong> does this automatically and is worth the trip to standalone Ghidra. The <strong>ClassyShark</strong> project (for Android) and <strong><code>vtable_finder</code></strong> for r2 also help.</p><p><strong>Calling conventions for virtual functions:</strong></p><ul><li><strong>Itanium ABI</strong> (Linux, macOS C++): first argument is the <code>this</code> pointer (<code>rdi</code>/<code>x0</code>/<code>r0</code>).</li><li><strong>Microsoft ABI</strong> (Windows C++): same idea on x64 (<code>rcx</code>); on x86 32-bit, <code>__thiscall</code> puts <code>this</code> in <code>ecx</code>.</li></ul><p>A function that takes its first argument in <code>this</code>-position and immediately dereferences it through offsets is almost certainly a member function.</p><h2 id="recognising-runtime-allocators" tabindex="-1">Recognising runtime allocators <a class="header-anchor" href="#recognising-runtime-allocators" aria-label="Permalink to &quot;Recognising runtime allocators&quot;">​</a></h2><p>The <code>malloc</code>/<code>free</code> of a binary tells you about its provenance. Each allocator has signatures.</p><p><strong>glibc malloc</strong> (ptmalloc2):</p><ul><li><code>arena_for_chunk</code>, <code>_int_malloc</code>, <code>_int_free</code> symbols (or their patterns).</li><li>Chunks have an 8-byte (32-bit) or 16-byte (64-bit) header with size and flag bits.</li><li>Tcaches in glibc 2.26+: per-thread caches with a distinctive linked-list shape.</li></ul><p><strong>jemalloc</strong> (FreeBSD, Firefox, some servers):</p><ul><li>Strings like <code>&lt;jemalloc&gt;: ...</code> or <code>je_malloc_init_hard</code>.</li><li>Size classes that go through <code>arena_dalloc_small</code> / <code>arena_dalloc_large</code>.</li><li>Often paired with <code>mallctl</code> for runtime stats.</li></ul><p><strong>tcmalloc</strong> (Google):</p><ul><li>Strings like <code>TCMALLOC_DEBUG</code>, <code>tcmalloc.aggressive_decommit</code>.</li><li><code>central_cache_</code>, <code>transfer_cache_</code>, <code>thread_cache_</code> symbols.</li></ul><p><strong>dlmalloc</strong> (Doug Lea):</p><ul><li>The classic, in many small/embedded Linux systems.</li><li><code>MAGIC_NUMBER</code> checks for chunk integrity.</li></ul><p><strong>mimalloc</strong> (Microsoft Research, newer projects):</p><ul><li><code>mi_heap_*</code>, <code>mi_malloc</code>, <code>mi_free</code>.</li></ul><p><strong>OpenBSD&#39;s omalloc / hardened_malloc</strong>:</p><ul><li>Aggressive chunk guards, junk-filling, page-per-chunk for large allocations. Distinctive overhead patterns.</li></ul><p>For embedded targets, custom allocators are common:</p><ul><li><strong>FreeRTOS heap_1/2/3/4/5</strong> — distinctive <code>pvPortMalloc</code> and <code>vPortFree</code> entry points; the heap variant affects coalescing and fragmentation behaviour.</li><li><strong>Zephyr <code>k_malloc</code></strong> — wraps a system heap with its own logic.</li><li><strong>Newlib <code>_malloc_r</code></strong> — re-entrant variants for thread safety.</li></ul><p>Each allocator&#39;s pattern is visible in disassembly. Once you identify it, you know how memory is laid out (chunk headers, free lists), which is invaluable for heap-overflow research.</p><h2 id="name-demangling" tabindex="-1">Name demangling <a class="header-anchor" href="#name-demangling" aria-label="Permalink to &quot;Name demangling&quot;">​</a></h2><p>C++ and Rust mangle names so the linker can distinguish overloads and namespaces. To read the source-equivalent names:</p><p><strong>C++ Itanium (Linux, macOS):</strong> prefix <code>_Z</code>. Tools: <code>c++filt</code>, <code>demangler</code> (Ghidra), <code>rabin2 -D</code>, online sites.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ c++filt &#39;_ZN3foo3barEi&#39;</span></span>
<span class="line"><span>foo::bar(int)</span></span></code></pre></div><p><strong>C++ MSVC:</strong> prefix <code>?</code>. Tools: <code>undname</code> (Windows SDK), Ghidra, IDA. Example: <code>?bar@foo@@QAEHH@Z</code> → <code>public: int __thiscall foo::bar(int)</code>.</p><p><strong>Rust legacy:</strong> Itanium-prefixed <code>_ZN</code>. Same demangler works but output is ugly (<code>rustc_demangle</code> cleans it up).</p><p><strong>Rust v0 (current):</strong> prefix <code>_R</code>. Tool: <code>rustc-demangle</code> or <code>r2 -e bin.demangle=true -e bin.lang=rust</code>.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ rustfilt _RNvCs1S7gVpKVQAk_4main3foo</span></span>
<span class="line"><span>main::foo</span></span></code></pre></div><p><strong>Swift:</strong> prefix <code>$s</code> or <code>_T</code>. Tool: <code>swift-demangle</code>.</p><p><strong>Objective-C:</strong> mostly readable as-is — <code>-[ClassName method:]</code> selectors appear in the binary.</p><p>R2 with <code>e bin.demangle=true</code> and <code>e bin.demanglecmd=true</code> runs the right demangler for the detected language. Ghidra handles this in its analyser.</p><h2 id="switch-tables-and-indirect-dispatch" tabindex="-1">Switch tables and indirect dispatch <a class="header-anchor" href="#switch-tables-and-indirect-dispatch" aria-label="Permalink to &quot;Switch tables and indirect dispatch&quot;">​</a></h2><p>Many functions compile a C <code>switch</code> into an <strong>indirect jump through a table</strong>. The compiler emits a small bounds check, then loads a target address from a <code>.rodata</code> table and jumps to it.</p><p>Patterns:</p><p><strong>x86_64 (GCC/Clang):</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>cmp     eax, 0xa            ; bounds check (default if &gt; max case)</span></span>
<span class="line"><span>ja      .default</span></span>
<span class="line"><span>lea     rdx, [rip+0x12345]  ; jump table base</span></span>
<span class="line"><span>movsxd  rax, dword [rdx+rax*4]</span></span>
<span class="line"><span>add     rax, rdx</span></span>
<span class="line"><span>jmp     rax</span></span></code></pre></div><p>The jump table at <code>[rip+0x12345]</code> is a list of <code>int32_t</code> offsets relative to the table itself, <em>not</em> absolute addresses. R2&#39;s analyser usually recovers these correctly with <code>aaaa</code> or <code>aac</code>.</p><p><strong>ARM Thumb-2:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>cmp     r0, #10</span></span>
<span class="line"><span>bhi     .default</span></span>
<span class="line"><span>tbb     [pc, r0]              ; table branch byte</span></span>
<span class="line"><span>.tbl:</span></span>
<span class="line"><span>.byte   (case0-.tbl)/2, (case1-.tbl)/2, ...</span></span></code></pre></div><p><code>TBB</code> (table branch byte) and <code>TBH</code> (table branch halfword) are ARM&#39;s compact dispatch encoding. R2 handles them but the table is embedded in code and easy to confuse with instructions; mark with <code>Cd 1 @ ...</code> if necessary.</p><p><strong>AArch64:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>adrp    x0, .tbl@PAGE</span></span>
<span class="line"><span>add     x0, x0, .tbl@PAGEOFF</span></span>
<span class="line"><span>ldrb    w1, [x0, w2, uxtw]</span></span>
<span class="line"><span>adr     x3, .tbl</span></span>
<span class="line"><span>add     x3, x3, w1, sxtb #2</span></span>
<span class="line"><span>br      x3</span></span></code></pre></div><p>Many switch tables defeat automatic xref recovery. When you see a function with an <code>br</code> or <code>jmp</code> to a register and no recovered outgoing edges, look for a nearby table. Annotate by hand (Chapter 11).</p><h2 id="anti-disassembly-defeat" tabindex="-1">Anti-disassembly defeat <a class="header-anchor" href="#anti-disassembly-defeat" aria-label="Permalink to &quot;Anti-disassembly defeat&quot;">​</a></h2><p>Hostile binaries (and some packers) include code that defeats naive linear disassembly. Common tricks:</p><p><strong>Junk byte after an unconditional branch.</strong> The disassembler tries to decode the byte after <code>jmp X</code> and gets garbage; the next valid instruction is offset by a few bytes. Defeat by anchoring disassembly at known good entry points and letting the analyser follow control flow rather than linearly.</p><p><strong>Overlapping instructions.</strong> Two valid instruction streams share bytes. Reading the same bytes from different start offsets produces different instructions. Defeat with a smart-disassembler that understands the control flow.</p><p><strong>API hashing.</strong> Code computes a hash of a function name and looks it up in a table at runtime instead of calling it by name. Defeat by computing the same hash over a list of likely names (GetProcAddress, LoadLibrary, etc.) and matching.</p><p><strong>Self-modifying code.</strong> Code patches itself before execution. Pure static analysis fails; dynamic or emulation (Unicorn/Qiling) is the answer.</p><p><strong>Opaque predicates.</strong> Conditional branches whose condition is always true (or always false), inserted to fool the analyser into following both paths. Defeat by recognising the pattern (constant comparisons, tautologies, dead-code-elimination-resistant tricks).</p><p>For each of these, the universal answer is: don&#39;t rely on linear disassembly. Use control-flow-aware analysis (which all modern disassemblers provide), and verify with dynamic analysis when the binary actively fights back.</p><h2 id="stack-canaries-and-other-compiler-inserted-checks" tabindex="-1">Stack canaries and other compiler-inserted checks <a class="header-anchor" href="#stack-canaries-and-other-compiler-inserted-checks" aria-label="Permalink to &quot;Stack canaries and other compiler-inserted checks&quot;">​</a></h2><p>Modern binaries include compiler-inserted security checks. They look like code but aren&#39;t part of the program&#39;s logic:</p><p><strong>Stack canaries:</strong> A magic value is pushed to the stack at function entry and checked at exit. If the stack was overflowed, the value changed, and the program aborts. Recognisable:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>mov rax, fs:[0x28]      ; load canary</span></span>
<span class="line"><span>mov [rbp - 8], rax      ; save on stack</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>mov rcx, [rbp - 8]</span></span>
<span class="line"><span>xor rcx, fs:[0x28]</span></span>
<span class="line"><span>jne stack_chk_fail</span></span>
<span class="line"><span>ret</span></span></code></pre></div><p>Sometimes the canary is <code>__stack_chk_guard</code>; the failure handler is <code>__stack_chk_fail</code>. Both are usually imported from libc.</p><p><strong>Shadow stack / CET (Control-flow Enforcement Technology):</strong> On modern x86_64 binaries with CET, every indirect-jump-target function starts with <code>endbr64</code> (<code>endbr32</code> on 32-bit). Without it, the CPU faults. R2 recognises this as <code>endbr64</code> mnemonic.</p><p><strong>Safe-Linking (glibc 2.32+):</strong> Forward pointers in the free list are XOR&#39;d with the address of the storing chunk, so a heap overflow can&#39;t trivially leak or hijack the list.</p><p>These are NOT part of the program&#39;s logic — pattern-recognise and skim past them.</p><h2 id="code-coverage-as-an-analysis-aid" tabindex="-1">Code-coverage as an analysis aid <a class="header-anchor" href="#code-coverage-as-an-analysis-aid" aria-label="Permalink to &quot;Code-coverage as an analysis aid&quot;">​</a></h2><p>When the binary runs in an environment you control:</p><ol><li>Run the binary while exercising different inputs/features.</li><li>Collect <strong>coverage</strong> with <code>gcov</code>, <code>llvm-cov</code>, AFL&#39;s coverage collector, DynamoRIO, Frida&#39;s <code>Stalker</code>, or QEMU&#39;s <code>qemu-log</code>.</li><li>Plot which functions were executed and which weren&#39;t.</li></ol><p>Code that ran during exercise is reachable from the inputs you gave. Code that didn&#39;t is either dead, defensive, or reached by input you haven&#39;t tried yet. The dead path is often the most interesting — backdoors, debug-only features, opt-in diagnostics — and is the right starting point for &quot;what does this binary hide?&quot;.</p><p>A simple coverage-mark workflow in r2:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; .!grep -oE &#39;0x[0-9a-f]+&#39; coverage.log | awk &#39;{print &quot;f covered.&quot;$1&quot; = &quot;$1}&#39;</span></span>
<span class="line"><span>[0x...]&gt; # then visualise via &quot;covered.&quot; flag space</span></span></code></pre></div><p>The same technique is used in fuzzing-driven reverse engineering: generate inputs that maximise coverage; the inputs that reach new code are the most interesting because they exercise new paths.</p><h2 id="diffing-as-discovery" tabindex="-1">Diffing as discovery <a class="header-anchor" href="#diffing-as-discovery" aria-label="Permalink to &quot;Diffing as discovery&quot;">​</a></h2><p>When you have two versions of a binary (v1 and v2 of a firmware, patched vs unpatched, original vs malware-infected), <strong>diffing</strong> reveals what changed. Three tools to know:</p><p><strong>BinDiff</strong> (Zynamics, free as of recent versions): the industry-standard binary diff tool. Matches functions by structural similarity, classifies as identical, matched-but-changed, or unmatched. Used with IDA or Ghidra.</p><p><strong>Diaphora</strong> (free, IDA plugin): comparable approach, open source.</p><p><strong>r2pipe scripted diff</strong> (Chapter 25 covers the script): hash each function&#39;s disassembly text, compare hashes across two binaries. Catches &gt;80% of what you care about for incremental analysis.</p><p>Diffing is invaluable for:</p><ul><li><strong>Vulnerability research:</strong> Bug fixes leak. The function that changed between v1.0.0 and v1.0.1 is your candidate vulnerability for v1.0.0 users who haven&#39;t updated.</li><li><strong>Malware analysis:</strong> Compare a clean binary against a possibly- tampered version. Differences are payload.</li><li><strong>Update reverse engineering:</strong> What did the vendor change? Often more revealing than the release notes.</li></ul><h2 id="a-few-cross-target-patterns-worth-knowing" tabindex="-1">A few cross-target patterns worth knowing <a class="header-anchor" href="#a-few-cross-target-patterns-worth-knowing" aria-label="Permalink to &quot;A few cross-target patterns worth knowing&quot;">​</a></h2><p><strong>The &quot;stack frame setup&quot; recognition.</strong> Every architecture has a canonical function entry idiom — <code>push rbp; mov rbp, rsp</code> on x86; <code>push {r4-r7, lr}</code> on Thumb; <code>stp x29, x30, [sp, #-N]!</code> on AArch64; <code>addiu $sp, $sp, -N; sw $ra, ...; sw $fp, ...</code> on MIPS. Spotting these helps you find function boundaries even when analysis is incomplete.</p><p><strong>The &quot;string table near a function pointer table&quot;.</strong> When you find a <code>.rodata</code> block with a list of function pointers immediately followed by, or interleaved with, strings, you have a dispatch table — likely command handlers in a daemon, callbacks in a plugin system, or message handlers in a protocol parser. Map the table, name each handler after its string.</p><p><strong>The &quot;GOT load then call&quot; pattern.</strong> PIC code calls libc through the GOT. Recognise <code>lea reg, [rip+got_entry]; call [reg]</code> as a single logical &quot;call function X&quot; even though it&#39;s two instructions.</p><p><strong>The &quot;long sequence of identical-looking calls&quot;.</strong> Often initialisation: registering callbacks, setting up a vtable, populating a config struct. If you decode one and they all look similar, the pattern likely repeats and you can batch-rename.</p><p><strong>The &quot;everything ends in a tail call to abort()/panic()&quot;.</strong> A function that ends in <code>bl abort</code> or <code>call exit</code> from many places is likely an assertion-failure path. Skim and move on.</p><p>These patterns are not specific to any tool. They are the texture of compiled code, learnable by hours of reading regardless of which disassembler you use.</p><h2 id="the-point" tabindex="-1">The point <a class="header-anchor" href="#the-point" aria-label="Permalink to &quot;The point&quot;">​</a></h2><p>The chapter on the playbook (Chapter 29) is about workflow discipline. This chapter is about pattern recognition. The two compound: discipline gives you the time to apply pattern recognition; pattern recognition makes each session productive.</p><p>Reverse engineering plateaus stop being about the tool around year 2 or 3; from then on the bottleneck is the <em>catalogue of patterns in your head</em> and the <em>speed at which you recognise them</em>. Both grow with practice. The exercises that grow them fastest are:</p><ul><li>Reverse engineer one open-source binary every few weeks, then compare your reconstruction against the actual source.</li><li>Read disassembly produced by every compiler you can install, for the same C source, to learn each compiler&#39;s idioms.</li><li>Reproduce the constants and patterns in the tables above from a binary you grep through, until you can spot them at a glance without help.</li></ul><p>The tool you use to do this matters less than that you do it. The sections of this chapter that survive — and the patterns you recognise from them ten years from now — will not be tied to any particular disassembler.</p>`,121)])])}const u=a(o,[["render",i]]);export{g as __pageData,u as default};
