import{c as a,Q as s,j as t,m as n}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"ARM Cortex-A and Linux Userland","description":"","frontmatter":{},"headers":[],"relativePath":"part3/13-arm-cortex-a-linux.md","filePath":"part3/13-arm-cortex-a-linux.md","lastUpdated":null}'),i={name:"part3/13-arm-cortex-a-linux.md"};function o(r,e,l,c,d,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="arm-cortex-a-and-linux-userland" tabindex="-1">ARM Cortex-A and Linux Userland <a class="header-anchor" href="#arm-cortex-a-and-linux-userland" aria-label="Permalink to &quot;ARM Cortex-A and Linux Userland&quot;">​</a></h1><p>Cortex-A is application-class ARM: cores big enough to run Linux, Android, and full-stack RTOSes (Zephyr&#39;s SMP support, RTEMS, BSDs). Reverse engineering a Cortex-A target is mostly Linux reverse engineering — ELF binaries, dynamic linking, the system call interface, often debug info you can use. This chapter covers the specifics.</p><p>For Linux kernel modules and the device tree, see Chapter 18.</p><h2 id="architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#architectural-overview" aria-label="Permalink to &quot;Architectural overview&quot;">​</a></h2><p>Cortex-A comes in three relevant flavours:</p><ul><li><strong>ARMv7-A</strong> (Cortex-A5/A7/A8/A9/A15/A17). 32-bit. Most older Linux-on-ARM systems: original Raspberry Pi, BeagleBone, NXP i.MX6, Allwinner H3, Freescale Vybrid. Mode-switchable between ARM and Thumb.</li><li><strong>ARMv8-A AArch64</strong> (Cortex-A35/A53/A55/A57/A72/A73/A75/A76/A77/ Apple silicon, Neoverse). 64-bit. Modern phones, Raspberry Pi 4+, most current single-board computers.</li><li><strong>ARMv8-A AArch32</strong> — backward-compatible 32-bit mode for ARMv8 cores. You will see this in 32-bit Linux distributions running on 64-bit hardware.</li></ul><p>For reverse engineering they differ in:</p><ul><li><strong>Calling convention.</strong> AArch64 uses x0–x7 for args, x0 for return. AArch32 uses r0–r3, r0 return. AAPCS rules; <code>afc arm32</code> and <code>afc arm64</code> in r2.</li><li><strong>Instruction encoding.</strong> AArch32 is fixed 32-bit ARM or 16/32-bit Thumb-2. AArch64 is fixed 32-bit. Thumb does not exist on AArch64.</li><li><strong>System registers.</strong> Different naming and access pattern. AArch64 uses <code>MRS x0, ID_AA64ISAR0_EL1</code>-style; AArch32 uses <code>MRC p15, 0, r0, c0, c0, 0</code>.</li><li><strong>Atomic primitives.</strong> AArch32 uses <code>LDREX/STREX</code>; AArch64 uses <code>LDXR/STXR</code> plus the new <code>CAS</code>/<code>SWP</code> instructions of ARMv8.1.</li></ul><p>For most application userland code these differences are invisible unless you are reading a libc, kernel, or hand-rolled lock.</p><h2 id="loading" tabindex="-1">Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;Loading&quot;">​</a></h2><p>A Linux ELF binary loads cleanly:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 /usr/bin/some_binary</span></span>
<span class="line"><span>[0x...]&gt; aaa</span></span></code></pre></div><p>R2 reads the ELF, picks up the PLT/GOT, finds imports and exports, and produces useful disassembly. For a stripped ELF, the function list is shorter but the binary is still navigable.</p><p>For an Android <code>.so</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 libnative.so</span></span></code></pre></div><p>For an AArch64 binary on a Mac (most likely scenario for a Mac reverse engineer working on Linux ARM software), no special flags — r2&#39;s <code>arm</code> arch handles all variants and reads <code>bits</code> from the ELF header.</p><p>If you have only a stripped binary with no ELF header (a <code>.bin</code> extracted from initramfs, an unpacked busybox applet), tell r2 the arch and bits explicitly:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a arm -b 32 -m 0x10000 stripped32.bin</span></span>
<span class="line"><span>$ r2 -a arm -b 64 -m 0x400000 stripped64.bin</span></span></code></pre></div><p>The default load address for a position-independent ELF is <code>0x10000</code> on AArch32 and <code>0x400000</code> on AArch64, but kernel images load at <code>0xffffffc008080000</code> (or thereabouts), and PIE binaries get arbitrary addresses at runtime — r2 picks reasonable defaults but you may need to override.</p><h2 id="imports-the-plt-and-the-got" tabindex="-1">Imports, the PLT, and the GOT <a class="header-anchor" href="#imports-the-plt-and-the-got" aria-label="Permalink to &quot;Imports, the PLT, and the GOT&quot;">​</a></h2><p>Every Linux ARM binary that uses libc has:</p><ul><li>an <strong>import table</strong> listing the functions it expects from shared libraries (<code>printf</code>, <code>malloc</code>, …);</li><li>a <strong>PLT</strong> (Procedure Linkage Table) — small stubs in <code>.plt</code> that jump to the resolved address of each import;</li><li>a <strong>GOT</strong> (Global Offset Table) — pointers in <code>.got</code> that the dynamic linker fills in at runtime.</li></ul><p>Inspect:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; ii                 # imports</span></span>
<span class="line"><span>[0x...]&gt; iE                 # exports</span></span>
<span class="line"><span>[0x...]&gt; iS                 # sections (look for .plt, .got)</span></span>
<span class="line"><span>[0x...]&gt; afl ~ sym.imp.     # PLT stubs</span></span>
<span class="line"><span>[0x...]&gt; axt @ sym.imp.malloc       # who calls malloc?</span></span></code></pre></div><p><code>sym.imp.foo</code> flags refer to the PLT entry for <code>foo</code>. R2 names them during loading. You navigate to a PLT stub and <code>axt</code> shows you every caller in the binary — typical first move when you are looking for, say, all the <code>socket()</code> calls.</p><h2 id="symbols-you-almost-always-have" tabindex="-1">Symbols you almost always have <a class="header-anchor" href="#symbols-you-almost-always-have" aria-label="Permalink to &quot;Symbols you almost always have&quot;">​</a></h2><p>Even fully stripped Linux binaries leak function names through:</p><ul><li>The <strong>dynamic symbol table</strong> (DT_SYMTAB / .dynsym) — needed for dynamic linking, not stripped by <code>strip</code>.</li><li><strong>DT_NEEDED</strong> — list of required shared libraries.</li><li><strong><code>.note.gnu.build-id</code></strong> — a build-ID hash you can correlate against debug info repositories (debuginfod).</li><li><strong>DT_RPATH</strong> / <strong>DT_RUNPATH</strong> — search paths embedded for the dynamic linker.</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; ies                 # entry-related symbols</span></span>
<span class="line"><span>[0x...]&gt; idp                 # PDB info (if any)</span></span>
<span class="line"><span>[0x...]&gt; iL                  # loaded libraries</span></span></code></pre></div><p>For a binary you want full symbols for, <code>debuginfod</code> is invaluable:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ debuginfod-find debuginfo /usr/bin/some_binary</span></span>
<span class="line"><span>$ debuginfod-find source /usr/bin/some_binary /path/to/source.c</span></span></code></pre></div><p>If the build-ID is in a public debuginfod (e.g., the <code>debuginfod.fedoraproject.org</code> server), you get debug info for free.</p><h2 id="calling-conventions-in-detail" tabindex="-1">Calling conventions in detail <a class="header-anchor" href="#calling-conventions-in-detail" aria-label="Permalink to &quot;Calling conventions in detail&quot;">​</a></h2><p><strong>AArch32 (ARM/Thumb)</strong>, EABI/AAPCS:</p><ul><li>Arguments: r0, r1, r2, r3, then stack</li><li>Return: r0 (and r1 for 64-bit)</li><li>Variadic: r0–r3 for first 4 args (regardless of int/float), more on stack</li><li>Floats: soft-float passes via integer regs; hard-float passes via s0–s15 then stack</li><li>Callee-saved: r4–r11</li><li>Stack alignment: 8-byte at function entry</li></ul><p><strong>AArch64</strong>, AAPCS64:</p><ul><li>Arguments: x0..x7 (integer), v0..v7 (float)</li><li>Return: x0 (and x1 for 128-bit)</li><li>Variadic: same registers, more on stack (standard AAPCS64 / Linux). Apple&#39;s darwin-arm64 ABI is different — variadics there go entirely on the stack. Worth knowing when reverse engineering macOS/iOS native binaries.</li><li>Callee-saved: x19..x28, v8..v15 (only low 64 bits of v8..v15)</li><li>Stack alignment: 16-byte at function entry</li></ul><p>For r2:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e anal.cc = arm32             # AArch32</span></span>
<span class="line"><span>[0x...]&gt; e anal.cc = arm64             # AArch64</span></span>
<span class="line"><span>[0x...]&gt; afc arm32 @ sym.foo</span></span>
<span class="line"><span>[0x...]&gt; afc arm64 @ sym.bar</span></span></code></pre></div><p>Hard-float on AArch32 needs explicit signatures:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afs float compute(float x, float y) @ sym.compute</span></span></code></pre></div><h2 id="position-independent-code" tabindex="-1">Position-independent code <a class="header-anchor" href="#position-independent-code" aria-label="Permalink to &quot;Position-independent code&quot;">​</a></h2><p>Modern Linux ARM userland is built PIC. Disassembly shows:</p><ul><li>references through <code>[pc, #imm]</code> (literal pool with relative offset)</li><li>GOT-relative loads (<code>adrp x0, :got:foo; ldr x0, [x0, :got_lo12:foo]</code> on AArch64)</li><li>PLT stubs that branch via the GOT</li></ul><p>R2 follows these correctly when the binary is loaded with the right mappings. Where it loses you is in raw memory dumps from <code>gcore</code> or process snapshots — the relocations are partially applied and r2&#39;s analysis sees stale literal-pool entries. For raw process dumps you typically have to either re-derive the load address from <code>/proc/PID/maps</code> and reload, or accept some xrefs going wrong.</p><h2 id="linux-specific-tricks" tabindex="-1">Linux-specific tricks <a class="header-anchor" href="#linux-specific-tricks" aria-label="Permalink to &quot;Linux-specific tricks&quot;">​</a></h2><p><strong>System calls.</strong> Direct <code>svc</code> (AArch32) or <code>svc #0</code> (AArch64) calls go through the syscall ABI:</p><ul><li>AArch32: r7 is the syscall number, r0..r5 the arguments</li><li>AArch64: x8 is the syscall number, x0..x5 the arguments</li></ul><p>R2 can decode these as comments if you set:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e asm.syscall = true</span></span>
<span class="line"><span>[0x...]&gt; e asm.syscall.col = 1</span></span></code></pre></div><p>Or annotate manually with <code>acs</code> (analyse comment syscall).</p><p><strong>libc internal symbols.</strong> If <code>_dl_runtime_resolve</code>, <code>__libc_start_main</code>, or <code>__cxa_finalize</code> show up, you have glibc. Musl uses different names (<code>_dlstart</code>, <code>__libc_start_init</code>). Recognise the difference; the function bodies for each have different shapes.</p><p><strong>Constructors / destructors.</strong> Look at <code>.init_array</code> / <code>.fini_array</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; iS ~ init</span></span>
<span class="line"><span>[0x...]&gt; pxw 64 @ section..init_array</span></span></code></pre></div><p>Each entry is a function pointer that runs before/after <code>main</code>. They are popular places for license checks, anti-debug routines, and backdoors. Always read them.</p><p><strong>RELRO.</strong> Modern binaries have read-only relocations (<code>.data.rel.ro</code>). If a binary is RELRO-disabled or uses lazy binding, GOT entries can be patched at runtime — relevant for some forms of process injection analysis.</p><h2 id="android-specifics" tabindex="-1">Android specifics <a class="header-anchor" href="#android-specifics" aria-label="Permalink to &quot;Android specifics&quot;">​</a></h2><p>Android shared libraries live under <code>/data/app/&lt;pkg&gt;/lib/&lt;arch&gt;/lib*.so</code> or in APKs. Extract from an APK:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ unzip app.apk -d app/</span></span>
<span class="line"><span>$ ls app/lib/</span></span>
<span class="line"><span>arm64-v8a/  armeabi-v7a/  x86/  x86_64/</span></span></code></pre></div><p>Load the <code>.so</code> you want — typically the <code>arm64-v8a</code> one for modern phones. JNI entry points have predictable names: <code>Java_&lt;package&gt;_&lt;class&gt;_&lt;method&gt;</code>. R2 demangles when you set:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e bin.demangle = true</span></span>
<span class="line"><span>[0x...]&gt; e bin.demanglecmd = true</span></span></code></pre></div><p>For native-only binaries (CLI tools, system services), they are ELFs in the conventional sense, but Android&#39;s bionic libc has different internals than glibc. Build a bionic signature DB if you do a lot of Android RE.</p><h2 id="kernel-userspace-interface" tabindex="-1">Kernel-userspace interface <a class="header-anchor" href="#kernel-userspace-interface" aria-label="Permalink to &quot;Kernel-userspace interface&quot;">​</a></h2><p>When reversing software that talks to a kernel driver, the interesting points are typically:</p><ul><li><strong><code>open(&quot;/dev/something&quot;, ...)</code></strong> followed by ioctls.</li><li><strong><code>ioctl(fd, _IOC(...), &amp;buf)</code></strong> — the request number encodes direction, size, type, command. Decode with the macros in <code>&lt;linux/ioctl.h&gt;</code>.</li><li><strong><code>mmap(NULL, size, ...)</code></strong> of a device file — typically a hardware register window or a DMA buffer.</li><li><strong>netlink sockets</strong> — <code>socket(AF_NETLINK, SOCK_RAW, NETLINK_*)</code>.</li></ul><p>For each of these, find the call site (<code>axt @ sym.imp.ioctl</code>), extract the request number from the immediate (often a magic constant), and look it up in the kernel source if you have it (<code>grep -r 0x40044901 linux/</code>).</p><h2 id="cortex-a-gotchas" tabindex="-1">Cortex-A gotchas <a class="header-anchor" href="#cortex-a-gotchas" aria-label="Permalink to &quot;Cortex-A gotchas&quot;">​</a></h2><p><strong>Mixed AArch32/AArch64 binaries are rare but exist.</strong> Some firmware (e.g., bootloaders, secure-world code) has both a 64-bit kernel and 32-bit BL31 code. Check <code>bits</code> on each section.</p><p><strong>TBI (Top Byte Ignore).</strong> AArch64 ignores the top 8 bits of pointers in some address translation modes. Pointer-tagging schemes (HWASAN, MTE) put metadata in those bits. R2&#39;s analysis treats them as opaque; do not be surprised if a &quot;pointer&quot; has nonsense in the high byte.</p><p><strong>PAC / BTI.</strong> ARMv8.3-A adds Pointer Authentication (<code>paciasp</code>, <code>autiasp</code>, etc.) and ARMv8.5-A adds Branch Target Identification (<code>bti c/j/jc</code>). Function entry instructions for these show up throughout modern AArch64 binaries. They are NOPs on hardware that does not implement the feature; r2 decodes them but does not currently treat them specially during analysis.</p><p><strong>Linker-relaxation tricks.</strong> Some ARM linkers can rewrite a <code>bl &lt;far&gt;</code> to <code>nop; bl &lt;near&gt;</code> if the call target ends up nearby. The disassembly is correct but the code looks padded. Recognise the pattern.</p><p>A clean Cortex-A workflow looks the same as desktop x86 RE: load, analyse, name the imports&#39; callers, follow the strings, follow the ioctls. Most of the rest of this book applies; the next chapters cover the architectures where things get weirder.</p>`,72)])])}const g=a(i,[["render",o]]);export{u as __pageData,g as default};
