import{c as e,Q as a,j as s,m as n}from"./chunks/framework.DxgWTcNC.js";const h=JSON.parse('{"title":"Functions, Types, and Structures","description":"","frontmatter":{},"headers":[],"relativePath":"part2/08-functions-types.md","filePath":"part2/08-functions-types.md","lastUpdated":null}'),o={name:"part2/08-functions-types.md"};function d(i,t,p,c,r,l){return a(),s("div",null,[...t[0]||(t[0]=[n(`<h1 id="functions-types-and-structures" tabindex="-1">Functions, Types, and Structures <a class="header-anchor" href="#functions-types-and-structures" aria-label="Permalink to &quot;Functions, Types, and Structures&quot;">​</a></h1><p>Disassembly is bytes-as-instructions. To turn it into something you can reason about, you annotate it with structure: function boundaries, parameter types, local variable layouts, and the C structs that the code is reading and writing. Radare2 has a complete type system for this, and using it well changes both the readability of disassembly and the quality of decompiler output.</p><h2 id="function-commands" tabindex="-1">Function commands <a class="header-anchor" href="#function-commands" aria-label="Permalink to &quot;Function commands&quot;">​</a></h2><p>You met <code>af</code> (analyse function) in Part I. The full family:</p><table tabindex="0"><thead><tr><th>Command</th><th>What it does</th></tr></thead><tbody><tr><td><code>af</code></td><td>analyse function at current/given address</td></tr><tr><td><code>af-</code></td><td>delete function</td></tr><tr><td><code>af+ name</code></td><td>create function with explicit name</td></tr><tr><td><code>afn name</code></td><td>rename current function</td></tr><tr><td><code>afi</code></td><td>print function info (size, blocks, vars, callees)</td></tr><tr><td><code>afv</code></td><td>list local variables</td></tr><tr><td><code>afs</code></td><td>print/set function signature</td></tr><tr><td><code>afS</code></td><td>set function signature from string</td></tr><tr><td><code>afc</code></td><td>get/set calling convention</td></tr><tr><td><code>afcl</code></td><td>list available calling conventions</td></tr><tr><td><code>afb</code></td><td>list basic blocks</td></tr><tr><td><code>afx</code></td><td>list xrefs from this function</td></tr><tr><td><code>aft</code></td><td>propagate types from signature into body</td></tr><tr><td><code>afta</code></td><td>analyse types (full pass)</td></tr><tr><td><code>afm name</code></td><td>merge into named function</td></tr></tbody></table><p>A common cleanup workflow: r2 found a function but missed the call that goes to it from elsewhere. Manually:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; af @ 0x08005000          # define</span></span>
<span class="line"><span>[0x...]&gt; afn handle_packet        # name it</span></span>
<span class="line"><span>[0x...]&gt; afs int handle_packet(uint8_t *buf, size_t len)   # signature</span></span>
<span class="line"><span>[0x...]&gt; aft                      # propagate types into the body</span></span></code></pre></div><p>Now decompiling shows <code>handle_packet(buf, len)</code> everywhere, with <code>buf</code> as <code>uint8_t*</code> and <code>len</code> as <code>size_t</code>.</p><h2 id="calling-conventions" tabindex="-1">Calling conventions <a class="header-anchor" href="#calling-conventions" aria-label="Permalink to &quot;Calling conventions&quot;">​</a></h2><p>A wrong calling convention means decompilation guesses parameters wrong. R2 ships with a calling convention database; list:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afcl</span></span>
<span class="line"><span>arm32</span></span>
<span class="line"><span>arm64</span></span>
<span class="line"><span>arm32fastcall</span></span>
<span class="line"><span>... (architecture-dependent)</span></span></code></pre></div><p>Set:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afc arm32 @ sym.foo</span></span>
<span class="line"><span>[0x...]&gt; e anal.cc = arm32     # global default for new functions</span></span></code></pre></div><p>For embedded ARM, the relevant ones are:</p><table tabindex="0"><thead><tr><th>Convention</th><th>When to use</th></tr></thead><tbody><tr><td><code>arm32</code></td><td>AAPCS — args in r0..r3 then stack, return in r0</td></tr><tr><td><code>arm32fastcall</code></td><td>the few cases where the compiler used a non-standard cc</td></tr><tr><td><code>armcdecl</code></td><td>callee-cleanup variant (rare)</td></tr><tr><td><code>arm64</code></td><td>AArch64 AAPCS — args in x0..x7</td></tr></tbody></table><p>For Cortex-M, the floating-point ABI matters: with FP-soft, all FP args go through integer registers; with FP-hard (most Cortex-M4F+), they use s0..s15. R2&#39;s <code>arm32</code> default assumes soft-float; if your binary is hard-float, function signatures with <code>float</code> parameters will be wrong. Override per function:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afs int dsp_fft(float *in, float *out, int n) @ sym.dsp_fft</span></span></code></pre></div><h2 id="local-variables" tabindex="-1">Local variables <a class="header-anchor" href="#local-variables" aria-label="Permalink to &quot;Local variables&quot;">​</a></h2><p>R2 detects stack and register variables during analysis. List them in the current function:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afv</span></span>
<span class="line"><span>arg int arg1 @ r0</span></span>
<span class="line"><span>arg int arg2 @ r1</span></span>
<span class="line"><span>var int var_4h @ sp+0x4</span></span>
<span class="line"><span>var int var_8h @ sp+0x8</span></span></code></pre></div><p>Rename a variable for readability:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afvn buf var_4h</span></span>
<span class="line"><span>[0x...]&gt; afvn count var_8h</span></span></code></pre></div><p>The renames flow through the whole function disassembly <em>and</em> through the decompiler output. This is one of the most impactful things you can do for a function you will spend more than 5 minutes on.</p><p>Set a type for a variable:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afvt buf &quot;uint8_t *&quot;</span></span>
<span class="line"><span>[0x...]&gt; afvt count size_t</span></span></code></pre></div><p>Then re-run decompilation (<code>pdg</code>) and the C output uses your types.</p><h2 id="the-type-system" tabindex="-1">The type system <a class="header-anchor" href="#the-type-system" aria-label="Permalink to &quot;The type system&quot;">​</a></h2><p>R2 has its own type definition language, accessible through <code>t*</code> commands.</p><table tabindex="0"><thead><tr><th>Command</th><th>Action</th></tr></thead><tbody><tr><td><code>t</code></td><td>list all types</td></tr><tr><td><code>tk</code></td><td>list types as SDB keys</td></tr><tr><td><code>to</code></td><td>open / parse a header file</td></tr><tr><td><code>tos</code></td><td>parse a string of C source</td></tr><tr><td><code>t-</code></td><td>delete a type</td></tr><tr><td><code>tp</code></td><td>print a struct/type at an address</td></tr><tr><td><code>tl</code></td><td>link a type to an address (variable annotation)</td></tr><tr><td><code>tc</code></td><td>print all types as C definitions</td></tr><tr><td><code>ts</code></td><td>structure operations</td></tr><tr><td><code>tu</code></td><td>union operations</td></tr><tr><td><code>te</code></td><td>enum operations</td></tr><tr><td><code>tf</code></td><td>function-type operations</td></tr><tr><td><code>tn</code></td><td>typename operations</td></tr></tbody></table><p>Define a struct from C source:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; &quot;tos struct usart_regs { uint32_t SR; uint32_t DR; uint32_t BRR; \\</span></span>
<span class="line"><span>        uint32_t CR1; uint32_t CR2; uint32_t CR3; uint32_t GTPR; };&quot;</span></span></code></pre></div><p>(Note the double-quoted form because the command spans multiple words.)</p><p>Or load from a header file:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; to /usr/include/arpa/inet.h</span></span>
<span class="line"><span>[0x...]&gt; to ./stm32f4_regs.h</span></span></code></pre></div><p>Once a type is known, reference it:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; tp usart_regs @ 0x40011000      # print as struct at address</span></span>
<span class="line"><span>[0x...]&gt; tl usart_regs = 0x40011000      # link the type to that address</span></span></code></pre></div><p>The link makes references to fields visible everywhere in the disassembly:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>str r1, [r3]              ; usart_regs.SR</span></span>
<span class="line"><span>str r2, [r3, #4]          ; usart_regs.DR</span></span></code></pre></div><p>Without the link you would see <code>[r3]</code> and <code>[r3, #4]</code> and have to do the offset arithmetic in your head.</p><h2 id="importing-vendor-headers" tabindex="-1">Importing vendor headers <a class="header-anchor" href="#importing-vendor-headers" aria-label="Permalink to &quot;Importing vendor headers&quot;">​</a></h2><p>The big payoff: load the vendor&#39;s CMSIS / SDK headers and r2 understands the entire peripheral block.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; to /opt/STM32CubeF4/Drivers/CMSIS/Device/ST/STM32F4xx/Include/stm32f407xx.h</span></span>
<span class="line"><span>[0x...]&gt; tl USART_TypeDef = 0x40011000</span></span>
<span class="line"><span>[0x...]&gt; tl GPIO_TypeDef = 0x40020000</span></span>
<span class="line"><span>... etc.</span></span></code></pre></div><p>Now the disassembly reads as actual struct field accesses. Every register access has a name. The decompiler output looks like the original C.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Vendor headers are <em>huge</em> and contain a lot of preprocessor magic that r2&#39;s <code>to</code> parser cannot fully chew. If <code>to</code> fails, run the header through <code>cpp</code> first:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ cpp -P -DSTM32F407xx -nostdinc -I /opt/STM32CubeF4/Drivers/CMSIS/Include \\</span></span>
<span class="line"><span>      stm32f407xx.h &gt; stm32_flat.h</span></span></code></pre></div><p>Then <code>to stm32_flat.h</code>. You may need to delete a few unsupported constructs (<code>__attribute__</code> extensions r2 does not parse) by hand.</p></div><h2 id="enums" tabindex="-1">Enums <a class="header-anchor" href="#enums" aria-label="Permalink to &quot;Enums&quot;">​</a></h2><p>Enums are how you make integer constants readable. Define:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; &quot;te enum gpio_mode { input=0, output=1, alt=2, analog=3 };&quot;</span></span></code></pre></div><p>Link a function parameter to the enum so its values render as names:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afvt mode &quot;enum gpio_mode&quot; @ sym.gpio_set_mode</span></span></code></pre></div><p>Now a call site <code>mov r1, #2; bl gpio_set_mode</code> shows <code>gpio_set_mode(r0, gpio_mode.alt)</code> in the decompiler output.</p><h2 id="function-signatures-and-aft" tabindex="-1">Function signatures and <code>aft</code> <a class="header-anchor" href="#function-signatures-and-aft" aria-label="Permalink to &quot;Function signatures and \`aft\`&quot;">​</a></h2><p><code>afs</code> sets a function&#39;s signature. <code>aft</code> propagates types from a function&#39;s signature into its body — when you set <code>arg1</code> as <code>uint8_t *buf</code>, <code>aft</code> follows reads and writes through that pointer and types those locations too.</p><p>The right order is:</p><ol><li>Set the function signature: <code>afs</code>.</li><li>Run <code>aft</code> for that function (or <code>afta</code> to do the whole binary).</li><li>Decompile (<code>pdg</code>) and check that types flow.</li></ol><p><code>afta</code> (the all-binary type pass) is part of <code>aaaa</code>. It is slow on big binaries but pays for itself: types propagate transitively, so naming one root function correctly cleans up many descendants.</p><h2 id="structures-from-indirect-access-patterns" tabindex="-1">Structures from indirect access patterns <a class="header-anchor" href="#structures-from-indirect-access-patterns" aria-label="Permalink to &quot;Structures from indirect access patterns&quot;">​</a></h2><p>When you see code like:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>ldr r1, [r0, #0]</span></span>
<span class="line"><span>ldr r2, [r0, #4]</span></span>
<span class="line"><span>ldrb r3, [r0, #8]</span></span>
<span class="line"><span>str r3, [r0, #9]</span></span></code></pre></div><p>you are looking at struct field access. R2 can sometimes infer the structure layout, but you usually do better by defining it yourself:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; &quot;tos struct packet { uint32_t magic; uint32_t length; uint8_t flags; uint8_t kind; };&quot;</span></span>
<span class="line"><span>[0x...]&gt; afvt packet &quot;struct packet *&quot; @ sym.handle_packet</span></span>
<span class="line"><span>[0x...]&gt; aft @ sym.handle_packet</span></span></code></pre></div><p>After <code>aft</code>, the disassembly shows <code>packet.magic</code>, <code>packet.length</code>, <code>packet.flags</code>, <code>packet.kind</code> instead of offsets. Decompiler output is correspondingly clearer.</p><h2 id="saving-types" tabindex="-1">Saving types <a class="header-anchor" href="#saving-types" aria-label="Permalink to &quot;Saving types&quot;">​</a></h2><p>Types live in the project (saved by <code>Ps</code>). They can also be exported as C source:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; tc                      # all types as C</span></span>
<span class="line"><span>[0x...]&gt; tc struct packet        # one type</span></span>
<span class="line"><span>[0x...]&gt; tc &gt; types.h            # to a file</span></span></code></pre></div><p>Commit <code>types.h</code> to your firmware-RE git repo. Reload in another session with <code>to types.h</code>. This is the cleanest way to share the structural understanding of a binary across people or sessions.</p>`,65)])])}const g=e(o,[["render",d]]);export{h as __pageData,g as default};
