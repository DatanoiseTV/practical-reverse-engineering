import{c as t,Q as o,j as a,m as r}from"./chunks/framework.DxgWTcNC.js";const p=JSON.parse('{"title":"Hardware Tricks, Backdoors, and Less-Known Pitfalls","description":"","frontmatter":{},"headers":[],"relativePath":"part6/33-tricks-pitfalls.md","filePath":"part6/33-tricks-pitfalls.md","lastUpdated":null}'),s={name:"part6/33-tricks-pitfalls.md"};function i(n,e,l,d,c,h){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="hardware-tricks-backdoors-and-less-known-pitfalls" tabindex="-1">Hardware Tricks, Backdoors, and Less-Known Pitfalls <a class="header-anchor" href="#hardware-tricks-backdoors-and-less-known-pitfalls" aria-label="Permalink to &quot;Hardware Tricks, Backdoors, and Less-Known Pitfalls&quot;">​</a></h1><p>The previous two chapters covered fault injection and side-channel analysis — the academically named techniques. This chapter covers the long tail of practical hardware-side findings that don&#39;t fit under a single banner. Most of these come up in security-research work on commercial devices, and most can be discovered with a multimeter, a logic analyser, and patience. Several have produced high-impact CVEs and product recalls.</p><p>The unifying theme: <strong>a lot of &quot;secure&quot; embedded systems are not secure against an attacker who has the physical device, a hot-air station, and a few hundred dollars of equipment.</strong> The reasons are boring and repetitive, and the same dozen mistakes show up across products from every vendor.</p><h2 id="debug-interfaces-left-enabled-in-production" tabindex="-1">Debug interfaces left enabled in production <a class="header-anchor" href="#debug-interfaces-left-enabled-in-production" aria-label="Permalink to &quot;Debug interfaces left enabled in production&quot;">​</a></h2><p>The single most common finding. A device ships with JTAG, SWD, or a UART boot console fully enabled. Sometimes the silkscreen labels the test points; sometimes they&#39;re unlabelled vias; sometimes they&#39;re hidden under a sticker. In all cases they survived production and they let you read flash, attach a debugger, or break into a boot prompt.</p><p>What to look for on an unfamiliar PCB:</p><ul><li><strong>Headers with 0.1&quot; or 0.05&quot; pitch, 4 to 20 pins.</strong> Common JTAG pinouts are 20-pin (ARM legacy), 10-pin (ARM Cortex SWD), 14-pin (TI / Renesas), 6-pin (FTDI-style serial). The Hardware Hacking Handbook has photos and pinouts.</li><li><strong>Single rows of 3 to 5 unlabelled vias near a chip.</strong> Very often a UART (TX, RX, GND, sometimes VCC and reset).</li><li><strong>Test pads on the bottom side near a SoC.</strong> Many products route their debug signals to test pads, not headers, so the programmer connects only at factory.</li><li><strong>Unpopulated header footprints.</strong> The PCB has the holes, the factory did not populate the connector. Solder one in.</li></ul><p>Tools that help:</p><ul><li><strong>JTAGulator</strong> (Joe Grand, Grand Idea Studio). A small open hardware device that probes 8–24 pins in turn, trying every permutation of TCK / TMS / TDI / TDO, and reports which combinations produce a valid JTAG IDCODE. Also tries UART bauds. Sold by Adafruit and others. A genuine time-saver on any unfamiliar board.</li><li><strong>Glasgow Interface Explorer</strong> (1bitsquared). FPGA-based general-purpose protocol analyser/explorer. Open hardware. Good for poking unknown bus protocols.</li><li><strong>Bus Pirate</strong> (Dangerous Prototypes, originally by Ian Lesnet). Older, still useful for SPI / I2C / UART / 1-Wire fingerprinting on test pads.</li><li><strong>A USB-UART adapter</strong> (FTDI, CP2102, CH340) and <code>picocom</code> / <code>screen</code>. For UART discovery once you&#39;ve identified the candidate TX line.</li><li><strong>An oscilloscope.</strong> For confirming a candidate UART&#39;s baud rate by measuring the width of a start bit.</li></ul><p>The workflow when a new PCB lands on your bench:</p><ol><li>Photograph both sides at high resolution.</li><li>Visually scan for headers, vias, and test points near the SoC.</li><li>Probe candidates for activity at power-on: a TX line will show UART traffic; SWCLK will toggle if a debugger is being looked for.</li><li>Run JTAGulator across any cluster of 4–8 unlabelled pins.</li><li>Once you have JTAG or SWD, try a chip-recognising programmer (OpenOCD, J-Link Commander, probe-rs <code>probe-rs info</code>).</li></ol><p>It is surprising how often steps 1–4 lead directly to a shell prompt or a flash-readable debug port.</p><h2 id="boot-consoles-and-recovery-modes" tabindex="-1">Boot consoles and recovery modes <a class="header-anchor" href="#boot-consoles-and-recovery-modes" aria-label="Permalink to &quot;Boot consoles and recovery modes&quot;">​</a></h2><p>Many SoCs have a vendor-supplied boot ROM that lives at a fixed address and offers a serial command interface for flashing, dumping memory, or running JTAG. The boot ROM is usually <strong>unkillable</strong> — it survives bricks, RDP, and most attacker mistakes — and it is documented in the vendor&#39;s datasheet because end customers need it for manufacturing.</p><p>Worth knowing:</p><ul><li><strong>STMicroelectronics System Bootloader</strong> at <code>0x1FFF0000</code> on most STM32 parts. Application note AN2606 documents the protocol (USART, USB, I2C, SPI variants). The bootloader is <strong>disabled</strong> by RDP Level 2 on some families; on Level 1 it usually still runs but cannot read out flash.</li><li><strong>NXP LPC ISP (In-System Programming)</strong> mode on Kinetis / LPC parts. Activated by pulling a specific pin low at reset. UART protocol documented in the user manual.</li><li><strong>Microchip / Atmel SAM-BA</strong> monitor on SAM-series parts. Activated by pulling a specific pin (typically <code>ERASE</code>) and resetting. USB or UART interface.</li><li><strong>Espressif ESP ROM bootloader</strong> at <code>0x40000000</code> (ESP32 family). Activated by holding <code>GPIO0</code> low at reset. Implements the <code>esptool</code> protocol documented at <code>https://github.com/espressif/esptool</code>.</li><li><strong>Raspberry Pi RP2040 BOOTSEL.</strong> Hold BOOTSEL during USB connect; the chip enumerates as a USB mass-storage device and accepts UF2 firmware drops. Unkillable.</li><li><strong>Allwinner FEL mode.</strong> USB-based recovery on Allwinner SoCs (popular in cheap Linux tablets / SBCs). Many published tools for FEL.</li></ul><p>These modes are not vulnerabilities, but they are very useful for recovery during your own work, and they are the entry point for attackers who have not figured out how to break the chip&#39;s main security.</p><h2 id="flash-and-otp-gotchas" tabindex="-1">Flash and OTP gotchas <a class="header-anchor" href="#flash-and-otp-gotchas" aria-label="Permalink to &quot;Flash and OTP gotchas&quot;">​</a></h2><p><strong>Flash erase is binary and biased.</strong> Most NOR/NAND flash erases to all-<code>1</code>s. Programming sets bits to <code>0</code>. You can change a <code>1</code> to <code>0</code> without erasing first, but you cannot change a <code>0</code> back to <code>1</code> without an erase cycle. Some published attacks exploit this asymmetry — for instance, you can disable a &quot;valid signature&quot; flag on a structure by setting bits to <code>0</code> without erasing the surrounding page.</p><p><strong>Read margin and &quot;ghost&quot; data.</strong> Flash cells store charge; the distinction between <code>1</code> and <code>0</code> is a voltage threshold. Some controllers expose a &quot;read margin&quot; command that reads with a shifted threshold, revealing previously-erased data that has not fully discharged. Documented for some SPI flash families (Winbond, Spansion). Hardware Hacking Handbook covers this in detail.</p><p><strong>OTP / eFuse is irreversible.</strong> OTP fuses can be programmed once; you cannot unburn them. Read the &quot;security fuse&quot; register before writing anything to OTP. Multiple commercial products have been permanently bricked by enabling debug-lockout fuses during &quot;hardening&quot; without leaving themselves a recovery path.</p><p><strong>Sticky bits and reset behaviour.</strong> Some chip security registers latch the value of a fuse at reset and ignore subsequent fuse changes within the same power cycle. Others are re-read each reset. The chip&#39;s reference manual is the truth; assumptions are the problem. Read carefully before relying on either behaviour.</p><p><strong>Mass erase doesn&#39;t always erase customer data.</strong> Some &quot;mass erase&quot; commands erase the application flash but leave info / configuration pages intact, or leave a small region the bootloader uses. Customer data — keys, serial numbers — sometimes survives what looks like a complete wipe.</p><h2 id="vendor-specific-recovery-and-test-modes" tabindex="-1">Vendor-specific recovery and test modes <a class="header-anchor" href="#vendor-specific-recovery-and-test-modes" aria-label="Permalink to &quot;Vendor-specific recovery and test modes&quot;">​</a></h2><p>Production-test modes that are not removed in customer firmware are a recurring finding. Examples:</p><ul><li><strong>Test commands left in serial bootloader.</strong> Several router vendors have shipped customer firmware with serial-port commands like <code>printenv</code>, <code>loadb</code>, or vendor-specific test-mode entries reachable from a U-Boot prompt. Sometimes the prompt is suppressed by default but pressing a specific key combination at boot exposes it.</li><li><strong>GPIO-strap-induced modes.</strong> Some SoCs have boot-mode straps that select between &quot;boot from flash&quot;, &quot;boot from UART&quot;, &quot;boot from JTAG&quot;, and &quot;boot from external memory&quot;. A jumper or wire swap can reroute boot through a route the firmware author did not consider.</li><li><strong>Recovery key combinations.</strong> &quot;Hold three buttons during boot to enter recovery&quot; is a documented feature on many consumer devices, but the recovery mode often grants more access than the customer feature implies.</li><li><strong>Hidden CLI on production daemons.</strong> Many embedded Linux products ship with a hidden CLI in their web/UI service that&#39;s accessible via a specific URL or query parameter. <code>cgi-bin/</code> paths, magic header values, undocumented JSON-RPC methods. Easy to find with a recursive <code>grep</code> once the firmware is extracted (Chapter 24).</li></ul><p>These are not exotic attacks. They are first-pass findings in half-day device assessments.</p><h2 id="encrypted-without-thinking" tabindex="-1">&quot;Encrypted&quot; without thinking <a class="header-anchor" href="#encrypted-without-thinking" aria-label="Permalink to &quot;&quot;Encrypted&quot; without thinking&quot;">​</a></h2><p>Encrypted firmware update files where the encryption key is shipped in plaintext inside the device, or generated from a device-unique value that&#39;s also stored in plaintext, or both, are unfortunately common. Look for:</p><ul><li><strong>Hard-coded AES keys</strong> in the bootloader or update agent binary. They look like high-entropy 16/24/32-byte byte arrays in <code>.rodata</code>.</li><li><strong>Hashes of MAC address / serial number</strong> as device-unique keys. The device&#39;s own MAC address is broadcast over WiFi.</li><li><strong>Hard-coded RSA private keys.</strong> Less common because the file is bigger, but it happens.</li><li><strong>CRC-only &quot;integrity&quot;</strong> of update images, advertised as &quot;secure update&quot;. Forge an image with a matching CRC and the device accepts it.</li></ul><p>The Hardware Hacking Handbook has a worked example. The <strong>Mirai botnet</strong> family famously exploits default credentials and similar weaknesses across IoT cameras.</p><h2 id="bypassing-readout-protection-by-side-channels" tabindex="-1">Bypassing readout protection by side channels <a class="header-anchor" href="#bypassing-readout-protection-by-side-channels" aria-label="Permalink to &quot;Bypassing readout protection by side channels&quot;">​</a></h2><p>A few chip families publicly known to leak the contents of readout-protected flash via timing or instruction-fetch side channels — even without fault injection:</p><ul><li>Various 8-bit microcontrollers (PIC, ATtiny) have published read-protect bypasses.</li><li>Some early ARM Cortex-M parts (Hardware Hacking Handbook, Riscure white papers, and various Hardwear.io conference talks document these).</li><li>Several smartcards (academic literature).</li></ul><p>The pattern is the same in each: a debugger-accessible operation (single-step, branch, single instruction execute) creates a visible side effect that depends on the contents of the &quot;protected&quot; memory.</p><p>When the vendor&#39;s marketing emphasises &quot;secure&quot; and &quot;tamper-proof&quot; without naming specific countermeasures, assume the protection is weaker than claimed and search for published research on the specific part number.</p><h2 id="glitching-peripherals-to-leak-across-security-boundaries" tabindex="-1">Glitching peripherals to leak across security boundaries <a class="header-anchor" href="#glitching-peripherals-to-leak-across-security-boundaries" aria-label="Permalink to &quot;Glitching peripherals to leak across security boundaries&quot;">​</a></h2><p>A subtler family of attacks: glitch not the CPU but a peripheral that crosses a security boundary. Examples documented in published research:</p><ul><li>DMA-based attacks: convince the DMA engine to copy out of a secure region while the CPU is in non-secure mode.</li><li>Cache flushes that leak across cores in SMP.</li><li>Hardware crypto engines that store keys in registers visible via debug interfaces.</li></ul><p>These require deep chip-specific knowledge and are usually the result of a dedicated research project rather than ad-hoc attack work.</p><h2 id="pitfalls-when-doing-the-work-yourself" tabindex="-1">Pitfalls when doing the work yourself <a class="header-anchor" href="#pitfalls-when-doing-the-work-yourself" aria-label="Permalink to &quot;Pitfalls when doing the work yourself&quot;">​</a></h2><p>A short list of things experienced hardware researchers do not have to be told twice:</p><ul><li><strong>Lift the chip off the board for a clean SPI flash dump.</strong> Other peripherals on the SPI bus can interfere; the SoC itself may drive the lines. A hot-air station and a SOIC clip cost less than the time spent debugging in-circuit reads.</li><li><strong>Always have a current-limited supply.</strong> A bench supply set to 50 mA limit at 3.3 V is your insurance policy against reverse-polarity, shorts, and surprises during glitching.</li><li><strong>Photograph the board before desoldering.</strong> And after. Document every wire you cut.</li><li><strong>Keep an oscilloscope on the supply rail.</strong> A glitch attempt that resets the chip looks identical to a glitch attempt that exposed an OTP fuse, until you check the trace.</li><li><strong>Don&#39;t burn the eFuse without a recovery path.</strong> Some chips have unrecoverable lockouts. Read every word of the relevant errata and application note before writing OTP.</li><li><strong>Treat the device as evidence if it might end up in court.</strong> Forensic work has different rules than research; talk to a lawyer before destructively analysing customer-owned devices.</li></ul><h2 id="a-short-reading-list" tabindex="-1">A short reading list <a class="header-anchor" href="#a-short-reading-list" aria-label="Permalink to &quot;A short reading list&quot;">​</a></h2><ul><li><strong>Hardware Hacking Handbook</strong> (O&#39;Flynn + van Woudenberg, No Starch 2021) — the consolidated text.</li><li><strong>The IoT Hacker&#39;s Handbook</strong> (Aditya Gupta, Apress 2019) — more emphasis on protocol/firmware level.</li><li><strong>Practical Hardware Pentesting</strong> (Jean-Georges Valle, Packt 2021) — methodology-focused.</li><li><strong>OWASP IoT Top 10</strong> — taxonomy of common IoT mistakes. Useful for triage.</li><li><strong>Specific conference talks</strong>: Black Hat USA / EU, DEF CON Hardware Hacking Village, Hardwear.io, recon.cx, CCC. Most are on YouTube.</li><li><strong>CVE databases</strong> — searching by chip family (<code>stm32 cve</code>, <code>esp32 cve</code>, <code>nrf52 cve</code>) returns the published vulnerabilities of the part you&#39;re working on. Many include enough detail to reproduce.</li></ul><p>A working engineer doing security review of embedded devices develops an instinct for these things over time. The shortcut is to read every Hardware Hacking Handbook chapter once a year and keep a notes file of the patterns you&#39;ve seen in the wild.</p>',46)])])}const g=t(s,[["render",i]]);export{p as __pageData,g as default};
