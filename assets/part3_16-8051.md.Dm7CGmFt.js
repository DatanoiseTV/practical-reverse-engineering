import{c as a,Q as t,j as s,m as n}from"./chunks/framework.DxgWTcNC.js";const g=JSON.parse('{"title":"8051","description":"","frontmatter":{},"headers":[],"relativePath":"part3/16-8051.md","filePath":"part3/16-8051.md","lastUpdated":null}'),i={name:"part3/16-8051.md"};function o(r,e,d,c,l,p){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="_8051" tabindex="-1">8051 <a class="header-anchor" href="#_8051" aria-label="Permalink to &quot;8051&quot;">​</a></h1><p>The 8051 is an 8-bit microcontroller core that turned 45 in the year this book was written, and it is still the architecture inside a remarkable amount of new silicon. Sub-GHz and 2.4 GHz radio SoCs (Nordic nRF24LE1 — a proprietary 2.4 GHz radio core, <em>not</em> Bluetooth; Realtek&#39;s 8051-core BLE parts; the Telink TLSR series for BLE HID), USB controllers (Cypress / Infineon FX2 / FX3 are 8051-based, sort of), wireless mice and keyboards, legacy industrial controllers, and an unending river of clone microcontrollers from Asian fabs all use 8051 cores.</p><p>If you are reverse engineering 2.4 GHz HID dongles, BLE keyboards (on the Telink TLSR family), cheap RF dongles, or older industrial gear, you will meet 8051. This chapter covers the parts of the architecture that matter for r2 work, the loading recipes, and the gotchas that separate &quot;working with an 8051 binary&quot; from &quot;working with anything else&quot;.</p><h2 id="architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#architectural-overview" aria-label="Permalink to &quot;Architectural overview&quot;">​</a></h2><p>The 8051 is genuinely strange by modern standards:</p><ul><li><strong>Harvard architecture.</strong> Code and data live in separate address spaces with separate instruction families. Code memory (<code>CODE</code> / <code>PMEM</code>) is read-only at runtime; data memory (<code>IDATA</code> / internal RAM, <code>XDATA</code> / external RAM, <code>SFR</code> / special function registers) is read-write.</li><li><strong>Banked register set.</strong> R0..R7 are 8 registers, but there are 4 banks of them. The current bank is selected by bits in PSW. Switching banks is fast; debugging switches is annoying.</li><li><strong>Internal data RAM.</strong> Classic 8051 has 128 bytes (<code>0x00</code>–<code>0x7F</code>). The 8052 and most modern derivatives add a second 128 bytes at <code>0x80</code>–<code>0xFF</code> that is only reachable via <em>indirect</em> addressing (<code>MOV @Ri</code>); <em>direct</em> addressing of <code>0x80</code>–<code>0xFF</code> reaches the SFR space instead. So <code>MOV A, 0x90</code> reads SFR P1; <code>MOV R0, #0x90; MOV A, @R0</code> reads upper-128 IRAM byte 0x90 on an 8052.</li><li><strong>Bit-addressable region.</strong> A 16-byte block of IDATA (bytes 0x20–0x2F) is bit-addressable: each of the 128 bits has its own address in the bit-address space.</li><li><strong>64 KiB code address space</strong> in classic 8051, expanded with banking on most modern derivatives. Different vendors implement banking differently (XDATA paging, EXTRA codes for the upper banks).</li><li><strong>Big-endian encoding for instruction immediates.</strong> <code>LJMP</code> and <code>LCALL</code> encode their 16-bit target high-byte-first, and <code>MOV DPTR, #imm16</code> encodes DPH before DPL. The CPU itself is byte-addressed and has no inherent endianness for data; multi-byte data layout (<code>int</code>, <code>long</code> in C) is compiler-defined — SDCC and Keil both default to big-endian, which is the opposite of every common 32-bit embedded platform.</li><li><strong>No stack pointer relative addressing.</strong> All addressing is direct, indirect through R0/R1, indirect through DPTR, or PC-relative for jumps. No SP-relative loads.</li><li><strong>A single accumulator</strong> (<code>A</code>) for arithmetic. Most operations go through it.</li></ul><p>For r2:</p><ul><li>The architecture name is <code>8051</code>.</li><li>The <code>bits</code> is <code>8</code> for the canonical case but the <code>cpu</code> flag often needs setting to a derivative that knows the right SFR map.</li></ul><h2 id="loading" tabindex="-1">Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;Loading&quot;">​</a></h2><p>A raw 8051 image:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a 8051 -b 8 -m 0x0 firmware.bin</span></span></code></pre></div><p>If you have an Intel HEX file (most common 8051 distribution format):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 firmware.hex                    # r2 reads the addresses from the file</span></span></code></pre></div><p>Many 8051 binaries are &gt;64 KiB and use bank switching. R2&#39;s 8051 plugin has limited support for banks; when you exceed 64 KiB you typically have to load each bank as its own mapping and treat bank-switch calls as opaque jumps.</p><p>Set the SFR map via the <code>cpu</code> flag:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a 8051 -b 8 -c 8051 -m 0x0 firmware.bin     # generic 8051 SFRs</span></span>
<span class="line"><span>$ r2 -a 8051 -b 8 -c 8052 firmware.bin            # 8052 SFRs (extra timer 2)</span></span>
<span class="line"><span>$ r2 -a 8051 -b 8 -c at89s51 firmware.bin         # Atmel-specific</span></span></code></pre></div><p>For chips with vendor-specific SFRs (Nordic nRF24LE1, Telink TLSR, Realtek RTL8762), check <code>e asm.cpu = ?</code> for available variants. If your specific chip is not listed, pick the closest cousin and manually flag the vendor-specific SFRs.</p><h2 id="sfrs-and-addressing" tabindex="-1">SFRs and addressing <a class="header-anchor" href="#sfrs-and-addressing" aria-label="Permalink to &quot;SFRs and addressing&quot;">​</a></h2><p>Special function registers live in addresses 0x80–0xFF in IDATA. The classic 8051 SFR map:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; f sfr.P0     = 0x80</span></span>
<span class="line"><span>[0x...]&gt; f sfr.SP     = 0x81</span></span>
<span class="line"><span>[0x...]&gt; f sfr.DPL    = 0x82</span></span>
<span class="line"><span>[0x...]&gt; f sfr.DPH    = 0x83</span></span>
<span class="line"><span>[0x...]&gt; f sfr.PCON   = 0x87</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TCON   = 0x88</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TMOD   = 0x89</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TL0    = 0x8A</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TL1    = 0x8B</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TH0    = 0x8C</span></span>
<span class="line"><span>[0x...]&gt; f sfr.TH1    = 0x8D</span></span>
<span class="line"><span>[0x...]&gt; f sfr.P1     = 0x90</span></span>
<span class="line"><span>[0x...]&gt; f sfr.SCON   = 0x98</span></span>
<span class="line"><span>[0x...]&gt; f sfr.SBUF   = 0x99</span></span>
<span class="line"><span>[0x...]&gt; f sfr.P2     = 0xA0</span></span>
<span class="line"><span>[0x...]&gt; f sfr.IE     = 0xA8</span></span>
<span class="line"><span>[0x...]&gt; f sfr.P3     = 0xB0</span></span>
<span class="line"><span>[0x...]&gt; f sfr.IP     = 0xB8</span></span>
<span class="line"><span>[0x...]&gt; f sfr.PSW    = 0xD0</span></span>
<span class="line"><span>[0x...]&gt; f sfr.A      = 0xE0     ; accumulator</span></span>
<span class="line"><span>[0x...]&gt; f sfr.B      = 0xF0</span></span></code></pre></div><p>Vendor-specific extensions add radio control registers, timers, ADCs, USB endpoint registers, and so on at addresses 0xA8–0xFF. Get the TRM, transcribe the SFR table to flags, and reload.</p><h2 id="instruction-set-highlights" tabindex="-1">Instruction set highlights <a class="header-anchor" href="#instruction-set-highlights" aria-label="Permalink to &quot;Instruction set highlights&quot;">​</a></h2><p>The 8051 has about 110 instructions. The ones you see most:</p><table tabindex="0"><thead><tr><th>Mnemonic</th><th>Meaning</th></tr></thead><tbody><tr><td><code>MOV A, #imm</code></td><td>move immediate to accumulator</td></tr><tr><td><code>MOV A, Rn</code></td><td>move from register</td></tr><tr><td><code>MOV A, @Ri</code></td><td>indirect through R0 or R1</td></tr><tr><td><code>MOVX A, @DPTR</code></td><td>read XDATA at DPTR (external memory)</td></tr><tr><td><code>MOVC A, @A+DPTR</code></td><td>read CODE at DPTR + A (table lookup)</td></tr><tr><td><code>MOVC A, @A+PC</code></td><td>read CODE relative to PC</td></tr><tr><td><code>LCALL addr</code></td><td>long call (16-bit address)</td></tr><tr><td><code>ACALL addr</code></td><td>absolute call (11-bit, within 2 KiB page)</td></tr><tr><td><code>LJMP addr</code></td><td>long jump</td></tr><tr><td><code>AJMP addr</code></td><td>absolute jump (11-bit)</td></tr><tr><td><code>SJMP rel</code></td><td>short jump (8-bit relative, ±128)</td></tr><tr><td><code>JZ</code>, <code>JNZ</code></td><td>jump if A is zero / non-zero</td></tr><tr><td><code>CJNE</code></td><td>compare and jump if not equal</td></tr><tr><td><code>DJNZ</code></td><td>decrement and jump if not zero</td></tr><tr><td><code>RET</code>, <code>RETI</code></td><td>return / return from interrupt</td></tr><tr><td><code>SETB bit</code></td><td>set bit</td></tr><tr><td><code>CLR bit</code></td><td>clear bit</td></tr><tr><td><code>MOV C, bit</code></td><td>move bit to carry</td></tr><tr><td><code>JB</code>, <code>JNB</code>, <code>JBC</code></td><td>jump if bit set / not set / set then clear</td></tr></tbody></table><p><code>MOVC @A+DPTR</code> is the lookup-table primitive; you will see it for every state machine transition table, every UTF-8 decode, every cosine table.</p><h2 id="a-typical-8051-function" tabindex="-1">A typical 8051 function <a class="header-anchor" href="#a-typical-8051-function" aria-label="Permalink to &quot;A typical 8051 function&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>; void uart_send(unsigned char c)</span></span>
<span class="line"><span>0x0400:  C0 E0           push  ACC</span></span>
<span class="line"><span>0x0402:  90 12 34        mov   DPTR, #0x1234   ; status reg</span></span>
<span class="line"><span>0x0405:  E0              movx  A, @DPTR</span></span>
<span class="line"><span>0x0406:  30 E0 FB        jnb   ACC.0, 0x0404   ; wait for TX ready</span></span>
<span class="line"><span>0x0409:  90 12 35        mov   DPTR, #0x1235   ; data reg</span></span>
<span class="line"><span>0x040C:  EF              mov   A, R7           ; arg in R7</span></span>
<span class="line"><span>0x040D:  F0              movx  @DPTR, A</span></span>
<span class="line"><span>0x040E:  D0 E0           pop   ACC</span></span>
<span class="line"><span>0x0410:  22              ret</span></span></code></pre></div><p>Reading this:</p><ul><li>The function takes its argument in <code>R7</code> (Keil/SDCC convention; the C compiler&#39;s choice).</li><li>It polls a status register at XDATA <code>0x1234</code>.</li><li>When bit 0 is set, it writes the character to data register at <code>0x1235</code>.</li><li>Push/pop of <code>A</code> is to preserve it across the call (compiler&#39;s choice).</li></ul><p>R2 will produce roughly this output. The decompiler (r2ghidra) handles 8051 reasonably well for simple functions; complex ones tend to produce code that is correct but ugly because of the constant shuttling through <code>A</code>.</p><h2 id="calling-conventions" tabindex="-1">Calling conventions <a class="header-anchor" href="#calling-conventions" aria-label="Permalink to &quot;Calling conventions&quot;">​</a></h2><p>8051 calling convention is whatever the C compiler decided. For SDCC (the most common open-source toolchain):</p><ul><li>First argument in <code>DPL</code>, <code>DPH</code> for pointer-class types; <code>R7</code> for <code>char</code>; <code>R7:R6</code> for <code>int</code>; <code>R7:R6:R5:R4</code> for <code>long</code>.</li><li>Subsequent arguments on the stack or in fixed memory locations.</li><li>Return values in the same registers.</li><li>Reentrant functions (compiled with <code>__reentrant</code>) use the stack for everything.</li></ul><p>For Keil C51:</p><ul><li>Args in <code>R7..R3</code> then in fixed memory.</li><li>Different mangling for parameter passing.</li></ul><p>R2 does not always guess the convention right. Set per-function:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afc sdcc8051 @ sym.foo</span></span>
<span class="line"><span>[0x...]&gt; afc keil8051 @ sym.bar</span></span></code></pre></div><p>If the right cc name is not present in <code>afcl</code>, define one in <code>~/.config/radare2/cc.sdb</code> (the calling-convention database).</p><h2 id="xdata-idata-code-telling-them-apart-in-disassembly" tabindex="-1">XDATA, IDATA, CODE: telling them apart in disassembly <a class="header-anchor" href="#xdata-idata-code-telling-them-apart-in-disassembly" aria-label="Permalink to &quot;XDATA, IDATA, CODE: telling them apart in disassembly&quot;">​</a></h2><p>Memory accesses in 8051 are explicit about which space they touch:</p><ul><li><code>MOV A, R0</code> — internal R-bank</li><li><code>MOV A, @R0</code> — IDATA at the address in R0</li><li><code>MOVX A, @DPTR</code> — XDATA at DPTR</li><li><code>MOVX A, @R0</code> — XDATA at R0 (low byte; high byte is P2)</li><li><code>MOVC A, @A+DPTR</code> — CODE at A+DPTR</li></ul><p>When you see <code>MOVX</code>, you are looking at external memory (often peripheral registers or external SRAM). Build a table of XDATA peripheral addresses for your chip.</p><h2 id="_8051-in-bluetooth-chips-a-worked-recipe" tabindex="-1">8051 in Bluetooth chips: a worked recipe <a class="header-anchor" href="#_8051-in-bluetooth-chips-a-worked-recipe" aria-label="Permalink to &quot;8051 in Bluetooth chips: a worked recipe&quot;">​</a></h2><p>Suppose you have firmware for an old Nordic nRF24LE1 keyboard. nRF24LE1 is an 8051 with a 2.4 GHz radio. Reverse engineering steps:</p><ol><li>Get the chip&#39;s TRM (publicly available from Nordic).</li><li>Build an SFR map from the radio control registers (RFCON, RFDAT, RFCSEN) and the standard 8051 SFRs.</li><li>Load:</li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a 8051 -b 8 -c 8051 -m 0x0 keyboard.bin</span></span>
<span class="line"><span>[0x0]&gt; .nrf24le1_sfr.r2     # apply the SFR flags</span></span>
<span class="line"><span>[0x0]&gt; aaa</span></span></code></pre></div><ol start="4"><li>Look at the reset vector (address 0):</li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x0]&gt; pd 4 @ 0x0</span></span>
<span class="line"><span>0x0000  LJMP 0x0064          ; reset vector</span></span>
<span class="line"><span>0x0003  LJMP 0x0070          ; external interrupt 0</span></span>
<span class="line"><span>0x000B  LJMP 0x0080          ; timer 0 interrupt</span></span>
<span class="line"><span>...</span></span></code></pre></div><ol start="5"><li><p>Define functions at each vector target.</p></li><li><p>Find the radio TX path:</p></li></ol><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x0]&gt; /c MOVX @DPTR, A      # search for XDATA writes</span></span>
<span class="line"><span>[0x0]&gt; # find one at the radio data register</span></span></code></pre></div><ol start="7"><li>Trace backwards from the radio access to the keyboard scan code handler.</li></ol><p>This pattern — start at the SFR access of the most distinctive peripheral, trace back — is the standard 8051 RE workflow. The opposite (top-down from main) is harder because the call graph in 8051 firmware is often deep and the function names are gone.</p><h2 id="_8051-gotchas" tabindex="-1">8051 gotchas <a class="header-anchor" href="#_8051-gotchas" aria-label="Permalink to &quot;8051 gotchas&quot;">​</a></h2><p><strong>Bank switching for code &gt;64 KiB.</strong> Some chips have a &quot;code bank&quot; register that selects which 32 KiB or 64 KiB region of flash is mapped into the upper half of code space. R2&#39;s 8051 plugin treats the code space as a flat 64 KiB. To analyse a multi-bank binary, load each bank separately.</p><p><strong>Bit-addressing is stateful.</strong> A bit address like <code>0x86</code> could mean &quot;bit 6 of byte 0x20+0x10&quot; depending on the address range. R2 shows them; cross-reference with the SFR map.</p><p><strong>Compiler-generated stubs everywhere.</strong> SDCC and Keil emit helper routines for 16-bit arithmetic, soft-multiply, soft-divide, long-to-string, etc. They often live at fixed addresses and have no symbols. Build a per-compiler signature DB and apply it.</p><p><strong>No real stack frames.</strong> Local variables live in fixed memory locations the compiler allocated at compile time, not on the stack. The decompiler shows them as global accesses; you have to recognise that the global &quot;g_var_4&quot; used only inside one function is actually that function&#39;s local variable.</p><p><strong>Interrupts switch banks automatically.</strong> ISRs commonly run with register bank 1 (or 2 or 3) selected; the main code runs with bank 0. A function that reads <code>R0</code> may be reading bank 1&#39;s R0 if it is inside an ISR. Check the bank-select bits in PSW context.</p><p>8051 reverse engineering is slow. There is no shortcut; the architecture is unfriendly, the toolchain conventions are inconsistent across vendors, and the firmware is usually written by people working at the limits of what the chip can do, which means clever, dense, and weakly commented even in source. r2 is a fine tool for it; the bottleneck is the architecture, not the tooling.</p>`,59)])])}const u=a(i,[["render",o]]);export{g as __pageData,u as default};
