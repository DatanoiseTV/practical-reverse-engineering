import{c as a,Q as s,j as t,m as n}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"MIPS (Routers and Embedded Linux)","description":"","frontmatter":{},"headers":[],"relativePath":"part3/17-mips.md","filePath":"part3/17-mips.md","lastUpdated":null}'),o={name:"part3/17-mips.md"};function i(d,e,c,r,l,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="mips-routers-and-embedded-linux" tabindex="-1">MIPS (Routers and Embedded Linux) <a class="header-anchor" href="#mips-routers-and-embedded-linux" aria-label="Permalink to &quot;MIPS (Routers and Embedded Linux)&quot;">​</a></h1><p>MIPS is the architecture in the back office of the consumer internet. Most home/SMB routers, many cable modems, NAS boxes, and a long tail of industrial gateways run a MIPS Linux. Atheros, Broadcom, MediaTek, Realtek, and Lantiq SoCs all spawned generations of MIPS-based gear. The chips are mostly out of production but the firmware is in active field service for years to come, and a substantial chunk of CVE research targets it.</p><p>This chapter covers MIPS as you encounter it in router-class firmware. The Linux device-driver and DTB material in Chapter 18 applies to MIPS routers directly; read both.</p><h2 id="architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#architectural-overview" aria-label="Permalink to &quot;Architectural overview&quot;">​</a></h2><p>The relevant MIPS variants in embedded Linux:</p><ul><li><strong>MIPS32 R1/R2</strong> — 32-bit, the common case for MT76xx, AR9xxx, BCM63xx.</li><li><strong>MIPS32 R6</strong> — newer, simplified instruction set, fewer compatible binaries. Some Lantiq parts.</li><li><strong>MIPS64</strong> — used in some high-end gear (Cavium Octeon, some Broadcom network processors). Layout differs significantly.</li><li><strong>microMIPS / MIPS16e</strong> — alternative ISA encodings with mixed 16/32-bit instructions for size-constrained code. They are <em>separate encoding tables</em> signalled by the ISA-mode bit (the low bit of the jump target); the CPU switches between standard MIPS and the alternative encoding via <code>JR.HB</code> to an address with the ISA bit set. Common in embedded ROM.</li></ul><p>Big-endian is the default in the wild for legacy reasons (most historical MIPS firmware is BE), but little-endian MIPS exists too (some MediaTek, some Cavium). Check both.</p><p>Key facts:</p><ul><li>32 general-purpose registers, conventional names below.</li><li><strong>Branch delay slots</strong> — the instruction <em>after</em> a branch executes before the branch is taken. Critical for reading flow.</li><li><strong>No condition flags.</strong> Conditional branches encode the comparison.</li><li><strong>Load delay slot</strong> historically existed too (MIPS I); modern MIPS32 hardware interlocks it. Compilers and assemblers respect the historical convention by interleaving.</li><li><strong><code>$t9</code> (=<code>$25</code>) holds the called function&#39;s address</strong> in standard PIC code, by convention. This matters for indirect calls.</li><li><strong>Lots of pseudo-instructions</strong> that the assembler expands. R2 shows the underlying instructions.</li></ul><h2 id="register-names" tabindex="-1">Register names <a class="header-anchor" href="#register-names" aria-label="Permalink to &quot;Register names&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ABI</th><th>Number</th><th>Role</th></tr></thead><tbody><tr><td><code>$zero</code></td><td><code>$0</code></td><td>hardwired zero</td></tr><tr><td><code>$at</code></td><td><code>$1</code></td><td>assembler temporary</td></tr><tr><td><code>$v0</code>–<code>$v1</code></td><td><code>$2</code>,<code>$3</code></td><td>return values</td></tr><tr><td><code>$a0</code>–<code>$a3</code></td><td><code>$4</code>–<code>$7</code></td><td>function arguments</td></tr><tr><td><code>$t0</code>–<code>$t7</code></td><td><code>$8</code>–<code>$15</code></td><td>temporaries (caller-saved)</td></tr><tr><td><code>$s0</code>–<code>$s7</code></td><td><code>$16</code>–<code>$23</code></td><td>saved (callee-saved)</td></tr><tr><td><code>$t8</code>–<code>$t9</code></td><td><code>$24</code>,<code>$25</code></td><td>more temporaries (<code>$t9</code> is called fn ptr)</td></tr><tr><td><code>$k0</code>–<code>$k1</code></td><td><code>$26</code>,<code>$27</code></td><td>reserved for kernel</td></tr><tr><td><code>$gp</code></td><td><code>$28</code></td><td>global pointer</td></tr><tr><td><code>$sp</code></td><td><code>$29</code></td><td>stack pointer</td></tr><tr><td><code>$fp</code> / <code>$s8</code></td><td><code>$30</code></td><td>frame pointer / saved</td></tr><tr><td><code>$ra</code></td><td><code>$31</code></td><td>return address</td></tr></tbody></table><p>R2 uses ABI names by default. Set:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e asm.syntax = att         # ABI names</span></span>
<span class="line"><span>[0x...]&gt; e asm.syntax = none        # raw $N numbers</span></span></code></pre></div><p>ABI is more readable.</p><h2 id="loading" tabindex="-1">Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;Loading&quot;">​</a></h2><p>A clean ELF (kernel module, userspace binary, OpenWRT package):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 ./busybox</span></span>
<span class="line"><span>[0x...]&gt; i</span></span>
<span class="line"><span>arch     mips</span></span>
<span class="line"><span>bits     32</span></span>
<span class="line"><span>endian   big       # or little</span></span></code></pre></div><p>A raw flash dump from a router:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ binwalk firmware.bin                   # find the boundaries</span></span>
<span class="line"><span>DECIMAL    HEXADECIMAL  DESCRIPTION</span></span>
<span class="line"><span>0          0x0          uImage header, ...</span></span>
<span class="line"><span>64         0x40         LZMA compressed data</span></span>
<span class="line"><span>1048576    0x100000     Squashfs filesystem</span></span></code></pre></div><p>Two-stage extraction:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ binwalk -e firmware.bin               # auto-extract</span></span>
<span class="line"><span>$ ls _firmware.bin.extracted/</span></span>
<span class="line"><span>40       40.lzma   100000.squashfs   ...</span></span>
<span class="line"><span>$ unsquashfs _firmware.bin.extracted/100000.squashfs</span></span>
<span class="line"><span>$ ls squashfs-root/</span></span>
<span class="line"><span>bin/  etc/  lib/  sbin/  usr/  var/  www/</span></span></code></pre></div><p>You now have the root filesystem. Inside, <code>bin/</code>, <code>sbin/</code>, and <code>usr/sbin/</code> hold the binaries you reverse engineer; <code>lib/</code> has shared libraries. The kernel itself is in <code>40.lzma</code> (decompress with <code>lzma</code> or <code>unlzma</code>, then <code>r2</code> it).</p><p>Load a userspace binary:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 squashfs-root/sbin/httpd</span></span>
<span class="line"><span>[0x...]&gt; aaa</span></span></code></pre></div><p>For raw kernel:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ unlzma -c 40.lzma &gt; vmlinux.bin</span></span>
<span class="line"><span>$ r2 -a mips -b 32 -e cfg.bigendian=true -m 0x80000000 vmlinux.bin</span></span></code></pre></div><p>The kernel load address is typically <code>0x80000000</code> for 32-bit MIPS Linux (the unmapped, cached kernel segment). Confirm by looking for the kernel&#39;s first instructions (typically a <code>lui</code> setting up the stack pointer, then a <code>jal</code> to a setup function).</p><h2 id="big-vs-little-endian" tabindex="-1">Big vs little endian <a class="header-anchor" href="#big-vs-little-endian" aria-label="Permalink to &quot;Big vs little endian&quot;">​</a></h2><p>Get this wrong and the disassembly is plausible but wrong. The fastest check: a MIPS instruction always has its 6-bit opcode in the top 6 bits when read in the <em>native</em> endianness. If you read 4 bytes from a likely-MIPS region and the top 6 bits give a known opcode (0 = SPECIAL, 8 = ADDI, 9 = ADDIU, 0x23 = LW, 0x2B = SW, 0x3 = JAL), you are matching the endianness; if you get garbage opcodes, swap.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ xxd firmware.bin | head -1</span></span>
<span class="line"><span>00000000: 27bd ffe0 afbf 001c afb0 0018 ...</span></span></code></pre></div><p><code>0x27</code> is <code>0010 0111</code> — top 6 bits <code>001001</code> = 0x09 (ADDIU). That is a frame allocation: <code>ADDIU $sp, $sp, -0x20</code>. Big-endian.</p><p>If we read it little-endian we get <code>0xffffbd27</code> which decodes to nonsense.</p><p>Switch in r2:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e cfg.bigendian = true</span></span>
<span class="line"><span>[0x...]&gt; e cfg.bigendian = false</span></span></code></pre></div><h2 id="branch-delay-slots" tabindex="-1">Branch delay slots <a class="header-anchor" href="#branch-delay-slots" aria-label="Permalink to &quot;Branch delay slots&quot;">​</a></h2><p>The line of code below a branch executes <em>before</em> the branch is taken. Disassembly:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x000400c0  beqz   $a0, 0x4012c          # if a0==0 jump</span></span>
<span class="line"><span>0x000400c4  addiu  $sp, $sp, -0x20       # this runs FIRST</span></span>
<span class="line"><span>0x000400c8  sw     $ra, 0x1c($sp)</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>0x0004012c  ...                          # branch target</span></span></code></pre></div><p>R2 shows the delay slot indented or commented:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x000400c0      beqz $a0, 0x4012c</span></span>
<span class="line"><span>                addiu $sp, $sp, -0x20    ; &lt;delay&gt;</span></span></code></pre></div><p>The decompiler handles delay slots correctly; you do not have to think about them when reading C output. But if you write patches by hand (Chapter 22), respect the delay slot — putting a NOP in the wrong place breaks behavior subtly.</p><h2 id="calling-convention-o32-abi" tabindex="-1">Calling convention (O32 ABI) <a class="header-anchor" href="#calling-convention-o32-abi" aria-label="Permalink to &quot;Calling convention (O32 ABI)&quot;">​</a></h2><p>The dominant MIPS userspace ABI is O32:</p><ul><li>Arguments: <code>$a0..$a3</code> (first 4), then stack</li><li>Return: <code>$v0</code> (and <code>$v1</code> for 64-bit returns)</li><li>Caller-saved: <code>$at</code>, <code>$v0..$v1</code>, <code>$a0..$a3</code>, <code>$t0..$t9</code></li><li>Callee-saved: <code>$s0..$s7</code>, <code>$sp</code>, <code>$fp</code>, <code>$ra</code></li><li><code>$gp</code> — in PIC O32 (the common case for Linux userland), <code>$gp</code> is caller-saved: every PIC function call may clobber it, so the caller spills it across calls if needed. In non-PIC code, <code>$gp</code> is treated as effectively constant after boot.</li><li>Stack alignment: 8-byte</li><li>The first 16 bytes of the caller&#39;s stack are reserved for spilling <code>$a0..$a3</code> if needed</li></ul><p>For r2:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e anal.cc = o32           # default</span></span>
<span class="line"><span>[0x...]&gt; afc o32 @ sym.foo</span></span></code></pre></div><p>Other ABIs (N32, N64) are uncommon for embedded; you mostly see them on Cavium and high-end Linux MIPS systems.</p><h2 id="position-independent-code-and-gp" tabindex="-1">Position-independent code and <code>$gp</code> <a class="header-anchor" href="#position-independent-code-and-gp" aria-label="Permalink to &quot;Position-independent code and \`$gp\`&quot;">​</a></h2><p>PIC MIPS code uses <code>$gp</code> (global pointer) to access globals. The setup sequence at function entry:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x000400c0  lui    $gp, 0x0c</span></span>
<span class="line"><span>0x000400c4  addiu  $gp, $gp, -0x6f60     # $gp = 0xb0a0</span></span>
<span class="line"><span>0x000400c8  addu   $gp, $gp, $t9         # $gp += $t9 (function PC)</span></span></code></pre></div><p>After this, accesses through <code>$gp</code> reach the global offset table. Indirect calls go through <code>$t9</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x...        lw    $t9, -0x7fdc($gp)     # load function ptr from GOT</span></span>
<span class="line"><span>0x...        jalr  $t9                   # call</span></span>
<span class="line"><span>0x...        nop                         # delay slot</span></span></code></pre></div><p>R2 follows these correctly when the binary is loaded with the right mappings. For raw kernel images (which are not PIC), <code>$gp</code> is set once at boot and used for <code>.sdata</code> accesses across the whole kernel.</p><h2 id="linux-router-specifics" tabindex="-1">Linux router specifics <a class="header-anchor" href="#linux-router-specifics" aria-label="Permalink to &quot;Linux router specifics&quot;">​</a></h2><p><strong>OpenWrt and similar.</strong> Most consumer-grade router firmware is OpenWrt-derived. The toolchain is <code>mips-openwrt-linux-musl-gcc</code> for modern builds; older devices use uClibc. Both have signature DBs you can build (Chapter 10).</p><p><strong>Init.</strong> <code>/sbin/init</code> calls <code>/etc/init.d/rcS</code> which loads everything. The order of services tells you the boot sequence; the binaries each service starts are the meat.</p><p><strong>Web UI.</strong> Almost always <code>/sbin/httpd</code> or <code>/usr/sbin/uhttpd</code>. Often the most fruitful target for vulnerability research.</p><p><strong>Custom binaries.</strong> Vendor-specific config daemons (<code>acsd</code>, <code>wlctld</code>, <code>upgrade.cgi</code>) are where the bugs are. Look for ioctls into custom kernel modules.</p><p><strong>WiFi driver.</strong> Always closed-source for older Broadcom/Qualcomm parts. The driver <code>.ko</code> is in <code>/lib/modules/$(uname -r)/</code>. Reverse engineering one is a major undertaking; see Chapter 18.</p><p><strong>Custom proprietary protocols.</strong> A lot of router firmware speaks non-standard protocols on TCP/UDP for cloud management, OEM provisioning, or &quot;phone home&quot; telemetry. Find them by:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; izz ~ /etc/                # config file paths</span></span>
<span class="line"><span>[0x...]&gt; izz ~ http://              # URLs</span></span>
<span class="line"><span>[0x...]&gt; izz ~ &lt;port-ish numbers&gt;</span></span>
<span class="line"><span>[0x...]&gt; axt @ sym.imp.bind         # listening sockets</span></span>
<span class="line"><span>[0x...]&gt; axt @ sym.imp.recvfrom     # UDP receivers</span></span></code></pre></div><h2 id="mips-gotchas" tabindex="-1">MIPS gotchas <a class="header-anchor" href="#mips-gotchas" aria-label="Permalink to &quot;MIPS gotchas&quot;">​</a></h2><p><strong><code>lui</code> + <code>ori</code>/<code>addiu</code> is the constant materialisation pattern.</strong> A 32-bit address split into upper-16 and lower-16. R2 collapses these into a single comment showing the resolved value, but if your load address is wrong, both halves point to nothing.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x...  lui   $a0, 0x402             ; upper half</span></span>
<span class="line"><span>0x...  addiu $a0, $a0, 0x12c        ; lower half -&gt; $a0 = 0x4020012c</span></span></code></pre></div><p><strong><code>jal</code> (Jump And Link) destination is in the same 256 MiB region.</strong> The 26-bit target is left-shifted by 2 and concatenated with the top 4 bits of <code>PC+4</code> to form the 32-bit destination (<code>(PC+4)[31:28] || target[25:0] || 00</code>). If your binary spans a 256 MiB boundary, <code>jal</code> cannot reach across, so the compiler emits <code>jalr $t9</code> style indirect calls. This trips up automatic xref recovery; check <code>axt</code> for missing edges.</p><p><strong>MIPS16e and microMIPS use the same opcode space as base MIPS, in different bits.</strong> Mixed-mode binaries exist; r2 needs hints (<code>ahb 16</code>) to switch.</p><p><strong>The <code>$t9 = function address</code> convention is <em>almost</em> always honoured.</strong> When it is not (some hand-written assembly, some optimised cases), an indirect call through <code>$t9</code> may go to the wrong place because the caller did not load the correct value. If the decompiler&#39;s reconstructed call graph looks broken, check <code>$t9</code>.</p><p><strong>Branch delay slots can be NOPped or filled with useful work.</strong> A useful instruction in the delay slot is a perfectly valid pattern, not a bug.</p><p>A typical router firmware analysis goes: extract with binwalk, mount the squashfs, identify the web-facing daemon, load it in r2, find the request dispatch table, trace each handler. The combination of clear binary structure, often-buggy code, and remote attack surface makes MIPS routers one of the highest-value targets in embedded security research today.</p>`,68)])])}const g=a(o,[["render",i]]);export{u as __pageData,g as default};
