import{c as t,Q as a,j as o,m as i}from"./chunks/framework.DxgWTcNC.js";const m=JSON.parse('{"title":"Side-Channel Analysis","description":"","frontmatter":{},"headers":[],"relativePath":"part6/32-side-channels.md","filePath":"part6/32-side-channels.md","lastUpdated":null}'),n={name:"part6/32-side-channels.md"};function s(r,e,l,c,h,d){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="side-channel-analysis" tabindex="-1">Side-Channel Analysis <a class="header-anchor" href="#side-channel-analysis" aria-label="Permalink to &quot;Side-Channel Analysis&quot;">​</a></h1><p>Where fault injection (Chapter 31) makes a chip misbehave to skip a check, <strong>side-channel analysis</strong> (SCA) listens to a chip behaving correctly and recovers secrets from the way it does so. Every CMOS gate dissipates a tiny amount of power when it switches; every computation takes a small, sometimes data-dependent amount of time; every nearby loop antenna picks up the emanations from the chip&#39;s power and bus activity. The amount of information leaked per operation is small. The amount of information leaked over millions of operations is, repeatedly, &quot;all of it.&quot;</p><p>SCA matters for reverse engineering for two reasons. First, it is how cryptographic keys come out of devices that otherwise refuse to release them. Second, it is how protocol details can be recovered from black-box devices without static analysis (timing differences in a &quot;PIN correct&quot; path vs a &quot;PIN wrong&quot; path leak the comparison structure).</p><p>This chapter is again grounded in published work, not invented exploits. The list of references at the end is the safest source material.</p><h2 id="what-side-channel-means" tabindex="-1">What &quot;side channel&quot; means <a class="header-anchor" href="#what-side-channel-means" aria-label="Permalink to &quot;What &quot;side channel&quot; means&quot;">​</a></h2><p>A side channel is information leaked through a channel that is not the intended output. The intended output of a smartcard&#39;s AES encryption is ciphertext. Its side channels include:</p><ul><li><strong>Time</strong> — how long the operation took. If the time depends on the key, time leaks the key.</li><li><strong>Power</strong> — the instantaneous current draw of the chip. Each switching transistor draws a small amount of power; the total trace correlates with the data being processed.</li><li><strong>EM radiation</strong> — the chip&#39;s switching produces small radio emanations. A coil held nearby (or just an oscilloscope probe on a magnetic loop) captures them.</li><li><strong>Cache state</strong> — on processors with caches, what&#39;s in the cache after the operation depends on which addresses the operation touched, which depends on the key.</li><li><strong>Acoustic emissions</strong> — capacitor whine, audible enough to matter in some pure cryptanalytic settings (Genkin et al., 2014, recovered RSA keys from acoustic emanations of a laptop).</li><li><strong>Power-supply pin voltage</strong> — variations on the power channel, but on the <em>target&#39;s</em> mains supply rather than its die.</li></ul><p>In practice the actionable channels are time, power, and EM.</p><h2 id="timing-attacks" tabindex="-1">Timing attacks <a class="header-anchor" href="#timing-attacks" aria-label="Permalink to &quot;Timing attacks&quot;">​</a></h2><p>The original side-channel attack. Kocher&#39;s 1996 paper &quot;Timing Attacks on Implementations of Diffie-Hellman, RSA, DSS, and Other Systems&quot; demonstrated that the running time of modular exponentiation reveals the secret exponent because individual multiply operations are key-bit-conditional. The attack is remote-feasible whenever the server returns a precise enough timing channel.</p><p>Modern relevance:</p><ul><li><strong>TLS implementations</strong> — historically vulnerable Lucky 13 / CBC padding-oracle / MAC-then-encrypt attacks all have timing components. Modern TLS stacks (BoringSSL, modern OpenSSL) are constant-time for the relevant operations; older or embedded TLS stacks may not be.</li><li><strong>Embedded password / PIN check</strong> — any <code>memcmp</code> against a secret leaks the position of the first mismatching byte through return time. The fix is <code>memcmp_consttime</code> (or <code>CRYPTO_memcmp</code> in OpenSSL parlance).</li><li><strong>HMAC verification</strong> — same issue.</li><li><strong>CPU-cache-based timing</strong> — Spectre, Meltdown, and the broader microarchitectural-side-channel family (FLUSH+RELOAD, PRIME+PROBE, Foreshadow, etc.). Mostly relevant on x86 and Cortex-A; less relevant on microcontroller targets that don&#39;t have multi-level caches.</li></ul><p>For embedded reverse engineering specifically, the high-yield timing target is the boot-time PIN/password check. Many bootloaders and product debug consoles compare against a string with <code>strcmp</code>, which short-circuits. Over a network with sub-millisecond timing resolution this can be exploited remotely; over a UART loopback with a logic analyser it is even faster.</p><h2 id="power-analysis" tabindex="-1">Power analysis <a class="header-anchor" href="#power-analysis" aria-label="Permalink to &quot;Power analysis&quot;">​</a></h2><p>The headline SCA technique. Two principal variants, both originally from Kocher, Jaffe, and Jun (&quot;Differential Power Analysis&quot;, 1999).</p><p><strong>Simple Power Analysis (SPA).</strong> Look at one power trace from one encryption with your eye (or a moving-average filter), and see distinct operations. A textbook example: RSA&#39;s square-and-multiply exponentiation has a &quot;square&quot; step that&#39;s always present and a &quot;multiply&quot; step that&#39;s present only when the current key bit is <code>1</code>. With high enough trace SNR, each bit of the secret exponent is visible as a different shape in the trace. SPA does not require many traces — sometimes one is enough — but it requires the leakage to be visible at the level of single operations.</p><p><strong>Differential / Correlation Power Analysis (DPA / CPA).</strong> Take thousands or millions of traces from different known plaintexts encrypted with the same unknown key. Compute the Pearson correlation between the measured power at each time sample and the predicted Hamming weight of some intermediate value (typically the AES SubBytes output) under each guess of one byte of the key. The correct key guess produces a sharp correlation peak at the time sample where the chip computed that intermediate; wrong guesses produce noise. Recover one key byte at a time. CPA against a software AES implementation on a Cortex-M typically succeeds with a few thousand to a few tens of thousands of traces, depending on SNR.</p><p>DPA/CPA does not require physical disassembly. A wire across the target&#39;s Vcore decoupling cap, an oscilloscope, and many traces are the entire physical setup. ChipWhisperer&#39;s purpose-built capture hardware automates the wire-and-scope into one device.</p><h2 id="em-analysis" tabindex="-1">EM analysis <a class="header-anchor" href="#em-analysis" aria-label="Permalink to &quot;EM analysis&quot;">​</a></h2><p>Replace the wire on the power rail with a small coil held over the package. The same information leaks via EM emanations, often with higher SNR because the coil can be positioned over the specific part of the die that does the operation (the FPU, the AES accelerator, the cache controller). EM analysis is what lets researchers attack secure elements with anti-tamper meshes on the power supply — the meshes do not contain the EM signal.</p><p>Practical attacks have been published against:</p><ul><li>AES implementations on Cortex-M (countless ChipWhisperer demos reproduce these).</li><li>RSA on smartcards.</li><li>TPM 2.0 implementations (Roca et al. &quot;ROCA&quot; attack against Infineon TPMs, partly leveraging side-channel observations on RSA-2048 key generation — though ROCA itself was a math weakness, not pure SCA).</li><li>Trusted Platform Module key extraction (multiple lab demonstrations).</li></ul><h2 id="the-tools" tabindex="-1">The tools <a class="header-anchor" href="#the-tools" aria-label="Permalink to &quot;The tools&quot;">​</a></h2><p><strong>ChipWhisperer</strong> (NewAE Technology). The same hardware as for FI (Chapter 31) is also the standard hobbyist SCA capture device. The Pro and Husky variants offer larger capture buffers and higher sample rates. The accompanying Jupyter tutorials walk through SPA, DPA, and CPA against bundled targets step-by-step.</p><p><strong>Oscilloscope + low-noise amplifier + decoupling-cap shunt.</strong> A modest scope (e.g., a Picoscope, Rigol, or Lecroy of recent vintage) at 100 MS/s plus a low-noise amplifier and a current shunt across the Vcore decoupling cap gives you everything ChipWhisperer Lite does, at higher cost and more setup friction.</p><p><strong>Riscure Inspector SCA.</strong> Professional commercial system. Drives arbitrary scopes, automates trace acquisition, and runs analytical attacks. Tens of thousands of dollars; used by certification labs.</p><p><strong>Software-only tooling.</strong> <code>pyscard</code> / <code>chipsec</code> for x86 target work; <code>lascar</code> and <code>scared</code> for analytical post-processing of captured traces; <code>pyESCA</code> for educational frame.</p><h2 id="workflow-against-an-aes-128-software-implementation" tabindex="-1">Workflow against an AES-128 software implementation <a class="header-anchor" href="#workflow-against-an-aes-128-software-implementation" aria-label="Permalink to &quot;Workflow against an AES-128 software implementation&quot;">​</a></h2><p>A reasonable first SCA exercise — also the running example in the ChipWhisperer tutorials:</p><ol><li><strong>Trigger.</strong> The target firmware toggles a GPIO right before the first SubBytes lookup of round 1. ChipWhisperer arms the capture on the trigger edge.</li><li><strong>Capture.</strong> Encrypt a few thousand random 16-byte plaintexts; capture a power trace for each (~thousands of samples per trace).</li><li><strong>Align.</strong> Software-align all traces against a common time reference (typically the trigger, but sometimes additional alignment is needed if the implementation has variable timing).</li><li><strong>Hypothesis.</strong> For each key-byte candidate (0..255) and each trace, compute the Hamming weight of the SBox output: <code>HW(sbox[plaintext_byte XOR key_byte])</code>.</li><li><strong>Correlation.</strong> For each time sample in the trace, compute the Pearson correlation between the column of Hamming-weight predictions across traces and the column of measured power values across traces.</li><li><strong>Pick the maximum.</strong> The correct key byte will produce a single sharp peak in correlation at the time sample corresponding to the SBox computation; the other 255 byte guesses will produce ~zero correlation everywhere.</li><li><strong>Repeat per byte.</strong> Recover the full 128-bit key one byte at a time. Total: 16 × CPA attacks, each cheap once the traces are captured.</li></ol><p>Done well with a clean implementation and decent SNR, this is a weekend project. Done badly — the implementation is masked, the clock is randomised, the chip has SCA countermeasures — it can be intractable.</p><h2 id="defences" tabindex="-1">Defences <a class="header-anchor" href="#defences" aria-label="Permalink to &quot;Defences&quot;">​</a></h2><p>The defender&#39;s playbook for SCA:</p><ul><li><strong>Constant-time implementations</strong> — <code>memcmp_consttime</code> for comparisons; constant-time AES via bit-slicing; constant-time RSA exponentiation via Montgomery ladder. The OpenSSL and BoringSSL implementations of common primitives are deliberately constant-time on modern platforms.</li><li><strong>Masking</strong> — split each secret intermediate into two or more shares whose XOR is the real value. Each share is itself uncorrelated with the secret; first-order DPA fails. Higher-order attacks (combining samples from multiple time points) can defeat masking but require many more traces.</li><li><strong>Hiding</strong> — randomise the timing of operations (insert random delays, shuffle the order of independent operations like AES SubBytes lookups). Forces the attacker to use more traces to average out the noise.</li><li><strong>Hardware crypto accelerators</strong> with built-in countermeasures — most modern microcontroller crypto blocks include some combination of masking, hiding, and dummy operations. Their effectiveness varies; certification (CAVP, FIPS, Common Criteria EAL levels) gives some evidence.</li><li><strong>Defensive coding</strong> — avoid <code>if (secret_byte == known) ...</code> patterns; use bitwise operations on the secret that compile to branchless code.</li></ul><h2 id="when-to-choose-sca-vs-fi" tabindex="-1">When to choose SCA vs. FI <a class="header-anchor" href="#when-to-choose-sca-vs-fi" aria-label="Permalink to &quot;When to choose SCA vs. FI&quot;">​</a></h2><ul><li>If you want a cryptographic <strong>key</strong>, SCA is the better tool. AES keys come out of software AES on a Cortex-M with CPA.</li><li>If you want to <strong>skip a check</strong> (PIN counter, signature verify, RDP enforcement), FI is faster.</li><li>If the chip has dedicated SCA countermeasures (modern secure element), neither hobbyist-grade tooling will succeed without significant additional engineering. Professional labs may.</li></ul><p>The two are complementary. A common attack chain on a hardware wallet or smartcard is to use FI to bypass an access check and then SCA to extract a key from the now-accessible operation.</p><h2 id="reading" tabindex="-1">Reading <a class="header-anchor" href="#reading" aria-label="Permalink to &quot;Reading&quot;">​</a></h2><ul><li><strong>Hardware Hacking Handbook</strong> (already cited). Chapters on SPA, DPA, and CPA with worked ChipWhisperer examples.</li><li><strong>The original Kocher papers</strong>: <ul><li>Kocher, &quot;Timing Attacks on Implementations of Diffie-Hellman, RSA, DSS, and Other Systems&quot; (1996).</li><li>Kocher, Jaffe, Jun, &quot;Differential Power Analysis&quot; (1999).</li></ul></li><li><strong>Mangard, Oswald, Popp, &quot;Power Analysis Attacks: Revealing the Secrets of Smart Cards&quot;</strong> (Springer, 2007). The standard academic textbook.</li><li><strong>CHES conference proceedings</strong> — annual academic state of the art.</li><li><strong>ChipWhisperer documentation</strong> at chipwhisperer.readthedocs.io.</li><li><strong>Riscure white papers</strong> at riscure.com/publications.</li><li><strong>lascar</strong> (<code>https://github.com/Ledger-Donjon/lascar</code>) — open source SCA analysis library, maintained by Ledger&#39;s Donjon security team. Code is a good way to understand the algorithms.</li></ul><p>A working engineer who finishes the ChipWhisperer tutorials, reads the Hardware Hacking Handbook, and skims the Kocher papers is positioned to take on most published SCA targets.</p>',40)])])}const u=t(n,[["render",s]]);export{m as __pageData,u as default};
