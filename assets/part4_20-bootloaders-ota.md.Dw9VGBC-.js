import{c as a,Q as t,j as s,m as i}from"./chunks/framework.DxgWTcNC.js";const m=JSON.parse('{"title":"Bootloaders, Image Headers, and OTA Blobs","description":"","frontmatter":{},"headers":[],"relativePath":"part4/20-bootloaders-ota.md","filePath":"part4/20-bootloaders-ota.md","lastUpdated":null}'),o={name:"part4/20-bootloaders-ota.md"};function n(r,e,l,d,p,c){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="bootloaders-image-headers-and-ota-blobs" tabindex="-1">Bootloaders, Image Headers, and OTA Blobs <a class="header-anchor" href="#bootloaders-image-headers-and-ota-blobs" aria-label="Permalink to &quot;Bootloaders, Image Headers, and OTA Blobs&quot;">​</a></h1><p>Most production embedded firmware is not a single binary; it is a chain. ROM bootloader -&gt; first-stage bootloader -&gt; second-stage bootloader -&gt; OS/RTOS -&gt; applications. Each stage may be signed, encrypted, compressed, or wrapped in a vendor-specific container. Understanding the chain matters because (a) the application is what you usually care about but the bootloader controls what it sees, (b) some of the most interesting bugs live in the bootloader, and (c) OTA update mechanisms are repeatedly the source of high-impact vulnerabilities.</p><p>This chapter covers how to recognise common image headers, how to unwrap and analyse them, and what to look for when the goal is the bootloader itself.</p><h2 id="the-boot-chain-in-miniature" tabindex="-1">The boot chain in miniature <a class="header-anchor" href="#the-boot-chain-in-miniature" aria-label="Permalink to &quot;The boot chain in miniature&quot;">​</a></h2><p>A typical Cortex-M boot:</p><ol><li><strong>ROM</strong> — vendor-burnt code. Configures clocks, validates the first-stage bootloader&#39;s signature (if secure boot is enabled), jumps to it.</li><li><strong>First-stage bootloader (FSBL)</strong> — your code or vendor&#39;s. Sets up RAM, configures the MPU, decides whether to enter recovery mode (button held, magic flag set). Either jumps to the application or to a recovery handler.</li><li><strong>Application</strong> — your firmware. Runs the actual product.</li></ol><p>A typical Linux router boot:</p><ol><li><strong>ROM/CFE/U-Boot SPL</strong> — hardware init, loads U-Boot from flash.</li><li><strong>U-Boot</strong> — finds and validates the kernel, the DTB, and an initramfs; loads them; jumps to the kernel.</li><li><strong>Kernel</strong> — early init, mounts the squashfs root, runs init.</li><li><strong>userspace</strong> — the actual services.</li></ol><p>A typical ESP32 boot:</p><ol><li><strong>ROM</strong> — Espressif&#39;s first-stage. Reads the second-stage bootloader from flash address <code>0x1000</code>.</li><li><strong>Second-stage bootloader</strong> — Espressif-supplied or custom. Reads the partition table, picks an OTA slot, validates the app image, copies it to IRAM/DRAM, jumps.</li><li><strong>Application</strong> — your ESP-IDF firmware.</li></ol><p>Each stage is a separate binary you can analyse separately.</p><h2 id="common-image-header-formats" tabindex="-1">Common image header formats <a class="header-anchor" href="#common-image-header-formats" aria-label="Permalink to &quot;Common image header formats&quot;">​</a></h2><p>The headers you will recognise on sight after a year of embedded work:</p><h3 id="intel-hex-hex" tabindex="-1">Intel HEX (.hex) <a class="header-anchor" href="#intel-hex-hex" aria-label="Permalink to &quot;Intel HEX (.hex)&quot;">​</a></h3><p>ASCII format with embedded address records. Every line:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>:LL AAAA TT DD..DD CC</span></span></code></pre></div><ul><li><code>:</code> — start</li><li><code>LL</code> — byte count</li><li><code>AAAA</code> — 16-bit address</li><li><code>TT</code> — record type (00 data, 01 EOF, 02 extended segment, 04 extended linear)</li><li><code>DD..DD</code> — data</li><li><code>CC</code> — checksum</li></ul><p>Convert with <code>objcopy</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ arm-none-eabi-objcopy -I ihex -O binary firmware.hex firmware.bin</span></span></code></pre></div><p>R2 reads <code>.hex</code> directly with the right architecture flags.</p><h3 id="s-record-motorola-s19-srec-s37" tabindex="-1">S-Record (Motorola .s19/.srec/.s37) <a class="header-anchor" href="#s-record-motorola-s19-srec-s37" aria-label="Permalink to &quot;S-Record (Motorola .s19/.srec/.s37)&quot;">​</a></h3><p>Similar to Intel HEX but Motorola&#39;s encoding. Same convert/load flow.</p><h3 id="uimage-u-boot" tabindex="-1">uImage (U-Boot) <a class="header-anchor" href="#uimage-u-boot" aria-label="Permalink to &quot;uImage (U-Boot)&quot;">​</a></h3><p>64-byte header at offset 0:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>00: 27 05 19 56                   magic IH_MAGIC</span></span>
<span class="line"><span>04: &lt;header CRC32, big-endian&gt;</span></span>
<span class="line"><span>08: &lt;unix timestamp&gt;</span></span>
<span class="line"><span>0C: &lt;data size&gt;</span></span>
<span class="line"><span>10: &lt;load address&gt;</span></span>
<span class="line"><span>14: &lt;entry point&gt;</span></span>
<span class="line"><span>18: &lt;data CRC32&gt;</span></span>
<span class="line"><span>1C: &lt;OS type&gt;     &lt;arch&gt;          &lt;image type&gt;     &lt;compression&gt;</span></span>
<span class="line"><span>20: &#39;image_name........&#39; (32 bytes, null-padded)</span></span>
<span class="line"><span>40: &lt;payload&gt;</span></span></code></pre></div><p>Decode:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ dumpimage -l firmware.uImage</span></span>
<span class="line"><span>Image Name:   Linux-4.14.146</span></span>
<span class="line"><span>Created:      Tue Jan  1 00:00:00 2024</span></span>
<span class="line"><span>Image Type:   MIPS Linux Kernel Image (lzma compressed)</span></span>
<span class="line"><span>Data Size:    1572800 Bytes = 1536.00 KiB = 1.50 MiB</span></span>
<span class="line"><span>Load Address: 80000000</span></span>
<span class="line"><span>Entry Point:  80000400</span></span>
<span class="line"><span></span></span>
<span class="line"><span>$ dumpimage -p 0 -o kernel.lzma firmware.uImage</span></span>
<span class="line"><span>$ unlzma kernel.lzma</span></span>
<span class="line"><span>$ r2 -a mips -b 32 -e cfg.bigendian=true -m 0x80000000 kernel</span></span></code></pre></div><h3 id="fit-flattened-image-tree" tabindex="-1">FIT (Flattened Image Tree) <a class="header-anchor" href="#fit-flattened-image-tree" aria-label="Permalink to &quot;FIT (Flattened Image Tree)&quot;">​</a></h3><p>U-Boot&#39;s modern multi-image format. A DTB-encoded structure that contains kernel, DTB(s), initramfs, and a <code>configurations</code> section that selects which combination to boot. <code>dumpimage -l image.itb</code> lists everything; <code>-p N</code> extracts.</p><h3 id="esp-image-format" tabindex="-1">ESP image format <a class="header-anchor" href="#esp-image-format" aria-label="Permalink to &quot;ESP image format&quot;">​</a></h3><p>Already covered (Chapter 14). Magic byte <code>0xE9</code>, segment headers, each segment with its own load address.</p><h3 id="nxp-freescale-s32k-fcb-headers" tabindex="-1">NXP / Freescale <code>.s32k</code> / <code>.fcb</code> headers <a class="header-anchor" href="#nxp-freescale-s32k-fcb-headers" aria-label="Permalink to &quot;NXP / Freescale \`.s32k\` / \`.fcb\` headers&quot;">​</a></h3><p>Some NXP families have a &quot;Flash Configuration Block&quot; at offset 0x400 containing chip-config bytes, and the user code follows. Skip the header before disassembly.</p><h3 id="atmel-sam-ba-bootloader-interface" tabindex="-1">Atmel SAM-BA bootloader interface <a class="header-anchor" href="#atmel-sam-ba-bootloader-interface" aria-label="Permalink to &quot;Atmel SAM-BA bootloader interface&quot;">​</a></h3><p>SAM-BA expects a specific layout in the first 0x2000 bytes. The application starts at 0x2000 (16 KiB in). Load with <code>-m 0x00002000</code> on a SAM-BA-bootloaded image.</p><h3 id="vendor-specific-realtek-allwinner-rockchip" tabindex="-1">Vendor-specific (Realtek, Allwinner, Rockchip, …) <a class="header-anchor" href="#vendor-specific-realtek-allwinner-rockchip" aria-label="Permalink to &quot;Vendor-specific (Realtek, Allwinner, Rockchip, …)&quot;">​</a></h3><p>Each SoC vendor with a custom bootloader has a custom header. The fastest way to identify one is to ask <code>binwalk</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ binwalk firmware.bin</span></span>
<span class="line"><span>DECIMAL    HEXADECIMAL  DESCRIPTION</span></span>
<span class="line"><span>0          0x0          Realtek firmware header, name: &quot;RTKBoot&quot;</span></span>
<span class="line"><span>40         0x28         ARM executable code (Thumb)</span></span></code></pre></div><p>Strip the header with <code>dd</code> and proceed.</p><h2 id="recognising-compression" tabindex="-1">Recognising compression <a class="header-anchor" href="#recognising-compression" aria-label="Permalink to &quot;Recognising compression&quot;">​</a></h2><p>A heavily compressed payload is statistically &quot;random-looking&quot; — high entropy, no obvious structure. <code>binwalk -E</code> plots entropy across the file:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ binwalk -E firmware.bin</span></span>
<span class="line"><span>0x0      0.000000</span></span>
<span class="line"><span>0x40     0.987         # high entropy from here = compressed</span></span>
<span class="line"><span>0x80     0.989</span></span>
<span class="line"><span>... (high entropy until)</span></span>
<span class="line"><span>0x80000  0.620         # back to normal — uncompressed payload follows</span></span></code></pre></div><p>Or check entropy with a small Python:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;"> sys, math, collections</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;"> open</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">(sys.argv[</span><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">],</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;">&#39;rb&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">).read()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">counts </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;"> collections.Counter(data)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">total </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;"> len</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">(data)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">entropy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">sum</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">((c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">total) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;"> math.log2(c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">total) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;"> c </span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;"> counts.values())</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#6CB6FF;">print</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">f</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;">&quot;entropy: </span><span style="--shiki-light:#005CC5;--shiki-dark:#F47067;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">entropy</span><span style="--shiki-light:#D73A49;--shiki-dark:#F47067;">:.3f</span><span style="--shiki-light:#005CC5;--shiki-dark:#F47067;">}</span><span style="--shiki-light:#032F62;--shiki-dark:#96D0FF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#ADBAC7;">)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#768390;"># 8.0 = max for bytes, 7.5+ = likely compressed</span></span></code></pre></div><p>For a known compression format, <code>binwalk</code> recognises the magic:</p><table tabindex="0"><thead><tr><th>Magic</th><th>Compression</th></tr></thead><tbody><tr><td><code>1F 8B</code></td><td>gzip</td></tr><tr><td><code>5D 00 00</code></td><td>LZMA (older variant)</td></tr><tr><td><code>FD 37 7A 58 5A 00</code></td><td>XZ</td></tr><tr><td><code>28 B5 2F FD</code></td><td>Zstandard</td></tr><tr><td><code>42 5A 68</code></td><td>bzip2</td></tr><tr><td><code>89 4C 5A 4F 00 0D 0A 1A 0A</code></td><td>lzop (LZO container)</td></tr><tr><td><code>04 22 4D 18</code></td><td>LZ4 frame</td></tr><tr><td><code>68 73 71 73</code></td><td>squashfs v4 (modern, little-endian)</td></tr><tr><td><code>73 71 73 68</code></td><td>squashfs ≤3.x (legacy, big-endian)</td></tr><tr><td><code>85 19</code> (LE) / <code>19 85</code> (BE)</td><td>JFFS2 node (<code>JFFS2_MAGIC_BITMASK = 0x1985</code>)</td></tr><tr><td><code>2D 72 6F 6D 31 66 73 2D</code></td><td>romfs (<code>-rom1fs-</code> ASCII)</td></tr></tbody></table><p>YAFFS2 has no fixed file-magic; identify it structurally via <code>binwalk</code>&#39;s YAFFS detector or by trying to mount with <code>unyaffs</code>.</p><p>Decompress with the matching tool and continue.</p><h2 id="the-application-image-inside-the-bootloader" tabindex="-1">The application image inside the bootloader <a class="header-anchor" href="#the-application-image-inside-the-bootloader" aria-label="Permalink to &quot;The application image inside the bootloader&quot;">​</a></h2><p>If the bootloader is a separate binary you have, find where it loads the application from. Patterns:</p><ul><li>A <code>memcpy(dest, src, len)</code> or open-coded equivalent where <code>src</code> is a flash address and <code>dest</code> is RAM, followed by a jump to a RAM address — this is the loader.</li><li>A function-pointer call where the pointer was loaded from a fixed flash address — the bootloader is calling the application&#39;s reset vector.</li><li>A long <code>memcmp</code> against a magic value — signature/integrity check.</li></ul><p>Trace from the call to the application&#39;s entry point. The address the bootloader jumps to is the application&#39;s load address.</p><h2 id="ota-update-mechanisms" tabindex="-1">OTA update mechanisms <a class="header-anchor" href="#ota-update-mechanisms" aria-label="Permalink to &quot;OTA update mechanisms&quot;">​</a></h2><p>OTA (Over-The-Air) updates are where many embedded vulnerabilities live. Common architectures:</p><p><strong>A/B partitions.</strong> Two slots. Bootloader picks the active slot, verifies it, runs it. New firmware writes to the <em>inactive</em> slot and flips a pointer. Safe against power loss.</p><p><strong>Single slot with backup.</strong> Same idea, but the inactive slot is just the previous version, kept for rollback.</p><p><strong>Streamed flash.</strong> New firmware streams in over the network and is written to flash as it arrives. Risky; partial writes can brick.</p><p><strong>Recovery partition.</strong> A small known-good firmware that can be booted to repair the main partition. The main partition&#39;s update is still risky; recovery is a fallback.</p><p>Things to look for when reverse engineering an OTA implementation:</p><p><strong>Signature verification.</strong> Find the signature check. Common patterns:</p><ul><li>Public-key crypto (Ed25519, ECDSA, RSA) verifying a signature blob appended to the image.</li><li>HMAC with a hard-coded key.</li><li>CRC32 (catastrophically weak — anyone can forge a &quot;valid&quot; image).</li><li>No verification at all (more common than you would hope).</li></ul><p>Find the verify function:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; izz ~ -i sign           # strings about signing</span></span>
<span class="line"><span>[0x...]&gt; izz ~ -i hash</span></span>
<span class="line"><span>[0x...]&gt; izz ~ -i verif</span></span></code></pre></div><p>Or look for the public key as a constant in the firmware: a 32-byte Ed25519 public key has no plausible compression and looks random.</p><p><strong>Anti-rollback.</strong> A monotonic version counter that the new image must not decrement. Enforced in eFuse, OTP, or just a flash counter. If absent, an attacker can install an old, vulnerable firmware over a patched one.</p><p><strong>Encryption.</strong> AES-CTR, AES-XTS, or vendor-specific. The decrypt key may be in eFuse (good), in plaintext flash (terrible), or derived from a chip-unique value (mediocre).</p><p><strong>Boot-time integrity.</strong> Some chains check the application image&#39;s integrity at every boot, not just at update time. Find the boot integrity routine; it is typically on the critical path of the bootloader.</p><h2 id="esp32-ota-specifics" tabindex="-1">ESP32 OTA specifics <a class="header-anchor" href="#esp32-ota-specifics" aria-label="Permalink to &quot;ESP32 OTA specifics&quot;">​</a></h2><p>ESP-IDF&#39;s OTA scheme uses two app partitions (<code>ota_0</code>, <code>ota_1</code>) and a separate <code>otadata</code> partition. The bootloader reads <code>otadata</code> to decide which app to run. Each app image carries an SHA-256 hash; the bootloader verifies it before jumping. With secure-boot enabled, the bootloader also verifies an Ed25519 signature.</p><p>Reverse engineer the second-stage bootloader by extracting the <code>bootloader.bin</code> from the flash image. On the classic ESP32, the bootloader lives at flash offset <code>0x1000</code> and is bounded above by the partition table at <code>0x8000</code> (so up to ~28 KiB). On ESP32-S3, C3, C6, and H2 the bootloader sits at flash offset <code>0x0</code> instead. Treat it as a regular ESP image with its own segments (Chapter 14).</p><h2 id="linux-router-ota" tabindex="-1">Linux router OTA <a class="header-anchor" href="#linux-router-ota" aria-label="Permalink to &quot;Linux router OTA&quot;">​</a></h2><p>Most router OTAs are: download a tarball or a custom image, validate checksum, write to MTD, reboot. The validation is often weak. Look at the update CGI handler in the web UI binary, trace what command it executes (<code>mtd write</code>, <code>nandwrite</code>, vendor-specific tool), and check what it actually validates.</p><p>A common pattern: the update file contains a header with vendor ID, model ID, and a CRC. The check is &quot;vendor matches, model matches, CRC valid&quot;. Forging is trivial; bricking via deliberate misuse is trivial; planting persistent backdoors via OTA is one of the most common router compromise paths.</p><h2 id="workflow-when-reverse-engineering-a-bootloader" tabindex="-1">Workflow when reverse engineering a bootloader <a class="header-anchor" href="#workflow-when-reverse-engineering-a-bootloader" aria-label="Permalink to &quot;Workflow when reverse engineering a bootloader&quot;">​</a></h2><ol><li><strong>Identify the format.</strong> binwalk + entropy + visual inspection.</li><li><strong>Extract.</strong> Strip headers, decompress, split into segments.</li><li><strong>Load each stage in r2</strong> with the right base. The bootloader typically runs from ROM or low flash; the application from a later flash region.</li><li><strong>Find the application hand-off.</strong> The instruction that branches from the bootloader to the application gives you both the application&#39;s load address and the function pointer / vector table layout the bootloader expects.</li><li><strong>Find the integrity check.</strong> Verify signature/hash routine.</li><li><strong>Find the update entry point.</strong> Function that receives a new image (over USB, UART, OTA, JTAG mailbox, ...).</li></ol><p>Bootloaders are smaller than applications and have very specific purposes. They are often the most efficient targets for vulnerability research because the attack surface is constrained, the failure mode is severe (full device compromise that survives application updates), and the code is usually written by a small team under time pressure.</p>`,76)])])}const g=a(o,[["render",n]]);export{m as __pageData,g as default};
