import{c as a,Q as s,j as t,m as n}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"Dynamic Analysis: GDB, OpenOCD, J-Link, ESIL","description":"","frontmatter":{},"headers":[],"relativePath":"part4/21-dynamic-analysis.md","filePath":"part4/21-dynamic-analysis.md","lastUpdated":null}'),i={name:"part4/21-dynamic-analysis.md"};function p(o,e,r,l,d,c){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="dynamic-analysis-gdb-openocd-j-link-esil" tabindex="-1">Dynamic Analysis: GDB, OpenOCD, J-Link, ESIL <a class="header-anchor" href="#dynamic-analysis-gdb-openocd-j-link-esil" aria-label="Permalink to &quot;Dynamic Analysis: GDB, OpenOCD, J-Link, ESIL&quot;">​</a></h1><p>Static analysis tells you what the firmware <em>can</em> do. Dynamic analysis tells you what it <em>does</em>. For embedded targets, the dynamic analysis story is built around three pieces:</p><ul><li><strong>Hardware debug</strong> through a probe (J-Link, ST-Link, CMSIS-DAP, OpenOCD-supported probes) that exposes a GDB-remote interface, which r2 attaches to.</li><li><strong>Emulation</strong> in QEMU, Renode, or Wokwi, again exposing GDB-remote.</li><li><strong>ESIL</strong> — radare2&#39;s internal symbolic emulator — when you have no hardware and want to step a function in pure software.</li></ul><p>This chapter covers all three.</p><h2 id="r2-s-debugger-overview" tabindex="-1">R2&#39;s debugger overview <a class="header-anchor" href="#r2-s-debugger-overview" aria-label="Permalink to &quot;R2&#39;s debugger overview&quot;">​</a></h2><p>The <code>d</code> (debug) family:</p><table tabindex="0"><thead><tr><th>Command</th><th>Action</th></tr></thead><tbody><tr><td><code>db</code></td><td>breakpoint commands</td></tr><tr><td><code>dc</code></td><td>continue</td></tr><tr><td><code>ds</code></td><td>step</td></tr><tr><td><code>dso</code></td><td>step over</td></tr><tr><td><code>dsu</code></td><td>step until address/condition</td></tr><tr><td><code>dr</code></td><td>registers</td></tr><tr><td><code>dm</code></td><td>memory map</td></tr><tr><td><code>do</code></td><td>open another process / file for debugging</td></tr><tr><td><code>dp</code></td><td>processes / threads</td></tr><tr><td><code>dt</code></td><td>trace</td></tr><tr><td><code>dx</code></td><td>execute raw bytes</td></tr></tbody></table><p>Visual mode in debug shows registers and stack alongside disassembly. <code>v</code> (panels) lets you arrange them however you like.</p><h2 id="connecting-to-a-gdb-remote" tabindex="-1">Connecting to a GDB remote <a class="header-anchor" href="#connecting-to-a-gdb-remote" aria-label="Permalink to &quot;Connecting to a GDB remote&quot;">​</a></h2><p>OpenOCD, J-Link GDB Server, QEMU&#39;s <code>-s</code> flag, Renode, pyOCD, and probe-rs all expose a GDB-remote stub on a TCP port. R2 connects to any of them with <code>-d gdb://</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -d gdb://localhost:3333 firmware.elf</span></span></code></pre></div><p>Or attach inside r2:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e dbg.backend = gdb</span></span>
<span class="line"><span>[0x...]&gt; e dbg.profile = ...</span></span>
<span class="line"><span>[0x...]&gt; :dc gdb://localhost:3333</span></span></code></pre></div><p>The same command set then works against the remote target. <code>dr</code> shows registers, <code>dm</code> shows memory regions, <code>db</code> sets breakpoints, <code>dc</code> continues, <code>ds</code> steps.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Some GDB stubs do not implement every part of the protocol. If a breakpoint type fails to set, it is usually because the stub does not implement hardware breakpoints (or has none free) or does not support write watchpoints. Check the stub&#39;s documentation.</p></div><h2 id="openocd" tabindex="-1">OpenOCD <a class="header-anchor" href="#openocd" aria-label="Permalink to &quot;OpenOCD&quot;">​</a></h2><p>OpenOCD is the most flexible debug-probe driver. It speaks to JTAG/SWD probes (FT232H, FT2232, J-Link, ST-Link, CMSIS-DAP, Black Magic Probe, ULINK, …) and exposes a GDB stub.</p><p>Typical session:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span># Terminal 1</span></span>
<span class="line"><span>$ openocd -f interface/cmsis-dap.cfg -f target/stm32f4x.cfg</span></span>
<span class="line"><span>... </span></span>
<span class="line"><span>Info : Listening on port 3333 for gdb connections</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Terminal 2</span></span>
<span class="line"><span>$ r2 -d gdb://localhost:3333 firmware.elf</span></span>
<span class="line"><span>[0x...]&gt; :dc</span></span>
<span class="line"><span>... runs ...</span></span>
<span class="line"><span>[Ctrl+C in r2]</span></span>
<span class="line"><span>[0x...]&gt; :dr pc</span></span>
<span class="line"><span>0x080001fa</span></span>
<span class="line"><span>[0x...]&gt; :db 0x080012a0      # set breakpoint</span></span>
<span class="line"><span>[0x...]&gt; :dc                 # continue, hits breakpoint</span></span></code></pre></div><p>For chips OpenOCD does not support out of the box, you can write a config file that combines a generic interface with a custom target description. The OpenOCD source tree under <code>src/target/</code> has hundreds of examples.</p><h2 id="j-link-gdb-server" tabindex="-1">J-Link GDB Server <a class="header-anchor" href="#j-link-gdb-server" aria-label="Permalink to &quot;J-Link GDB Server&quot;">​</a></h2><p>SEGGER&#39;s J-Link probes come with <code>JLinkGDBServer</code> — a vendor GDB stub that supports more chips than OpenOCD and is rock-solid for ARM. Same usage pattern:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ JLinkGDBServerCL -if SWD -device STM32F407ZG -port 3333</span></span>
<span class="line"><span>$ r2 -d gdb://localhost:3333 firmware.elf</span></span></code></pre></div><p>The SEGGER stub supports unlimited software breakpoints in flash via its own RAM-based monitor — useful when the chip has only 4–8 hardware breakpoints and you want to set 30.</p><h2 id="qemu" tabindex="-1">QEMU <a class="header-anchor" href="#qemu" aria-label="Permalink to &quot;QEMU&quot;">​</a></h2><p>QEMU emulates many embedded targets: ARM (system), AArch64, MIPS, RISC-V, Xtensa, x86, PowerPC. Run a kernel:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ qemu-system-arm -M virt -nographic -kernel zImage -dtb board.dtb \\</span></span>
<span class="line"><span>    -append &#39;console=ttyAMA0&#39; -gdb tcp::1234 -S</span></span></code></pre></div><p><code>-S</code> halts the CPU at start; <code>-gdb tcp::1234</code> exposes a GDB stub. Connect:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -d gdb://localhost:1234 vmlinux</span></span>
<span class="line"><span>[0x...]&gt; :dc</span></span></code></pre></div><p>For bare-metal ARM (Cortex-M):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ qemu-system-arm -M netduinoplus2 -kernel firmware.elf -gdb tcp::1234 -S</span></span></code></pre></div><p>QEMU&#39;s Cortex-M support is not perfect — some peripherals are modelled, others are not. For peripherals that are not modelled, reads return zero and writes are dropped. The disassembly executes, but behaviour diverges from real hardware. This is fine for following control flow; it is wrong for testing peripheral interactions.</p><h2 id="renode" tabindex="-1">Renode <a class="header-anchor" href="#renode" aria-label="Permalink to &quot;Renode&quot;">​</a></h2><p>Renode is a more sophisticated embedded-systems emulator from Antmicro. It models RTOS interactions, multiple cores, interconnects, and a wider range of peripherals. Setup is more involved than QEMU but the fidelity is higher. Same GDB-remote story once running.</p><h2 id="wokwi" tabindex="-1">Wokwi <a class="header-anchor" href="#wokwi" aria-label="Permalink to &quot;Wokwi&quot;">​</a></h2><p>Wokwi runs ESP32, Arduino, Raspberry Pi Pico, and STM32 firmware in the browser, with virtual sensors and displays. It exposes GDB-remote via a CLI tool. For ESP32 reverse engineering without hardware, it is surprisingly capable.</p><h2 id="breakpoints-watchpoints-stepping" tabindex="-1">Breakpoints, watchpoints, stepping <a class="header-anchor" href="#breakpoints-watchpoints-stepping" aria-label="Permalink to &quot;Breakpoints, watchpoints, stepping&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; :db 0x08001234         # software breakpoint</span></span>
<span class="line"><span>[0x...]&gt; :dbh 0x08001234        # hardware breakpoint (limited supply)</span></span>
<span class="line"><span>[0x...]&gt; :dbw 0x20000040 4 rw   # watchpoint on 4 bytes, read+write</span></span>
<span class="line"><span>[0x...]&gt; :db                    # list</span></span>
<span class="line"><span>[0x...]&gt; :db- 0x08001234        # delete one</span></span>
<span class="line"><span>[0x...]&gt; :db-*                  # delete all</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[0x...]&gt; :ds                    # step one instruction</span></span>
<span class="line"><span>[0x...]&gt; :dso                   # step over</span></span>
<span class="line"><span>[0x...]&gt; :dsu sym.HAL_GPIO_Init # step until</span></span>
<span class="line"><span>[0x...]&gt; :dc                    # continue</span></span></code></pre></div><p>Watchpoints are the most powerful tool for understanding peripheral interaction. Set a watchpoint on the data register of a UART, run, and every time the firmware writes a byte to the UART, the debugger breaks with the call stack showing exactly where the byte came from.</p><h2 id="inspecting-state" tabindex="-1">Inspecting state <a class="header-anchor" href="#inspecting-state" aria-label="Permalink to &quot;Inspecting state&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; :dr            # all registers</span></span>
<span class="line"><span>[0x...]&gt; :dr r0         # one register</span></span>
<span class="line"><span>[0x...]&gt; :dr=           # registers and their values aligned</span></span>
<span class="line"><span>[0x...]&gt; :dr r0=0x100   # set register</span></span>
<span class="line"><span>[0x...]&gt; :pxw 32 @ sp   # 32 bytes of words at the stack pointer</span></span>
<span class="line"><span>[0x...]&gt; :pxw 32 @ 0x20000000  # SRAM</span></span></code></pre></div><p>Register-relative seeking works the same way as before:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; :pdf @ pc</span></span>
<span class="line"><span>[0x...]&gt; :pxw 16 @ r0</span></span></code></pre></div><h2 id="tracing" tabindex="-1">Tracing <a class="header-anchor" href="#tracing" aria-label="Permalink to &quot;Tracing&quot;">​</a></h2><p><code>dt</code> traces execution. Useful when you want to record what happens between two breakpoints rather than step interactively:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; :dts+ on             # start tracing</span></span>
<span class="line"><span>[0x...]&gt; :dc                  # run</span></span>
<span class="line"><span>[0x...]&gt; :dts+ off            # stop</span></span>
<span class="line"><span>[0x...]&gt; :dts                 # list traced instructions</span></span></code></pre></div><p>Each traced address gets a flag in the <code>traces</code> flag space, so you can later iterate over them with <code>@@= trace.*</code>.</p><h2 id="esil-emulation-without-hardware" tabindex="-1">ESIL: emulation without hardware <a class="header-anchor" href="#esil-emulation-without-hardware" aria-label="Permalink to &quot;ESIL: emulation without hardware&quot;">​</a></h2><p>ESIL (Evaluable Strings Intermediate Language) is r2&#39;s tiny stack- based language for representing every instruction. The ESIL VM executes ESIL strings; r2 emulates the whole instruction set by translating instructions to ESIL on demand.</p><p>Why use ESIL:</p><ul><li>You have no hardware (the chip is unobtainable, expensive, or destroyed).</li><li>You want to recover constant arguments to a function from a caller that computes them.</li><li>You want to follow control flow through a deobfuscation routine without ever running the binary.</li><li>You want to fuzz a single function in isolation.</li></ul><p>Setup:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aei                 # init ESIL VM</span></span>
<span class="line"><span>[0x...]&gt; aeim                # init memory (creates a stack at SP)</span></span>
<span class="line"><span>[0x...]&gt; aeip                # set ESIL pc to current seek</span></span>
<span class="line"><span>[0x...]&gt; aer                 # ESIL register state</span></span></code></pre></div><p>Step:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aes                 # one ESIL step</span></span>
<span class="line"><span>[0x...]&gt; aeso                # step over (skips into calls)</span></span>
<span class="line"><span>[0x...]&gt; aesu sym.target     # step until address</span></span>
<span class="line"><span>[0x...]&gt; aess N              # N steps</span></span></code></pre></div><p>Inspect:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aer r0              # value of r0 in ESIL VM</span></span>
<span class="line"><span>[0x...]&gt; aer                 # all</span></span>
<span class="line"><span>[0x...]&gt; aex \`aer pc\`        # disassembly of current ESIL pc</span></span></code></pre></div><p>Set state:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aer r0=0x12345678   # set</span></span>
<span class="line"><span>[0x...]&gt; wx 11 22 33 44 @ 0x20000000   # write memory</span></span></code></pre></div><p>Analyse functions with ESIL:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aae                 # analyse all by ESIL emulation</span></span>
<span class="line"><span>[0x...]&gt; aef                 # ESIL-emulate the current function</span></span></code></pre></div><p><code>aae</code> is part of <code>aaaa</code>. It improves call-graph and xref recovery in binaries with computed jumps and indirect calls — ESIL can sometimes resolve a <code>bx r0</code> if it can determine the value of r0 statically.</p><h2 id="a-worked-example-recovering-an-indirect-call-target" tabindex="-1">A worked example: recovering an indirect call target <a class="header-anchor" href="#a-worked-example-recovering-an-indirect-call-target" aria-label="Permalink to &quot;A worked example: recovering an indirect call target&quot;">​</a></h2><p>Suppose you see:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>0x08001234  ldr r3, [pc, #0x10]   ; load function ptr table base</span></span>
<span class="line"><span>0x08001236  ldrb r0, [...]        ; load index</span></span>
<span class="line"><span>0x08001238  ldr r3, [r3, r0, lsl 2]</span></span>
<span class="line"><span>0x0800123a  blx r3                ; what does this call?</span></span></code></pre></div><p>Static analysis can&#39;t resolve the target (the index varies). With ESIL:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aei</span></span>
<span class="line"><span>[0x...]&gt; aeim</span></span>
<span class="line"><span>[0x...]&gt; aeip</span></span>
<span class="line"><span>[0x...]&gt; aer r0=0           # try index 0</span></span>
<span class="line"><span>[0x...]&gt; aesu 0x0800123a    # step until the blx</span></span>
<span class="line"><span>[0x...]&gt; aer r3             # the resolved function pointer</span></span>
<span class="line"><span>0x080020a0</span></span>
<span class="line"><span>[0x...]&gt; ax 0x0800123a 0x080020a0    # add manual xref</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[0x...]&gt; aer r0=1           # try index 1</span></span>
<span class="line"><span>[0x...]&gt; aeip</span></span>
<span class="line"><span>[0x...]&gt; aesu 0x0800123a</span></span>
<span class="line"><span>[0x...]&gt; aer r3</span></span>
<span class="line"><span>0x080020c4</span></span>
<span class="line"><span>[0x...]&gt; ax 0x0800123a 0x080020c4</span></span></code></pre></div><p>A short Python r2pipe loop (Chapter 25) iterates through every plausible index and adds xrefs for each. The decompiler then sees all the call destinations.</p><h2 id="memory-and-peripherals-during-emulation" tabindex="-1">Memory and peripherals during emulation <a class="header-anchor" href="#memory-and-peripherals-during-emulation" aria-label="Permalink to &quot;Memory and peripherals during emulation&quot;">​</a></h2><p>ESIL&#39;s default memory is sparse — reads from unmapped addresses return zero, writes are buffered. For peripherals, this is wrong: real hardware has side effects. You can:</p><ul><li>Map a region and pre-fill it: <code>wx ff... @ 0x40021000</code>.</li><li>Mock peripheral behaviour with ESIL hooks (<code>aeh</code> family). For example, every read from <code>0x40004404</code> returns 0x80 (UART TX ready):</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; aeh r 0x40004404 0x80</span></span></code></pre></div><ul><li>For complex peripherals, scripting with r2pipe is more practical.</li></ul><h2 id="limitations-of-esil" tabindex="-1">Limitations of ESIL <a class="header-anchor" href="#limitations-of-esil" aria-label="Permalink to &quot;Limitations of ESIL&quot;">​</a></h2><p>ESIL covers the common subset of every supported architecture. It misses:</p><ul><li>Floating-point (limited support; many FP instructions are not modelled).</li><li>Vector/SIMD instructions (mostly absent).</li><li>System-level instructions (MSR, CSR writes, cache operations).</li><li>Some architecture-specific oddities (Thumb-2 IT blocks behave correctly only with recent r2; Xtensa windowed register slides are partially modelled).</li></ul><p>For floating-point or vector-heavy code, ESIL is not the right tool. Use Unicorn (Chapter 26) or hardware emulation.</p><h2 id="putting-it-together" tabindex="-1">Putting it together <a class="header-anchor" href="#putting-it-together" aria-label="Permalink to &quot;Putting it together&quot;">​</a></h2><p>A common workflow:</p><ol><li>Statically reverse engineer the firmware to the point where you understand the major functions and want to verify a hypothesis.</li><li>Set up hardware debug via OpenOCD / J-Link, or QEMU/Renode if no hardware available.</li><li>Set breakpoints at the functions you want to observe.</li><li>Run, hit breakpoints, inspect state, step.</li><li>For functions where stepping is too slow or hardware is unavailable, fall back to ESIL.</li></ol><p>Dynamic analysis closes the loop on static analysis. The combination — &quot;I think this function does X, let me set a breakpoint and confirm&quot; — is much faster than either alone.</p>`,81)])])}const g=a(i,[["render",p]]);export{u as __pageData,g as default};
