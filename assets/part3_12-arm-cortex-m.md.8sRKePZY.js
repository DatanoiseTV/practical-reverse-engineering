import{c as a,Q as s,j as t,m as n}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"ARM Cortex-M (STM32, nRF, SAMD)","description":"","frontmatter":{},"headers":[],"relativePath":"part3/12-arm-cortex-m.md","filePath":"part3/12-arm-cortex-m.md","lastUpdated":null}'),o={name:"part3/12-arm-cortex-m.md"};function i(r,e,c,l,p,d){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="arm-cortex-m-stm32-nrf-samd" tabindex="-1">ARM Cortex-M (STM32, nRF, SAMD) <a class="header-anchor" href="#arm-cortex-m-stm32-nrf-samd" aria-label="Permalink to &quot;ARM Cortex-M (STM32, nRF, SAMD)&quot;">​</a></h1><p>Cortex-M is the most common 32-bit microcontroller architecture in the world, and reverse engineering a Cortex-M firmware image is the most common starting point for an embedded reverse engineer. This chapter covers the architectural details you need to know, the loading recipe, the analysis tweaks, the family-specific gotchas (STM32 vs nRF52 vs SAMD), and a worked example.</p><h2 id="architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#architectural-overview" aria-label="Permalink to &quot;Architectural overview&quot;">​</a></h2><p>Cortex-M is an ARMv6-M (M0/M0+/M1), ARMv7-M (M3) / ARMv7E-M (M4/M7), or ARMv8-M (M23/M33) / ARMv8.1-M (M55) profile. Key facts for reverse engineering:</p><ul><li><strong>Thumb-only.</strong> Cortex-M cores execute Thumb-2 (M3/M4/M7) or a Thumb subset (M0/M0+). They do not execute classic ARM instructions. The low bit of every code address is <code>1</code> (Thumb interworking convention).</li><li><strong>Memory map is fixed.</strong> The architecture defines what address ranges hold what: <ul><li><code>0x00000000</code> — code (alias for the boot region, often flash)</li><li><code>0x08000000</code> — flash (STM32 convention; some vendors differ)</li><li><code>0x20000000</code> — SRAM</li><li><code>0x40000000</code> — peripherals</li><li><code>0xE0000000</code>–<code>0xE00FFFFF</code> — Private Peripheral Bus (PPB). The System Control Space (SCS — NVIC, SysTick, DWT, SCB) is a sub-range at <code>0xE000E000</code>–<code>0xE000EFFF</code>.</li></ul></li><li><strong>Vector table.</strong> First 16 words of code are: initial SP, reset vector, NMI, HardFault, then four ARMv7-M-only fault vectors (MemManage, BusFault, UsageFault, plus SecureFault on v8-M), four reserved slots, SVC, DebugMon, one reserved slot, PendSV, SysTick. External interrupt handlers start at word 16. The number of external interrupts is family-dependent (about 80 on a typical STM32F4). On ARMv6-M (M0/M0+) the MemManage/BusFault/UsageFault slots are reserved — only HardFault exists for all faults.</li><li><strong>AAPCS calling convention.</strong> r0–r3 for arguments, return in r0, r4–r11 callee-saved, lr is the return address. This is the same as Linux ARM, just on smaller cores.</li><li><strong>Bit-banding (M3/M4 only).</strong> Address ranges <code>0x22000000–0x23FFFFFF</code> and <code>0x42000000–0x43FFFFFF</code> alias bits in SRAM and peripheral space. A 32-bit access to <code>0x22000000 + (off*32 + bit*4)</code> reads or writes bit <code>bit</code> of byte <code>off</code> in SRAM. This is rare but confusing when you see it.</li><li><strong>No MMU.</strong> What you see in disassembly is what executes. Addresses are physical.</li></ul><h2 id="loading-recipe" tabindex="-1">Loading recipe <a class="header-anchor" href="#loading-recipe" aria-label="Permalink to &quot;Loading recipe&quot;">​</a></h2><p>For a raw firmware blob (no ELF), the canonical command:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a arm -b 16 -c cortex -m 0x08000000 firmware.bin</span></span></code></pre></div><ul><li><code>-a arm</code> — the architecture</li><li><code>-b 16</code> — Thumb is 16-bit-aligned</li><li><code>-c cortex</code> — the CPU profile (enables M-profile system register decoding such as MSR/MRS to PRIMASK, FAULTMASK, BASEPRI, CONTROL)</li><li><code>-m 0x08000000</code> — STM32 flash base. For nRF52 application image use the address just past the SoftDevice — <code>0x00026000</code> for S132 v7.x (152 KiB) or <code>0x00027000</code> for S140 v7.x (156 KiB); older SoftDevice versions end at lower addresses, so check the exact SoftDevice version in flash. For SAMD21 use <code>0x00002000</code> after the UF2 bootloader; bare-flash images map at <code>0x00000000</code>.</li></ul><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Some Cortex-M binaries you find in the wild are extracted from a larger flash dump and have lost their leading-zero region. The vector table may not be at offset 0 of the file. Look at the first 32 bytes in <code>xxd</code>: if they look like RAM-pointer + flash-pointer + flash-pointer, you are at the start; if they look like instructions, you are not.</p></div><h2 id="the-vector-table-is-your-map" tabindex="-1">The vector table is your map <a class="header-anchor" href="#the-vector-table-is-your-map" aria-label="Permalink to &quot;The vector table is your map&quot;">​</a></h2><p>After loading, dump the first 256 bytes:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; pxw 256 @ 0x08000000</span></span>
<span class="line"><span>0x08000000  0x20020000 0x080001cd 0x08000231 0x08000235  ... ...</span></span>
<span class="line"><span>0x08000010  0x08000239 0x0800023d 0x08000241 0x00000000  ... ...</span></span>
<span class="line"><span>0x08000020  0x00000000 0x00000000 0x00000000 0x08000245  ... ...</span></span>
<span class="line"><span>0x08000030  0x08000249 0x00000000 0x0800024d 0x08000251  ... ...</span></span>
<span class="line"><span>...</span></span></code></pre></div><p>Reading this:</p><ul><li>Word 0: <code>0x20020000</code> — initial SP. <code>0x20000000 + 0x20000</code> means 128 KiB of SRAM ends at this address; SP grows down from here.</li><li>Word 1: <code>0x080001cd</code> — reset vector (<code>0x080001cc</code> with the Thumb bit set). Define a function there and rename it:</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; af @ 0x080001cc</span></span>
<span class="line"><span>[0x08000000]&gt; afn reset_handler 0x080001cc</span></span></code></pre></div><ul><li>Words 2–15: standard exceptions. Names from the Cortex-M architecture:</li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span># (handler addresses shown below are the resolved targets from the</span></span>
<span class="line"><span># example vector dump above, not the vector slot offsets — slots 7..10</span></span>
<span class="line"><span># and slot 13 are reserved on v7-M, with zero entries you should skip.)</span></span>
<span class="line"><span>[0x...]&gt; f sym.NMI_Handler         = 0x08000231   # vector slot 2 (offset 0x08)</span></span>
<span class="line"><span>[0x...]&gt; f sym.HardFault_Handler   = 0x08000235   # slot 3 (0x0C)</span></span>
<span class="line"><span>[0x...]&gt; f sym.MemManage_Handler   = 0x08000239   # slot 4 (0x10)</span></span>
<span class="line"><span>[0x...]&gt; f sym.BusFault_Handler    = 0x0800023d   # slot 5 (0x14)</span></span>
<span class="line"><span>[0x...]&gt; f sym.UsageFault_Handler  = 0x08000241   # slot 6 (0x18)</span></span>
<span class="line"><span># slots 7..10 (0x1C..0x28) reserved on v7-M (SecureFault occupies slot 7 on v8-M)</span></span>
<span class="line"><span>[0x...]&gt; f sym.SVC_Handler         = 0x08000245   # slot 11 (0x2C)</span></span>
<span class="line"><span>[0x...]&gt; f sym.DebugMon_Handler    = 0x08000249   # slot 12 (0x30)</span></span>
<span class="line"><span># slot 13 (0x34) reserved</span></span>
<span class="line"><span>[0x...]&gt; f sym.PendSV_Handler      = 0x0800024d   # slot 14 (0x38)</span></span>
<span class="line"><span>[0x...]&gt; f sym.SysTick_Handler     = 0x08000251   # slot 15 (0x3C)</span></span></code></pre></div><ul><li>Words 16+: external interrupts. The mapping is family-specific. For STM32F407, IRQ 0 is <code>WWDG_IRQHandler</code>, IRQ 1 is <code>PVD_IRQHandler</code>, IRQ 6 is <code>EXTI0_IRQHandler</code>, IRQ 37 is <code>USART1_IRQHandler</code>. Use the vendor&#39;s <code>startup_stm32f407xx.s</code> (in any STM32CubeF4 install) as your ground truth.</li></ul><p>A useful trick: vector handlers that are unused typically point to a <code>Default_Handler</code> that loops forever. Functions that are <em>not</em><code>Default_Handler</code> are the IRQs the firmware actually services. Find them with:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; pxw 4*N @ 0x08000040     # the external IRQ vector array</span></span>
<span class="line"><span>[0x...]&gt; # any value other than the default handler is interesting</span></span></code></pre></div><h2 id="peripheral-identification" tabindex="-1">Peripheral identification <a class="header-anchor" href="#peripheral-identification" aria-label="Permalink to &quot;Peripheral identification&quot;">​</a></h2><p>The next biggest payoff is naming the peripherals. STM32, nRF, and SAMD all have well-defined memory maps published in the reference manuals.</p><p>For STM32F4, the relevant bases are:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; f peri.RCC      = 0x40023800   # clock control</span></span>
<span class="line"><span>[0x...]&gt; f peri.GPIOA    = 0x40020000</span></span>
<span class="line"><span>[0x...]&gt; f peri.GPIOB    = 0x40020400</span></span>
<span class="line"><span>[0x...]&gt; f peri.GPIOC    = 0x40020800</span></span>
<span class="line"><span>[0x...]&gt; f peri.USART1   = 0x40011000</span></span>
<span class="line"><span>[0x...]&gt; f peri.USART2   = 0x40004400</span></span>
<span class="line"><span>[0x...]&gt; f peri.SPI1     = 0x40013000</span></span>
<span class="line"><span>[0x...]&gt; f peri.I2C1     = 0x40005400</span></span>
<span class="line"><span>[0x...]&gt; f peri.TIM1     = 0x40010000</span></span>
<span class="line"><span>[0x...]&gt; f peri.TIM2     = 0x40000000</span></span>
<span class="line"><span>[0x...]&gt; f peri.NVIC     = 0xE000E100</span></span>
<span class="line"><span>[0x...]&gt; f peri.SCB      = 0xE000ED00</span></span>
<span class="line"><span>[0x...]&gt; f peri.SysTick  = 0xE000E010</span></span></code></pre></div><p>After flagging, every reference like <code>LDR Rn, [Rm]</code> against an address that resolves to a flag shows the flag name as a comment.</p><p>Better still, load the CMSIS header so structs are typed (Chapter 8):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; to /opt/STM32CubeF4/Drivers/CMSIS/Device/ST/STM32F4xx/Include/stm32f407xx.h</span></span>
<span class="line"><span>[0x...]&gt; tl GPIO_TypeDef = 0x40020000</span></span>
<span class="line"><span>[0x...]&gt; tl GPIO_TypeDef = 0x40020400</span></span>
<span class="line"><span>[0x...]&gt; tl USART_TypeDef = 0x40011000</span></span>
<span class="line"><span>... etc.</span></span></code></pre></div><p>Now <code>STR R1, [R3, #0x14]</code> at a USART base shows <code>USART_TypeDef.CR1</code>.</p><div class="tip custom-block"><p class="custom-block-title">Tip</p><p>The <code>tl</code> linking is per-address. For a family with eight GPIO ports all using the same struct, write a small loop:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; .(tl_gpio)          # macro, defined like this:</span></span>
<span class="line"><span>[0x...]&gt; &quot;(tl_gpio</span></span>
<span class="line"><span>  tl GPIO_TypeDef = 0x40020000</span></span>
<span class="line"><span>  tl GPIO_TypeDef = 0x40020400</span></span>
<span class="line"><span>  tl GPIO_TypeDef = 0x40020800</span></span>
<span class="line"><span>  tl GPIO_TypeDef = 0x40020c00</span></span>
<span class="line"><span>)&quot;</span></span></code></pre></div><p>Save the macro in your <code>~/.radare2rc</code> or per-project rc file.</p></div><h2 id="stm32-specific-notes" tabindex="-1">STM32-specific notes <a class="header-anchor" href="#stm32-specific-notes" aria-label="Permalink to &quot;STM32-specific notes&quot;">​</a></h2><p><strong>Cube HAL.</strong> STM32CubeMX generates code that uses the HAL. Functions named <code>HAL_*</code> are recoverable with zignatures (Chapter 10). Build a signature database from the vendor&#39;s HAL static library — every HAL function has a stable signature across patch versions.</p><p><strong>LL drivers.</strong> The &quot;low level&quot; drivers in CubeF4+ (<code>LL_GPIO_*</code>, <code>LL_USART_*</code>) are inlined heavily. They mostly disappear into the calling function rather than appearing as separate symbols. Pattern- match by recognising the inline expansion (a sequence of LDR/STR to peripheral registers).</p><p><strong>FreeRTOS port.</strong> Most STM32 firmware uses the official ARM Cortex-M4F port of FreeRTOS. The PendSV handler is the context switcher; SVC is the syscall entry. Both have stereotyped sequences you learn to spot. Build a FreeRTOS signature DB and match it; it covers <code>xTaskCreate</code>, <code>vTaskDelay</code>, <code>xQueueSend</code>, and friends.</p><p><strong>Bootloader region.</strong> ST&#39;s reference designs put the system bootloader in ROM at <code>0x1FFF0000</code>. If you see references to that address from your firmware, they are calls into the ST bootloader. ST publishes the bootloader&#39;s API in AN2606.</p><h2 id="nrf52-specific-notes" tabindex="-1">nRF52-specific notes <a class="header-anchor" href="#nrf52-specific-notes" aria-label="Permalink to &quot;nRF52-specific notes&quot;">​</a></h2><p><strong>SoftDevice.</strong> The Bluetooth stack is a separately-flashed binary that lives from <code>0x00000000</code> to the SoftDevice end (e.g., <code>0x00026000</code> for S132 v7.x; the size varies by version). The application starts above it. SoftDevice calls happen via SVC: a software interrupt with a service number. The BLE SVC range starts around <code>0x60</code> (<code>BLE_SVC_BASE</code>), so you will see calls in roughly that range and onwards (<code>sd_ble_*</code> are in the <code>0x60</code>–<code>0x9F</code> block, <code>sd_ble_gap_*</code> higher within it). The exact mapping is in the <code>s132_nrf52_*_API/include/*.h</code> headers.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>When loading an nRF52 dump that includes the SoftDevice, load both: the SoftDevice at <code>0x00000000</code> and the application at the SoftDevice&#39;s end. Otherwise the SVC handlers point into nothing and analysis is incomplete.</p></div><p><strong>SDK structure.</strong> The Nordic SDK uses a distinct naming convention (<code>nrf_drv_*</code>, <code>nrf_*</code>, <code>app_*</code>). Build a signature DB from the SDK once and reuse across projects.</p><h2 id="samd-specific-notes" tabindex="-1">SAMD-specific notes <a class="header-anchor" href="#samd-specific-notes" aria-label="Permalink to &quot;SAMD-specific notes&quot;">​</a></h2><p><strong>ASF / Atmel Start.</strong> The Atmel Software Framework has its own naming style (<code>gpio_set_pin_function</code>, <code>usart_*</code>). Less ubiquitous than ST&#39;s HAL but still common; signature-DB the same way.</p><p><strong>UF2 bootloader.</strong> Many SAMD boards (Adafruit, MakeCode) ship with a UF2 bootloader at <code>0x00000000</code>. The application starts at <code>0x00002000</code>. UF2 is a USB-mountable file format — <code>firmware.uf2</code> on the disk maps to flash with embedded address records. Convert with <code>uf2conv</code> (Chapter 4).</p><p><strong>Debug halt vs reset boot.</strong> SAMD&#39;s reset behaviour depends on the GCLK/PM configuration the bootloader leaves; if your binary hangs in analysis with a strange initial PC, double-check that the entry point you defined matches what the chip&#39;s internal boot logic actually jumps to.</p><h2 id="a-worked-example-identifying-the-uart-setup-function" tabindex="-1">A worked example: identifying the UART setup function <a class="header-anchor" href="#a-worked-example-identifying-the-uart-setup-function" aria-label="Permalink to &quot;A worked example: identifying the UART setup function&quot;">​</a></h2><p>Suppose you have an STM32F407 firmware and you want to find where USART1 is initialised. Workflow:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; f peri.USART1 = 0x40011000</span></span>
<span class="line"><span>[0x08000000]&gt; axt @ peri.USART1</span></span>
<span class="line"><span>0x08001234  -&gt; peri.USART1 (DATA in fcn.0800122c)</span></span>
<span class="line"><span>0x08001244  -&gt; peri.USART1 (DATA in fcn.0800122c)</span></span>
<span class="line"><span>0x08001260  -&gt; peri.USART1 (DATA in fcn.0800122c)</span></span>
<span class="line"><span>... etc.</span></span></code></pre></div><p>All references to USART1 are in one function. Open it:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; pdf @ fcn.0800122c</span></span></code></pre></div><p>You see writes to <code>+0x0c</code> (CR1), <code>+0x10</code> (CR2), <code>+0x14</code> (CR3), <code>+0x08</code> (BRR). That sequence is the signature of HAL_UART_Init or LL_USART_Init. Confirm by linking the type:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; to stm32f407xx.h</span></span>
<span class="line"><span>[0x08000000]&gt; tl USART_TypeDef = 0x40011000</span></span>
<span class="line"><span>[0x08000000]&gt; pdf @ fcn.0800122c</span></span></code></pre></div><p>Now the disassembly reads <code>STR R1, [R3, USART_TypeDef.CR1]</code>. Decompile:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; pdg @ fcn.0800122c</span></span></code></pre></div><p>The output is recognisably a UART init: BRR computed from a fractional-clock value, CR1 set with TE/RE/UE bits, CR2 and CR3 cleared. Rename:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x08000000]&gt; afn HAL_UART_Init 0x0800122c</span></span></code></pre></div><p>…and proceed up the call graph. Five minutes in, you have a usable map of the firmware&#39;s UART initialisation path.</p><h2 id="cortex-m-specific-gotchas" tabindex="-1">Cortex-M-specific gotchas <a class="header-anchor" href="#cortex-m-specific-gotchas" aria-label="Permalink to &quot;Cortex-M-specific gotchas&quot;">​</a></h2><p><strong>Vector relocation.</strong> The SCB-&gt;VTOR register can move the vector table from <code>0x08000000</code> to anywhere. Bootloaders do this when they hand off to the application: they set VTOR to the application&#39;s vector table base. If you analyse a bootloader and the disassembly references vector addresses that look wrong, look for a write to <code>0xE000ED08</code>.</p><p><strong>TrustZone-M (Cortex-M33/M55).</strong> Code lives in two security states with separately-banked stack pointers. A v8-M dump may have a Secure and Non-Secure partition with different vector tables. Check <code>SAU/IDAU</code> configuration in the binary; load the two halves separately.</p><p><strong>Compact branch encoding.</strong> Thumb&#39;s <code>B</code>, <code>BL</code>, <code>BLX</code> instructions use PC-relative offsets that the disassembler must compute. If your load address is wrong by even 4 bytes, every branch target is plausible-looking but wrong. The vector-table sanity check above is the fastest way to catch this.</p><p><strong>Hard-float ABI.</strong> Cortex-M4F and M7 with FPU often use the hard-float ABI: float arguments in s0–s15. If your function signatures describe floats as integer args, the decompiler shows nonsense. Set the calling convention to <code>arm32_hardfloat</code> if available, or annotate the signature explicitly (Chapter 8).</p><p>The remaining architecture chapters follow the same shape: how the CPU works, how to load it, how to identify the vendor&#39;s framework, what bites you. Read in order if you are new to embedded; jump straight to your target if you already know what you are doing.</p>`,61)])])}const g=a(o,[["render",i]]);export{u as __pageData,g as default};
