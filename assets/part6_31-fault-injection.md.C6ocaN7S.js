import{c as t,Q as a,j as o,m as i}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"Fault Injection and Glitching","description":"","frontmatter":{},"headers":[],"relativePath":"part6/31-fault-injection.md","filePath":"part6/31-fault-injection.md","lastUpdated":null}'),r={name:"part6/31-fault-injection.md"};function n(s,e,l,c,h,d){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="fault-injection-and-glitching" tabindex="-1">Fault Injection and Glitching <a class="header-anchor" href="#fault-injection-and-glitching" aria-label="Permalink to &quot;Fault Injection and Glitching&quot;">​</a></h1><p>The chapters so far worked with whatever the chip willingly tells you: code in flash, output on a UART, registers visible through SWD. When the chip refuses — readout protection enabled, secure boot enforced, debug locked — there is a second class of techniques that does not ask for permission. <strong>Fault injection</strong> (FI) is deliberately knocking a chip off-rails for a few nanoseconds so it mis-executes a single instruction or mis-reads a single byte. Done in the right place, that single fault skips a security check, returns a wrong value from a fuse read, or breaks a conditional branch so secret-handling code runs in a non-secret context.</p><p>This chapter is an honest, citation-grounded overview of the field. It does not contain step-by-step recipes for any specific commercial device. It does explain enough that, combined with the references at the end, you can decide whether FI is the right tool for your problem and what hardware to buy if it is.</p><h2 id="what-fault-injection-is-mechanically" tabindex="-1">What fault injection is, mechanically <a class="header-anchor" href="#what-fault-injection-is-mechanically" aria-label="Permalink to &quot;What fault injection is, mechanically&quot;">​</a></h2><p>A microcontroller is a synchronous digital circuit. Every flip-flop samples its input on a clock edge; every gate&#39;s output is valid only after a setup time on its inputs. The chip works because every signal has settled to a clean <code>0</code> or <code>1</code> before the next clock edge captures it.</p><p>A glitch violates that contract on purpose:</p><ul><li><strong>Voltage glitching</strong> — briefly drop the core supply rail (<code>Vcore</code>) below its specified minimum. Some flops fail to retain their value; some combinational paths fail to evaluate in time. The chip&#39;s instruction stream continues but with the wrong bits somewhere.</li><li><strong>Clock glitching</strong> — feed in a much shorter clock cycle than the chip expects, or an extra edge between normal edges. Logic that takes a full cycle to evaluate gets sampled early, producing garbage.</li><li><strong>Electromagnetic (EM) fault injection</strong> — fire a brief pulse from a small coil held against the package. Eddy currents in the die&#39;s metal layers flip the state of nearby flops.</li><li><strong>Laser fault injection</strong> — focus a short laser pulse on a thinned-down silicon die. The photons generate carriers in the doped regions and flip individual gates.</li></ul><p>The first two are accessible to anyone with a few hundred dollars of equipment. EM is harder (the coil and pulse generator are non-trivial). Laser is laboratory equipment. Most published attacks against commercial microcontrollers use voltage or EM.</p><h2 id="what-you-can-hope-to-achieve" tabindex="-1">What you can hope to achieve <a class="header-anchor" href="#what-you-can-hope-to-achieve" aria-label="Permalink to &quot;What you can hope to achieve&quot;">​</a></h2><p>Realistic outcomes from a successful glitch in an attack chain:</p><ul><li><strong>Skip a conditional branch.</strong> The most common useful primitive: the chip evaluates <code>if (rdp_enabled) { lock_debug(); }</code> and the fault causes the branch to fall through.</li><li><strong>Wrong value from a fuse read.</strong> The eFuse sequencer reads a byte; the fault causes the read to return all <code>1</code>s or to leak a previous bus value, bypassing the &quot;protection enabled&quot; flag.</li><li><strong>Corrupt a comparison result.</strong> PIN check: <code>if (memcmp(input, stored, 4) == 0)</code>. The fault forces <code>==</code> to be true regardless of the inputs.</li><li><strong>Skip a CRC / signature check.</strong> The bootloader validates the application image and would refuse to jump to a tampered one; the fault skips the validation.</li><li><strong>Recover the program counter from an unexpected location.</strong> A reset vector glitch, or a fault during context-switch save, can expose secrets in registers or memory that should not be readable.</li></ul><p>What FI does <strong>not</strong> do well:</p><ul><li>Read out an entire flash image directly — you usually need a follow-up step (re-enable the debug port, or chain FI to a flash read primitive).</li><li>Defeat strong cryptographic operations — DES/AES still work bitwise correctly even with corrupted state; you want SCA (Chapter 32) for that.</li><li>Work against properly hardened code with hardware glitch detection (modern Cortex-M v8.1 and some secure elements include it).</li></ul><h2 id="the-tools" tabindex="-1">The tools <a class="header-anchor" href="#the-tools" aria-label="Permalink to &quot;The tools&quot;">​</a></h2><p><strong>ChipWhisperer</strong> (<code>https://github.com/newaetech/chipwhisperer</code>, <code>https://newae.com</code>). Open hardware and software for both SCA and FI. By Colin O&#39;Flynn (NewAE Technology, Halifax). Variants in production at time of writing:</p><ul><li><strong>ChipWhisperer-Nano</strong> — entry-level, USB-powered, ~USD 50–100. Voltage glitching only, fixed target. Good for learning.</li><li><strong>ChipWhisperer-Lite</strong> — the long-standing standard. ~USD 250. Voltage + clock glitching, capture for SCA, ARM target on board.</li><li><strong>ChipWhisperer-Husky</strong> — newer, more capable, ~USD 1k. Adds higher trigger speed, better capture depth.</li><li><strong>ChipWhisperer-Pro</strong> — for serious SCA work, much larger capture buffers.</li></ul><p>The accompanying <strong>NewAE Wiki</strong> and Jupyter-notebook tutorials are freely available and step-by-step.</p><p><strong>ChipSHOUTER</strong> (also NewAE). Dedicated EMFI tool. ~USD 10k. Coil mounted on an XY stage; pulses up to a few hundred volts into the coil for the EM transient. Used in published research and in professional pen-test labs.</p><p><strong>Riscure Inspector FI</strong> and <strong>Inspector SCA</strong> — commercial, professional tooling from Riscure (Delft). Tens of thousands of dollars. Used by secure-element vendors, automotive OEMs, payment labs. White papers are often public.</p><p><strong>DIY voltage glitchers</strong> — for hobbyist-level voltage glitching, people have built crowdsourced PCBs around a fast N-channel MOSFET (to short Vcore briefly to ground), an FPGA or fast MCU for timing, and a target socket. The Hardware Hacking Handbook (reference below) covers this in detail.</p><p>The combination &quot;ChipWhisperer-Lite plus the public NewAE tutorials&quot; is the best entry point for a working engineer. You can reproduce published academic attacks within a weekend.</p><h2 id="target-preparation" tabindex="-1">Target preparation <a class="header-anchor" href="#target-preparation" aria-label="Permalink to &quot;Target preparation&quot;">​</a></h2><p>Before you can glitch a chip you need to:</p><ol><li><strong>Identify the core voltage rail and its decoupling.</strong> Modern MCUs have a <code>Vcore</code> separate from I/O voltage (typically 1.2 V or 1.8 V for the core; 3.3 V for I/O). Find it on the schematic or with a multimeter. Glitching Vcore is what you want; the I/O rail&#39;s brownout would simply reset the chip.</li><li><strong>Reduce decoupling locally.</strong> Glitching means pulling the rail low for a few nanoseconds; if there&#39;s a 10 µF capacitor right at the core pin, you cannot do that fast enough. Many published attacks involve removing on-board ceramic caps near the part.</li><li><strong>Establish a trigger.</strong> The chip must do the operation you want to fault at a known, repeatable time. The trigger can be: <ul><li>a GPIO the firmware toggles before the operation,</li><li>a pattern in UART output the glitcher matches against,</li><li>a power-rail pattern the glitcher recognises,</li><li>a reset event with a fixed timing offset.</li></ul></li><li><strong>Find the glitch parameters.</strong> Three knobs to sweep: offset from trigger (when to glitch), width (how long to hold the rail low), and amplitude (how low to pull it). The search space is huge. Tools provide hill-climbing or genetic search; brute force with a few thousand trials per parameter combination is also common for first-pass exploration.</li></ol><p>A successful glitch campaign on a previously-attacked chip can run in hours. A campaign against a new target — locating the right operation in time, finding parameters that produce useful faults without crashing the chip outright — can take weeks.</p><h2 id="published-real-world-attacks" tabindex="-1">Published real-world attacks <a class="header-anchor" href="#published-real-world-attacks" aria-label="Permalink to &quot;Published real-world attacks&quot;">​</a></h2><p>This is not an exhaustive list, but each item is independently verifiable from the source it cites. Where I am unsure of a specific year or revision, I have left it general.</p><p><strong>Espressif ESP32 secure boot bypass — LimitedResults (limitedresults.com).</strong> A widely-cited series of blog posts (2019–2020) documenting voltage glitching against ESP32 V1 silicon. The attacks bypassed the secure-boot signature check and, in follow-up work, recovered the flash-encryption key from eFuses by faulting the fuse-readout state machine. Espressif issued silicon revisions and software mitigations in response; later ESP32 variants (S2/S3/C-series) have additional countermeasures.</p><p><strong>Nordic nRF52 series APPROTECT bypass — LimitedResults and others.</strong> Voltage glitching used to bypass the readback protection on nRF52832 / nRF52840 in production silicon. Documented in multiple blog posts and a Black Hat USA 2020 presentation (Tomas Sušánka, &quot;nRF52 Debug Resurrection (APPROTECT Bypass)&quot;). Nordic later released revised silicon that mitigates the attack.</p><p><strong>Trezor One PIN-counter bypass — Kraken Security Labs (2019).</strong> Voltage glitching used to bypass the PIN-retry counter on the Trezor One hardware wallet, enabling unlimited PIN guessing. Required physical possession of the device and disassembly. Documented in their blog post and a series of follow-up responses from Trezor (who issued mitigations in firmware).</p><p><strong>STM32 readout-protection downgrade.</strong> Multiple researchers have demonstrated downgrade attacks against STM32 RDP Level 1 (and in some published cases against Level 2) using voltage or EM glitching. Documented in Hardware Hacking Handbook chapters and in multiple Hardwear.io / Black Hat talks. ST has updated guidance and recommends additional defensive measures in their AN4701 &quot;Proprietary Code Read-Out Protection on STM32&quot; application note.</p><p><strong>Smartcard glitch attacks.</strong> A long-running research thread in the academic literature dating from the late 1990s. Eurocrypt, CHES, and FDTC have decades of papers on faulting smartcard implementations of RSA, DES, and AES. These attacks are why modern smartcards include dedicated glitch detectors.</p><p><strong>Tesla Model S/X infotainment dumps.</strong> Multiple researchers (Comma.ai, KU Leuven and others) have documented eFuse and key extraction work against Tesla MCU silicon, including techniques involving fault injection. Tesla has updated countermeasures in successive hardware revisions.</p><p>For each of these, the underlying research is the citation — read the original sources rather than the press coverage. Press articles about &quot;hardware hacks&quot; frequently omit or distort the specifics that matter.</p><h2 id="mitigations-and-detection" tabindex="-1">Mitigations and detection <a class="header-anchor" href="#mitigations-and-detection" aria-label="Permalink to &quot;Mitigations and detection&quot;">​</a></h2><p>Defensive techniques the chip designer can deploy, and that you will increasingly see in modern silicon:</p><ul><li><strong>Hardware glitch detectors.</strong> Comparators on the supply rail and on the clock that trigger a reset if they detect an out-of-spec condition. Now standard on ARMv8-M security extension parts and on most modern secure elements.</li><li><strong>Redundant computation.</strong> Compute the security-critical check twice (or three times) and compare. A single fault that flips one result does not flip the other. The Hardware Hacking Handbook calls this &quot;do it twice&quot; and covers patterns in detail.</li><li><strong>Constant-time / data-independent flow.</strong> Convert the branch-on-secret pattern (<code>if (auth_ok) ...</code>) into a data-flow that uses the result without branching. Even when glitched, the path executes the same instructions.</li><li><strong>Random dummy operations.</strong> Insert non-deterministic delays before security-critical operations. The attacker&#39;s trigger becomes harder to align.</li><li><strong>Memory and bus parity / ECC.</strong> A faulted bus transfer is detected by ECC and either corrected or signals a fault.</li><li><strong>Defensive programming idioms.</strong> &quot;Never trust a <code>==</code> for a security decision; use bitwise checks where a single bit flip changes the outcome predictably.&quot; There is academic literature (&quot;Fault-attack-resistant cryptographic implementations&quot;) covering this in depth.</li></ul><p>For a working engineer doing reverse engineering — not defence — the relevance is that newer silicon is harder to glitch. Attacks against parts released in the last 2–3 years are typically harder than against parts from a decade ago.</p><h2 id="when-to-choose-fi-vs-another-approach" tabindex="-1">When to choose FI vs. another approach <a class="header-anchor" href="#when-to-choose-fi-vs-another-approach" aria-label="Permalink to &quot;When to choose FI vs. another approach&quot;">​</a></h2><p>FI is the right tool when:</p><ul><li>The chip refuses to give up its secrets through normal channels.</li><li>Debug is locked; flash is read-out-protected; no remote bug-class exploit exists.</li><li>You have physical access to the device, can disassemble it without destroying it, and can remove decoupling capacitors.</li><li>The attack target is not strong cryptography (use SCA for that), but a single conditional or fuse read.</li></ul><p>FI is the wrong tool when:</p><ul><li>The chip you want to attack has only a few units in existence and bricking it is unacceptable.</li><li>The secret is protected by a strong cryptographic computation that does not depend on a single branch decision.</li><li>The chip has hardware glitch detection in production silicon.</li><li>You only need to recover behaviour, not secrets — Chapter 21&#39;s dynamic-analysis approach is simpler.</li></ul><h2 id="reading" tabindex="-1">Reading <a class="header-anchor" href="#reading" aria-label="Permalink to &quot;Reading&quot;">​</a></h2><p>The single best resource for getting started:</p><ul><li><strong>Hardware Hacking Handbook</strong> by Colin O&#39;Flynn and Jasper van Woudenberg (No Starch Press, 2021). Three chapters on FI plus several on SCA. The current canonical text. Already cited in Appendix D.</li></ul><p>After that:</p><ul><li><strong>ChipWhisperer documentation and Jupyter tutorials</strong> (<code>https://chipwhisperer.readthedocs.io</code> and the <code>chipwhisperer/jupyter</code> GitHub repo). Reproducible attacks against an Atmel/Microchip target you can buy and follow along with.</li><li><strong>LimitedResults blog</strong> (<code>https://limitedresults.com</code>) — particularly the ESP32 and nRF52 series.</li><li><strong>Kraken Security Labs blog</strong> — Trezor write-up and related hardware-wallet work.</li><li><strong>Riscure white papers</strong> (<code>https://www.riscure.com/publications</code>) — long-form treatments of SCA and FI from a professional services perspective.</li><li><strong>CHES</strong> (Cryptographic Hardware and Embedded Systems) conference proceedings — the academic state of the art for defence and attack on embedded crypto. Free via IACR.</li><li><strong>FDTC</strong> (Fault Diagnosis and Tolerance in Cryptography) workshop — narrower focus on FI specifically.</li></ul>',48)])])}const g=t(r,[["render",n]]);export{u as __pageData,g as default};
