import{c as a,Q as t,j as n,m as s}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"Symbol Recovery and Zignatures","description":"","frontmatter":{},"headers":[],"relativePath":"part2/10-symbols-zignatures.md","filePath":"part2/10-symbols-zignatures.md","lastUpdated":null}'),i={name:"part2/10-symbols-zignatures.md"};function o(r,e,c,d,l,p){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="symbol-recovery-and-zignatures" tabindex="-1">Symbol Recovery and Zignatures <a class="header-anchor" href="#symbol-recovery-and-zignatures" aria-label="Permalink to &quot;Symbol Recovery and Zignatures&quot;">​</a></h1><p>A stripped firmware image is a list of unnamed functions. Some of those functions are application code that nobody outside the original developers will ever recognise, but a substantial fraction are library code: the C runtime, vendor HALs, RTOS kernel routines, crypto primitives, TCP/IP stacks. Those are recoverable. This chapter covers how.</p><p>The technique is <strong>function signature matching</strong>: you build a database of recognisable byte patterns from a known binary (a vendor SDK, a reference build, an open-source library), and then ask r2 to look for matches in the unknown binary. When it finds one, the function gets renamed automatically. The IDA equivalent is FLIRT; r2&#39;s is <strong>zignatures</strong> (&quot;signatures&quot; is taken).</p><h2 id="why-this-matters" tabindex="-1">Why this matters <a class="header-anchor" href="#why-this-matters" aria-label="Permalink to &quot;Why this matters&quot;">​</a></h2><p>Imagine you load a 256 KiB STM32 firmware. <code>aaa</code> finds 600 functions. About 200 of them are HAL routines you can recognise from the vendor SDK. Without signatures, you read each one cold; with signatures, those 200 land already-named (<code>HAL_GPIO_Init</code>, <code>HAL_UART_Transmit</code>, <code>HAL_RCC_OscConfig</code>, …) and you only have to read the 400 that are genuinely application code.</p><p>The same applies to:</p><ul><li><strong>Newlib / glibc-arm</strong>: <code>memcpy</code>, <code>memset</code>, <code>strcpy</code>, <code>strlen</code>, <code>printf</code>, <code>malloc</code>, <code>free</code>. Without these named, every function looks like it reinvents string handling.</li><li><strong>mbedTLS / wolfSSL</strong>: AES, SHA, RSA primitives.</li><li><strong>lwIP / FreeRTOS+TCP</strong>: socket, packet, ARP, ICMP code.</li><li><strong>FreeRTOS / Zephyr / NuttX</strong>: scheduler, queue, task management.</li><li><strong>libgcc</strong>: long division, soft-float, exception unwinding.</li></ul><p>A good signature database can name 30–60% of the functions in a typical embedded firmware. That is hours of reading you do not have to do.</p><h2 id="zignature-commands" tabindex="-1">Zignature commands <a class="header-anchor" href="#zignature-commands" aria-label="Permalink to &quot;Zignature commands&quot;">​</a></h2><p>The <code>z*</code> family:</p><table tabindex="0"><thead><tr><th>Command</th><th>What it does</th></tr></thead><tbody><tr><td><code>z</code></td><td>list all loaded zignatures</td></tr><tr><td><code>z.</code></td><td>list zigs matching the current function</td></tr><tr><td><code>zb</code></td><td>best matches for current function</td></tr><tr><td><code>zg</code></td><td>generate a zignature for the current function</td></tr><tr><td><code>zo</code></td><td>save zignatures to a file</td></tr><tr><td><code>z/</code></td><td>search the binary for matches against loaded zignatures</td></tr><tr><td><code>z*</code></td><td>print zignatures as r2 commands (re-runnable)</td></tr><tr><td><code>z-</code></td><td>delete a zignature</td></tr><tr><td><code>zs</code></td><td>list zignature spaces (group by name)</td></tr></tbody></table><p>A zignature is a fingerprint of a function consisting of:</p><ul><li>the <strong>bytes</strong> of the function (with wildcards for relocations and immediate operands that vary);</li><li>the <strong>graph</strong> structure (basic blocks, edges);</li><li>the <strong>mnemonics</strong> in order;</li><li>the <strong>xrefs</strong> the function makes (which functions it calls);</li><li>the function&#39;s <strong>size</strong>.</li></ul><p>You can match on any subset.</p><h2 id="building-a-zignature-database-from-a-known-binary" tabindex="-1">Building a zignature database from a known binary <a class="header-anchor" href="#building-a-zignature-database-from-a-known-binary" aria-label="Permalink to &quot;Building a zignature database from a known binary&quot;">​</a></h2><p>Step 1: get a binary you do have symbols for. The vendor SDK ships with example builds and library archives:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ ls /opt/STM32CubeF4/Drivers/STM32F4xx_HAL_Driver/Lib/</span></span>
<span class="line"><span>libSTM32F4xx_HAL_Driver.a</span></span>
<span class="line"><span>$ arm-none-eabi-ar x libSTM32F4xx_HAL_Driver.a</span></span>
<span class="line"><span>$ ls *.o</span></span>
<span class="line"><span>stm32f4xx_hal.o</span></span>
<span class="line"><span>stm32f4xx_hal_gpio.o</span></span>
<span class="line"><span>stm32f4xx_hal_uart.o</span></span>
<span class="line"><span>...</span></span></code></pre></div><p>Step 2: load each <code>.o</code> (or the linked archive) in r2 and analyse:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 stm32f4xx_hal_gpio.o</span></span>
<span class="line"><span>[0x...]&gt; aaa</span></span></code></pre></div><p>Step 3: generate zignatures for every function:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; zg                   # for current function only</span></span>
<span class="line"><span>[0x...]&gt; zg @@f               # for every function (preferred)</span></span>
<span class="line"><span>[0x...]&gt; zs                   # list zig spaces</span></span>
<span class="line"><span>[0x...]&gt; zo stm32f4_hal.sdb   # save to file</span></span></code></pre></div><p>Step 4: repeat for every library you care about, then concatenate:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ ls *.sdb</span></span>
<span class="line"><span>stm32f4_hal.sdb</span></span>
<span class="line"><span>newlib_arm_thumb.sdb</span></span>
<span class="line"><span>freertos.sdb</span></span>
<span class="line"><span>mbedtls.sdb</span></span></code></pre></div><p>Or build a single combined database by loading all of them in one r2 session before saving.</p><h2 id="matching-against-an-unknown-binary" tabindex="-1">Matching against an unknown binary <a class="header-anchor" href="#matching-against-an-unknown-binary" aria-label="Permalink to &quot;Matching against an unknown binary&quot;">​</a></h2><p>Step 1: load the unknown binary with the right architecture and analyse:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a arm -b 16 -m 0x08000000 unknown.bin</span></span>
<span class="line"><span>[0x08000000]&gt; aaa</span></span></code></pre></div><p>Step 2: load the zignature database:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; zo stm32f4_hal.sdb       # load</span></span>
<span class="line"><span>[0x...]&gt; zo newlib_arm_thumb.sdb</span></span>
<span class="line"><span>[0x...]&gt; zo freertos.sdb</span></span>
<span class="line"><span>[0x...]&gt; z                         # confirm loaded</span></span>
<span class="line"><span>[0x...]&gt; zs                        # spaces</span></span></code></pre></div><p>Step 3: search:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; z/                        # search and apply matches</span></span></code></pre></div><p><code>z/</code> walks every loaded zignature, scores it against every function in the binary, and renames functions that match above the threshold. The threshold is <code>e zign.threshold</code> (0.0 to 1.0; default 0.0 — match anything plausible). Higher values produce fewer but more confident matches.</p><p>Step 4: review what changed:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; afl ~ ^sym\\.            # functions that now have names</span></span>
<span class="line"><span>[0x...]&gt; afl ~ fcn\\.             # still anonymous</span></span></code></pre></div><p>Adjust the threshold and re-run <code>z/</code> until the match count looks right. Too low and you get false positives (a generic 4-instruction function matches twenty things in your DB); too high and you miss real matches because the compiler used slightly different optimisation settings.</p><h2 id="per-zignature-controls" tabindex="-1">Per-zignature controls <a class="header-anchor" href="#per-zignature-controls" aria-label="Permalink to &quot;Per-zignature controls&quot;">​</a></h2><p>Inspect a single zignature:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; z foo</span></span>
<span class="line"><span>name: foo</span></span>
<span class="line"><span>bytes: 80b500f0..fa00bd...</span></span>
<span class="line"><span>graph: cc=2, nbbs=3, edges=4, ebbs=1</span></span>
<span class="line"><span>addr: 0x08001234</span></span>
<span class="line"><span>refs: HAL_GPIO_Init, HAL_RCC_GetClockFreq</span></span></code></pre></div><p><code>bytes</code> is the byte fingerprint with <code>..</code> wildcards where the bytes are not stable (typically PC-relative offsets, immediate constants the linker fills in, or BL targets).</p><p><code>graph</code> is the structural metric. Two functions with identical byte sequences but different graph metrics are not a match.</p><p><code>refs</code> is the call graph fragment. If the unknown function calls the same other functions in the same order, the match is strongly reinforced.</p><p>Increase confidence in a match by requiring multiple metrics:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e zign.match.bytes = true     # default true</span></span>
<span class="line"><span>[0x...]&gt; e zign.match.graph = true</span></span>
<span class="line"><span>[0x...]&gt; e zign.match.refs  = true</span></span>
<span class="line"><span>[0x...]&gt; e zign.match.types = true</span></span>
<span class="line"><span>[0x...]&gt; e zign.threshold = 0.9        # be strict</span></span></code></pre></div><h2 id="caveats" tabindex="-1">Caveats <a class="header-anchor" href="#caveats" aria-label="Permalink to &quot;Caveats&quot;">​</a></h2><p><strong>Compiler flags matter.</strong> A function compiled with <code>-Os</code> and the same function compiled with <code>-O2</code> produce different byte patterns and different graphs. If your unknown binary was built with vendor defaults and your reference was built with optimisation tweaks, match rates drop. Build the reference with the vendor&#39;s published flags.</p><p><strong>ABI matters.</strong> A HAL built for hard-float ABI matches differently than one built for soft-float. Match the unknown&#39;s ABI when building the reference.</p><p><strong>Library version matters.</strong> STM32 HAL <code>1.27.0</code> and <code>1.28.0</code> differ in dozens of functions. The differences are small (a few extra instructions, a different return type) but break exact matches. Either build a database per version, or accept lower match rates and verify each match by hand.</p><p><strong>Tiny functions match too easily.</strong> A 3-instruction <code>memset</code> fragment matches the same fragment in <code>memcpy</code>, <code>memmove</code>, and any other function that ends with a similar <code>LDR; STR; SUBS; BNE</code> loop. Filter out matches below some size:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e zign.minsz = 0x20      # only consider functions ≥ 32 bytes</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>Always confirm signature matches before relying on them. A wrong match is worse than no match because it gives you a confidently incorrect name. Open the function, read it, decide if the name fits.</p></div><h2 id="symbol-recovery-from-elsewhere" tabindex="-1">Symbol recovery from elsewhere <a class="header-anchor" href="#symbol-recovery-from-elsewhere" aria-label="Permalink to &quot;Symbol recovery from elsewhere&quot;">​</a></h2><p>Zignatures are not the only source of names. Scan all of these on every binary:</p><p><strong>Strings near function entries.</strong> When code does <code>printf(&quot;foo: %d\\n&quot;, ...)</code>, the format string is a strong hint about what the function does:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; izz~ &quot;:&quot;            # all strings with colons (often format strings)</span></span>
<span class="line"><span>[0x...]&gt; axt @ str.foo_d     # which functions reference that string?</span></span></code></pre></div><p><strong>Error message strings.</strong> RTOSes emit error strings with the function name in them: <code>&quot;vTaskDelay called with NULL handle&quot;</code> tells you the function nearby is <code>vTaskDelay</code>.</p><p><strong>Function pointer tables.</strong> A const array of function pointers with adjacent string descriptions is a goldmine. Dump:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; pxw 256 @ 0x08010000   # words at the table</span></span></code></pre></div><p>If the table interleaves <code>function_address, &quot;function_name_string&quot;</code>, you can rename them in a loop with an r2pipe script (Chapter 25).</p><p><strong>Build IDs and compiler signatures.</strong> Find them with:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; izz ~ GCC</span></span>
<span class="line"><span>[0x...]&gt; izz ~ clang</span></span>
<span class="line"><span>[0x...]&gt; izz ~ &quot;Build:&quot;</span></span></code></pre></div><p>These do not name functions but they tell you the toolchain, which informs which signature DB to use.</p><p><strong>Vector tables.</strong> On Cortex-M, the first 16+IRQ words are the vector table. You know the IRQ ordering from the vendor&#39;s header file. Each vector entry&#39;s target is an interrupt handler whose name you can reasonably guess from the IRQ. Chapter 12 covers this in detail.</p><p><strong>Linker section names.</strong> ELF firmware sometimes leaves <code>.text.HAL_GPIO_Init</code> style section names even after symbol stripping. Check <code>iS</code> for them and recover the function names from the sections.</p><p>The combination — zignatures plus strings plus vector tables plus section names plus a careful read of the entry function — typically names 40–70% of an embedded firmware&#39;s functions before you have read a single line of disassembly cold.</p>`,64)])])}const g=a(i,[["render",o]]);export{u as __pageData,g as default};
