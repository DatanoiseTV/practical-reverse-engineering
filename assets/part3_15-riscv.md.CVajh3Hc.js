import{c as s,Q as a,j as t,m as o}from"./chunks/framework.DxgWTcNC.js";const u=JSON.parse('{"title":"RISC-V (ESP32-C, BL602, generic RV32)","description":"","frontmatter":{},"headers":[],"relativePath":"part3/15-riscv.md","filePath":"part3/15-riscv.md","lastUpdated":null}'),i={name:"part3/15-riscv.md"};function n(d,e,c,r,p,l){return a(),t("div",null,[...e[0]||(e[0]=[o(`<h1 id="risc-v-esp32-c-bl602-generic-rv32" tabindex="-1">RISC-V (ESP32-C, BL602, generic RV32) <a class="header-anchor" href="#risc-v-esp32-c-bl602-generic-rv32" aria-label="Permalink to &quot;RISC-V (ESP32-C, BL602, generic RV32)&quot;">​</a></h1><p>RISC-V is the architecture you reach for when the alternative was proprietary, expensive, or politically inconvenient. In the embedded space it appears in: Espressif&#39;s ESP32-C3, C6, H2, P4 (Andes-IP RV32); Bouffalo&#39;s BL602/BL604/BL808; SiFive&#39;s HiFive boards; WCH&#39;s CH32V series; Allwinner&#39;s D1 (single-core RV64GC); and a fast-growing list of others. This chapter covers the RV32 variants you will encounter in microcontroller firmware, with notes on RV64 where they differ.</p><h2 id="architectural-overview" tabindex="-1">Architectural overview <a class="header-anchor" href="#architectural-overview" aria-label="Permalink to &quot;Architectural overview&quot;">​</a></h2><p>RISC-V is a clean, fixed-32-bit (with 16-bit compressed extension) load-store ISA. Compared to ARM:</p><ul><li><strong>Fewer surprises in the encoding.</strong> Every base instruction is 32 bits; compressed (<code>c</code> extension) instructions are 16 bits. No Thumb-vs-ARM mode switching; the decoder figures out length from the low 2 bits of the first halfword.</li><li><strong>No condition flags.</strong> Every conditional branch encodes the condition (<code>beq</code>, <code>bne</code>, <code>blt</code>, <code>bge</code>, <code>bltu</code>, <code>bgeu</code>) and the two registers it compares.</li><li><strong>PC-relative everything.</strong> <code>auipc</code> (Add Upper Immediate to PC) is the building block of position-independent addressing.</li><li><strong>Lots of registers</strong> (32 integer + 32 float in the floating-point variants). Calling convention reserves specific roles.</li><li><strong>Modular ISA.</strong> A given chip implements some combination of base and extensions. Most embedded RV32 cores are <code>RV32IMC</code> (integer + multiply + compressed) or <code>RV32IMAC</code> (+ atomics). Some include <code>F</code>/<code>D</code> for floating-point. ESP32-C3 is RV32IMC.</li></ul><h2 id="calling-convention" tabindex="-1">Calling convention <a class="header-anchor" href="#calling-convention" aria-label="Permalink to &quot;Calling convention&quot;">​</a></h2><p>ABI register names you should memorise:</p><table tabindex="0"><thead><tr><th>Reg name</th><th>ABI name</th><th>Role</th></tr></thead><tbody><tr><td>x0</td><td>zero</td><td>constant zero</td></tr><tr><td>x1</td><td>ra</td><td>return address</td></tr><tr><td>x2</td><td>sp</td><td>stack pointer</td></tr><tr><td>x3</td><td>gp</td><td>global pointer (linker-fixed)</td></tr><tr><td>x4</td><td>tp</td><td>thread pointer</td></tr><tr><td>x5–x7</td><td>t0–t2</td><td>temporaries (caller-saved)</td></tr><tr><td>x8</td><td>s0/fp</td><td>saved/frame pointer</td></tr><tr><td>x9</td><td>s1</td><td>saved</td></tr><tr><td>x10–x11</td><td>a0, a1</td><td>function args / return values</td></tr><tr><td>x12–x17</td><td>a2–a7</td><td>function args</td></tr><tr><td>x18–x27</td><td>s2–s11</td><td>saved (callee-saved)</td></tr><tr><td>x28–x31</td><td>t3–t6</td><td>temporaries</td></tr></tbody></table><ul><li>Arguments: <code>a0..a7</code> (8 args in registers, more on stack)</li><li>Return: <code>a0</code> (and <code>a1</code> for 64-bit returns on RV32)</li><li>Saved: <code>s0..s11</code> (callee-saved)</li><li>Temporaries: <code>t0..t6</code> (caller-saved)</li></ul><p>R2 displays both raw (<code>x10</code>) and ABI (<code>a0</code>) names depending on <code>asm.syntax</code>:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e asm.syntax = att          # ABI names</span></span>
<span class="line"><span>[0x...]&gt; e asm.syntax = abi          # ABI names (alias)</span></span>
<span class="line"><span>[0x...]&gt; e asm.syntax = none         # raw register numbers</span></span></code></pre></div><p>ABI names are easier to read.</p><h2 id="loading" tabindex="-1">Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;Loading&quot;">​</a></h2><p>For a clean ELF (the common case for SiFive, WCH, BL602 with proper build):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 firmware.elf</span></span>
<span class="line"><span>[0x...]&gt; i</span></span>
<span class="line"><span>arch     riscv</span></span>
<span class="line"><span>bits     32</span></span>
<span class="line"><span>machine  riscv</span></span></code></pre></div><p>R2 reads the architecture from the ELF header. <code>aaa</code> and you are off.</p><p>For a raw blob:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ r2 -a riscv -b 32 -c rv32imac -m 0x42000000 firmware.bin       # ESP32-C3</span></span>
<span class="line"><span>$ r2 -a riscv -b 32 -c rv32imafc -m 0x21010000 firmware.bin      # BL602</span></span>
<span class="line"><span>$ r2 -a riscv -b 64 -c rv64gc -m 0x80000000 firmware.bin         # generic Linux RV64</span></span></code></pre></div><p>The <code>-c</code> flag matters because it tells r2 which extensions are available — without <code>c</code> (compressed), r2 will not decode 16-bit opcodes. With <code>c</code> enabled, r2 handles both widths transparently.</p><div class="warning custom-block"><p class="custom-block-title">Warning</p><p>RISC-V&#39;s compressed extension uses the same opcode space as base RV32I, distinguished by the low 2 bits. If you set <code>-c rv32i</code> (no compressed) on a binary that uses compressed, every other instruction decodes as garbage. Default to <code>rv32imac</code> or <code>rv32imc</code> for embedded firmware unless you are sure compressed is disabled.</p></div><h2 id="esp32-c3-c6-specifics" tabindex="-1">ESP32-C3 / C6 specifics <a class="header-anchor" href="#esp32-c3-c6-specifics" aria-label="Permalink to &quot;ESP32-C3 / C6 specifics&quot;">​</a></h2><p>The ESP32-C3 is a single-core RV32IMC chip. The C6 adds an LP (low-power) RISC-V core alongside the main HP core, both with slightly different memory maps. From a reverse engineering standpoint they look like ARM Cortex-M with a different ISA.</p><p>Memory map for ESP32-C3 (per the ESP32-C3 TRM §3):</p><table tabindex="0"><thead><tr><th>Address</th><th>Region</th></tr></thead><tbody><tr><td><code>0x3C000000–0x3C800000</code></td><td>DROM (rodata, flash data-bus mapped)</td></tr><tr><td><code>0x3FC80000–0x3FCE0000</code></td><td>DRAM (data-bus view of SRAM, 400 KiB)</td></tr><tr><td><code>0x40000000–0x4005FFFF</code></td><td>Internal ROM</td></tr><tr><td><code>0x4037C000–0x403DFFFF</code></td><td>IRAM (instruction-bus view of SRAM)</td></tr><tr><td><code>0x42000000–0x42800000</code></td><td>IROM (flash code, instruction-bus mapped)</td></tr><tr><td><code>0x50000000–0x50001FFF</code></td><td>RTC fast memory</td></tr><tr><td><code>0x60000000–0x600FFFFF</code></td><td>Peripherals</td></tr></tbody></table><p>The image format is the same Espressif format as ESP32 (Chapter 14). The same multi-segment loading approach applies. ROM symbol files exist in ESP-IDF under <code>components/esp_rom/esp32c3/ld/</code>.</p><h2 id="bl602-specifics" tabindex="-1">BL602 specifics <a class="header-anchor" href="#bl602-specifics" aria-label="Permalink to &quot;BL602 specifics&quot;">​</a></h2><p>Bouffalo Lab&#39;s BL602 is a single-core RV32IMAC (up to 192 MHz) with an 802.11 b/g/n WiFi MAC integrated. Memory map highlights (per the BL602 Reference Manual):</p><ul><li>Boot ROM at <code>0x21000000</code></li><li>ITCM (instruction tightly-coupled memory) at <code>0x22014000</code></li><li>DTCM at <code>0x42014000</code></li><li>Main SRAM at <code>0x42000000</code>–<code>0x42013FFF</code></li><li>Flash (XIP) at <code>0x23000000</code></li><li>Peripherals at <code>0x40000000</code></li></ul><p>The vendor SDK is open-source (<code>bl_iot_sdk</code>). Build the SDK once and use it as a signature source.</p><h2 id="generic-rv32imac-microcontroller-flow" tabindex="-1">Generic RV32IMAC microcontroller flow <a class="header-anchor" href="#generic-rv32imac-microcontroller-flow" aria-label="Permalink to &quot;Generic RV32IMAC microcontroller flow&quot;">​</a></h2><p>Without a vendor-specific BSP, the flow is:</p><ol><li><p>Load with the right <code>-c</code> (rv32imac for most chips).</p></li><li><p>The first interesting code is at the reset vector, which on most RV32 microcontrollers is the address pointed to by <code>mtvec</code> after reset — if you have no boot ROM symbols, find it by looking at the start of the loaded image. The vendor&#39;s startup file usually begins with a <code>j</code> (jump) to the actual entry, sometimes preceded by a few interrupt vectors.</p></li><li><p>The reset handler typically sets <code>gp</code> (global pointer):</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>.option push</span></span>
<span class="line"><span>.option norelax</span></span>
<span class="line"><span>1: auipc gp, %pcrel_hi(__global_pointer$)</span></span>
<span class="line"><span>   addi  gp, gp, %pcrel_lo(1b)</span></span>
<span class="line"><span>.option pop</span></span></code></pre></div><p>If you see <code>auipc gp, ...; addi gp, gp, ...</code> near the start, that is <code>gp</code> initialisation. After that, accesses through <code>gp</code> reach <code>.sdata</code> and <code>.sbss</code>.</p></li><li><p>After <code>gp</code>, the reset handler clears <code>.bss</code>, copies <code>.data</code> from flash to RAM, then calls <code>main</code>.</p></li></ol><p>A typical RISC-V function prologue:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>addi  sp, sp, -0x20      ; allocate stack frame</span></span>
<span class="line"><span>sw    ra, 0x1c(sp)       ; save return address</span></span>
<span class="line"><span>sw    s0, 0x18(sp)       ; save frame pointer</span></span>
<span class="line"><span>addi  s0, sp, 0x20       ; new frame pointer</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>lw    s0, 0x18(sp)       ; restore</span></span>
<span class="line"><span>lw    ra, 0x1c(sp)</span></span>
<span class="line"><span>addi  sp, sp, 0x20</span></span>
<span class="line"><span>ret</span></span></code></pre></div><p>Compressed forms are common and shorter:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>c.addi sp, -0x20</span></span>
<span class="line"><span>c.sw   ra, 0x1c(sp)</span></span>
<span class="line"><span>c.sw   s0, 0x18(sp)</span></span>
<span class="line"><span>c.addi s0, sp, 0x20</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>c.lw   s0, 0x18(sp)</span></span>
<span class="line"><span>c.lw   ra, 0x1c(sp)</span></span>
<span class="line"><span>c.addi sp, 0x20</span></span>
<span class="line"><span>c.jr   ra</span></span></code></pre></div><p>R2 displays both with the same readability.</p><h2 id="csrs-control-and-status-registers" tabindex="-1">CSRs (Control and Status Registers) <a class="header-anchor" href="#csrs-control-and-status-registers" aria-label="Permalink to &quot;CSRs (Control and Status Registers)&quot;">​</a></h2><p>RISC-V CSRs are accessed with the <code>csrr*</code> family:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>csrr  a0, mstatus           ; read CSR into a0</span></span>
<span class="line"><span>csrw  mtvec, a0             ; write a0 to CSR</span></span>
<span class="line"><span>csrrs a0, mie, a1           ; read mie, then set bits in a1</span></span>
<span class="line"><span>csrrc a0, mie, a1           ; read mie, then clear bits in a1</span></span></code></pre></div><p>CSR numbers (12-bit) and names are part of the spec:</p><ul><li><code>mstatus</code> (0x300), <code>misa</code> (0x301), <code>mie</code> (0x304), <code>mtvec</code> (0x305)</li><li><code>mscratch</code> (0x340), <code>mepc</code> (0x341), <code>mcause</code> (0x342)</li><li><code>mip</code> (0x344)</li><li><code>mhartid</code> (0xF14) — hart ID; useful for SMP code</li></ul><p>Vendor-specific (custom) M-mode CSRs live in <code>0xBC0</code>–<code>0xBFF</code> (MRW) and <code>0xFC0</code>–<code>0xFFF</code> (MRO). The <code>0x7C0</code>–<code>0x7FF</code> range is M-mode-only debug custom — accessing those CSRs from non-debug code traps. ESP32-C chips use the custom ranges for vendor extensions; values come from the <code>riscv-private.h</code> headers in ESP-IDF.</p><p>R2 decodes CSR access correctly for the standard set; for vendor CSRs you may see <code>csrr a0, 0x7c1</code> instead of a name. Annotate with <code>CC</code> (comments).</p><h2 id="instructions-worth-recognising" tabindex="-1">Instructions worth recognising <a class="header-anchor" href="#instructions-worth-recognising" aria-label="Permalink to &quot;Instructions worth recognising&quot;">​</a></h2><p>A few RISC-V idioms that throw newcomers:</p><p><strong>Computed branch:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>auipc t0, 0x1            ; t0 = pc + 0x1000</span></span>
<span class="line"><span>jalr  zero, 0x234(t0)    ; jump to t0 + 0x234, no return</span></span></code></pre></div><p>This is how a non-PIE binary does a long branch. R2 resolves the target and shows it as a single-line <code>j</code> if you have analysis enabled.</p><p><strong>Constant materialisation:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>lui   a0, 0x12345         ; upper 20 bits</span></span>
<span class="line"><span>addi  a0, a0, 0x678       ; lower 12 bits -&gt; a0 = 0x12345678</span></span></code></pre></div><p>R2&#39;s ESIL (and decompilers) collapse this pair to <code>a0 = 0x12345678</code>.</p><p><strong><code>jal zero</code> is an unconditional jump:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>jal   zero, label         ; equivalent to &quot;j label&quot;</span></span></code></pre></div><p>R2 shows it as <code>j label</code>. Some compilers prefer the explicit form.</p><p><strong>Tail call:</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>tail other_function       ; pseudo-instruction for: la t1, other_function; jr t1</span></span></code></pre></div><p><code>tail</code> does not push <code>ra</code>; the called function returns directly to the original caller. Decompilers handle this correctly; pay attention when reading raw disassembly.</p><h2 id="risc-v-gotchas" tabindex="-1">RISC-V gotchas <a class="header-anchor" href="#risc-v-gotchas" aria-label="Permalink to &quot;RISC-V gotchas&quot;">​</a></h2><p><strong>The <code>c</code> extension&#39;s compressed instructions can confuse stack analysis.</strong> If a function has both compressed and uncompressed instructions, automatic stack-frame recovery occasionally picks the wrong frame size. Verify with <code>afv</code> and re-run <code>afta</code> if it looks wrong.</p><p><strong>Position-independent code uses <code>auipc</code> heavily.</strong> Every reference to a global variable starts with <code>auipc xN, %hi(symbol)</code>. R2 resolves these correctly when the binary is loaded at the right address. If references look off, double-check <code>-m</code>.</p><p><strong>RISC-V vector extension (<code>v</code>) is rare in microcontrollers</strong> but appears in some HPC-class chips. R2&#39;s vector decoding is incomplete on older versions; if you see undecoded vector instructions, update r2 from git.</p><p><strong>Interrupt vector layout differs.</strong> Standard RISC-V uses a single trap vector (<code>mtvec</code> direct mode) with software dispatch on <code>mcause</code>. Some vendor extensions (CLIC, CLINT-vectored mode) use a table of vectors instead. ESP32-C3 uses Espressif&#39;s &quot;Interrupt Matrix&quot; to route up to 31 peripheral sources through CLINT-style core interrupts — it is <em>not</em> CLIC. Some newer Espressif RISC-V parts (ESP32-P4 and some C-series configurations) do use CLIC.</p><p><strong>Calling convention for floats.</strong> Soft-float (no <code>F</code> extension): floats pass in <code>a0..a7</code>. Hard-float (<code>F</code>/<code>D</code>): floats in <code>fa0..fa7</code>. Mismatched assumptions ruin decompiler output for FP-heavy code.</p><h2 id="building-a-risc-v-signature-db" tabindex="-1">Building a RISC-V signature DB <a class="header-anchor" href="#building-a-risc-v-signature-db" aria-label="Permalink to &quot;Building a RISC-V signature DB&quot;">​</a></h2><p>The same approach as Chapter 10 works:</p><ol><li>Build the vendor SDK (esp-idf, bl_iot_sdk, NuttX, Zephyr) for your target with the same toolchain version.</li><li>Run <code>aaa</code> on each <code>.o</code> from the build.</li><li><code>zg @@f</code> then <code>zo</code> to save.</li><li>Load against unknown firmware and <code>z/</code>.</li></ol><p>Espressif publishes pre-built <code>.a</code> archives for each release; using those directly skips building.</p><p>The general rule: RISC-V is the <em>easiest</em> of the embedded architectures to read once you have the right <code>-c</code> and the right load address. The decoder is straightforward, the calling convention is regular, and the literal-pool tricks of ARM are largely absent (replaced by the simpler <code>auipc + addi</code>/<code>auipc + lw</code> pair). It is a nice architecture to learn r2 on.</p>`,69)])])}const m=s(i,[["render",n]]);export{u as __pageData,m as default};
