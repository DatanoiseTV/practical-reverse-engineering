import{c as a,Q as s,j as t,m as n}from"./chunks/framework.DxgWTcNC.js";const g=JSON.parse('{"title":"Loading Raw Images: Memory Maps, Vector Tables, MMIO","description":"","frontmatter":{},"headers":[],"relativePath":"part4/19-raw-images.md","filePath":"part4/19-raw-images.md","lastUpdated":null}'),o={name:"part4/19-raw-images.md"};function i(r,e,d,l,c,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="loading-raw-images-memory-maps-vector-tables-mmio" tabindex="-1">Loading Raw Images: Memory Maps, Vector Tables, MMIO <a class="header-anchor" href="#loading-raw-images-memory-maps-vector-tables-mmio" aria-label="Permalink to &quot;Loading Raw Images: Memory Maps, Vector Tables, MMIO&quot;">​</a></h1><p>When a binary arrives without an ELF header — a raw flash dump, a <code>dd</code> of an MTD partition, an extracted blob from a multi-stage bootloader — you have to reconstruct everything the loader would have told r2 automatically. This chapter is the procedural recipe: how to infer architecture, base address, segment boundaries, and peripheral mappings from raw bytes. Some of this overlapped with the per-arch chapters in Part III; this chapter consolidates the reasoning into one place you can return to.</p><h2 id="what-r2-needs-to-load-a-blob" tabindex="-1">What r2 needs to load a blob <a class="header-anchor" href="#what-r2-needs-to-load-a-blob" aria-label="Permalink to &quot;What r2 needs to load a blob&quot;">​</a></h2><p>The minimum to get useful disassembly:</p><ul><li><strong>Architecture</strong> (<code>-a arm</code>, <code>-a mips</code>, etc.)</li><li><strong>Bits</strong> (<code>-b 16</code>, <code>-b 32</code>, <code>-b 64</code>)</li><li><strong>Endianness</strong> (<code>-e cfg.bigendian=true|false</code>)</li><li><strong>Base load address</strong> (<code>-m 0x...</code>)</li><li><strong>CPU subtype</strong>, sometimes (<code>-c cortex</code>, <code>-c esp32</code>, <code>-c rv32imac</code>)</li></ul><p>Get any one of these wrong and the disassembly is garbage. Get all of them right and r2 produces the same quality output it would for an ELF.</p><p>The rest of this chapter is how to derive each of those.</p><h2 id="step-1-identify-the-architecture" tabindex="-1">Step 1: Identify the architecture <a class="header-anchor" href="#step-1-identify-the-architecture" aria-label="Permalink to &quot;Step 1: Identify the architecture&quot;">​</a></h2><p>In rough order of certainty:</p><p><strong>Strings.</strong> Build IDs, vendor banners, and source-file paths identify the architecture more reliably than any heuristic:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ strings firmware.bin | grep -iE &#39;gcc|clang|rust|go|sdcc&#39;</span></span>
<span class="line"><span>GCC: (Arm GNU Toolchain ...) 13.2.1</span></span>
<span class="line"><span>$ strings firmware.bin | grep -iE &#39;arm|cortex|xtensa|mips|riscv|esp32&#39;</span></span>
<span class="line"><span>$ strings firmware.bin | grep -iE &#39;stm32|nrf|esp|wch|bouffalo|atheros|broadcom|mediatek&#39;</span></span></code></pre></div><p>The <code>GCC: (Arm GNU Toolchain ...)</code> string is the definitive answer. A <code>Espressif IoT Development Framework</code> string is too. Vendor SoC names narrow further.</p><p><strong>File-format header.</strong> If the blob starts with <code>0xE9</code> byte and a plausible 4-byte entry point, it is an ESP32 image (Chapter 14). If it starts with <code>27051956</code> (uImage magic, big-endian), it is a U-Boot image with embedded architecture info — <code>dumpimage</code> reads it. If it starts with <code>7F 45 4C 46</code>, it is an ELF (you do not need this chapter).</p><p><strong>Opcode-statistics tools.</strong> <code>cpu_rec.py</code> (Airbus CERT) classifies unknown blobs by byte n-gram statistics across a corpus of known architectures. It is the most accurate &quot;unknown blob -&gt; architecture&quot; tool widely available:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ cpu_rec.py firmware.bin</span></span>
<span class="line"><span>firmware.bin                           full(0x100000, ARM)            chunk(ARM)</span></span></code></pre></div><p><code>binwalk -A</code> runs Capstone disassembly across architectures and picks the best fit. Less accurate than cpu_rec for short images, faster for big ones.</p><p><strong>Manual byte inspection.</strong> The signatures of common architectures in the first 64 bytes are surprisingly recognisable once you have seen each:</p><ul><li><strong>ARM Thumb (Cortex-M)</strong>: starts with two 4-byte words that look like a RAM-region pointer (<code>0x20XXXXXX</code>) and a flash code pointer with the low bit set (<code>0xXXXXXXX1</code>). Then a vector table of more flash pointers.</li><li><strong>ARM AArch32 application</strong>: starts with branches (<code>EA xx xx xx</code>) or a literal pool followed by code.</li><li><strong>MIPS</strong>: bytes like <code>27 BD FF E0</code> (big-endian <code>ADDIU $sp, $sp, -0x20</code>) or <code>E0 FF BD 27</code> (little-endian).</li><li><strong>RISC-V</strong>: bytes whose low byte ends in <code>11</code> binary (low 2 bits = <code>11</code>) are uncompressed 32-bit instructions; <code>0x13</code> (RV32I <code>OP-IMM</code>, the <code>addi</code> family) and <code>0x17</code> (<code>AUIPC</code>) at function entries are typical. Bytes ending in <code>00</code>/<code>01</code>/<code>10</code> are 16-bit compressed (RVC) instructions. The <code>auipc/addi</code> pair for global-pointer setup is a strong tell.</li><li><strong>Xtensa</strong>: 24-bit instructions that look statistically lumpy; first bytes often <code>36 41 00</code> (entry instruction) or similar.</li><li><strong>8051</strong>: <code>02 xx xx</code> (LJMP) at offset 0; very low-density.</li></ul><p>Once you have a candidate, validate by loading it and reading the first 64 instructions. If they look like a sensible startup sequence (stack pointer setup, BSS clear, data copy from flash to RAM, call to main), you are right. If they look random, you are wrong — try another arch or another endianness.</p><h2 id="step-2-determine-the-base-address" tabindex="-1">Step 2: Determine the base address <a class="header-anchor" href="#step-2-determine-the-base-address" aria-label="Permalink to &quot;Step 2: Determine the base address&quot;">​</a></h2><p>The base address is whatever virtual address the firmware was linked at. Methods:</p><p><strong>The vector table</strong> (Cortex-M, RISC-V with vectored interrupts). On Cortex-M, the second word is the reset vector. It points into flash. The flash base on a typical chip is published in the TRM:</p><table tabindex="0"><thead><tr><th>Vendor / family</th><th>Flash base</th></tr></thead><tbody><tr><td>STM32</td><td><code>0x08000000</code></td></tr><tr><td>nRF52</td><td><code>0x00000000</code></td></tr><tr><td>SAMD</td><td><code>0x00000000</code></td></tr><tr><td>LPC55S6x</td><td><code>0x10000000</code> or <code>0x00000000</code> (secure/non-secure)</td></tr><tr><td>RP2040</td><td><code>0x10000000</code></td></tr></tbody></table><p>The reset vector word, masked low bit removed, must lie within the flash region. That is your base address.</p><p><strong>Internal pointers.</strong> A long blob with no obvious structure usually contains literal-pool pointers to other parts of itself. If you disassemble the blob at <em>some</em> base and see <code>LDR R0, =0x08001234</code>, and <code>0x08001234</code> lies within the loaded image, your base is right. If <code>0x08001234</code> lies <em>outside</em> the loaded image and looks like it should point to a string or table, your base is wrong by some multiple of the image size.</p><p>Algorithm: try base <code>0x00000000</code>. Disassemble. Find the most common literal-pool target. Compute the offset between that target and the likely string/table location in the image. The image is loaded at that offset.</p><p><strong>Bootloader hand-off.</strong> If the binary follows a bootloader, the bootloader&#39;s last instruction is typically a jump to the application&#39;s reset vector. Reverse engineer the bootloader (which is at the known reset address) to recover the application&#39;s base.</p><p><strong>Padding and alignment.</strong> Linker scripts align section boundaries to power-of-two addresses. The base address is almost always a multiple of <code>0x1000</code> (page size) or <code>0x10000</code> (sector size on common flash).</p><div class="tip custom-block"><p class="custom-block-title">Tip</p><p>For Cortex-M specifically, write a small Python script that loads the binary, reads the first 256 4-byte words, and reports those that look like valid vectors (low bit set, target within <code>0x08000000</code> ± <code>flash_size</code>). If 30+ words match consistently, you have the right base. This trick works for any architecture with a vector table at the start of the image — adjust the magic addresses for the chip.</p></div><h2 id="step-3-determine-the-endianness" tabindex="-1">Step 3: Determine the endianness <a class="header-anchor" href="#step-3-determine-the-endianness" aria-label="Permalink to &quot;Step 3: Determine the endianness&quot;">​</a></h2><p>For ARM and Xtensa, almost always little-endian. For MIPS, both are common; check.</p><p>Quick test: pick a likely instruction (anything that is <em>not</em> zero) and decode under both endiannesses. The one that produces a sensible opcode is right. For example, <code>27 BD FF E0</code> decodes as a stack allocation in big-endian MIPS and as nonsense in little-endian.</p><p>Set:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e cfg.bigendian = true</span></span>
<span class="line"><span>[0x...]&gt; e cfg.bigendian = false</span></span></code></pre></div><p>Endianness can change <em>within</em> a binary (rare but happens for heterogeneous SoCs with different cores). Hint per-region with <code>ahb</code> if you encounter this.</p><h2 id="step-4-identify-segments" tabindex="-1">Step 4: Identify segments <a class="header-anchor" href="#step-4-identify-segments" aria-label="Permalink to &quot;Step 4: Identify segments&quot;">​</a></h2><p>Most embedded firmware is a single contiguous flash blob. But a fully-linked image often has:</p><ul><li><strong><code>.text</code></strong> — code in flash</li><li><strong><code>.rodata</code></strong> — strings and constants in flash</li><li><strong><code>.data</code></strong> — initial values for RAM-resident global variables, <em>stored in flash</em> but copied to RAM at startup</li><li><strong><code>.bss</code></strong> — RAM-resident, zero-initialised at startup, <em>not in the image</em></li><li><strong>literal pools</strong> — small data interleaved into <code>.text</code></li></ul><p>In a raw blob, all of <code>.text</code>, <code>.rodata</code>, and <code>.data</code> are present. The boundaries between them matter for analysis: instructions live in <code>.text</code>; reading <code>.rodata</code> as instructions produces garbage.</p><p>Heuristics for finding <code>.rodata</code>:</p><ul><li>A run of high-ASCII printable characters is <code>.rodata</code>.</li><li>A run of 32-bit words that look like pointers but are followed by a string is a const struct array (also <code>.rodata</code>).</li><li>A region with very different byte-frequency statistics from the preceding region is a section change.</li></ul><p>For a raw blob, telling r2 about the boundary lets analysis ignore non-code regions:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>[0x...]&gt; e anal.from = 0x08000000</span></span>
<span class="line"><span>[0x...]&gt; e anal.to   = 0x08020000   # end of .text estimate</span></span></code></pre></div><p>Then run analysis. R2 will not waste time disassembling rodata as code (and will not invent fake functions there).</p><h2 id="step-5-mmio-regions" tabindex="-1">Step 5: MMIO regions <a class="header-anchor" href="#step-5-mmio-regions" aria-label="Permalink to &quot;Step 5: MMIO regions&quot;">​</a></h2><p>After loading, the disassembly will have many references to addresses in the <code>0x40000000</code>-or-similar peripheral region. Annotate them.</p><p>For a known SoC, transcribe the peripheral base addresses to flags once and reuse:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span># stm32f4_peri.r2</span></span>
<span class="line"><span>f peri.RCC      = 0x40023800</span></span>
<span class="line"><span>f peri.PWR      = 0x40007000</span></span>
<span class="line"><span>f peri.GPIOA    = 0x40020000</span></span>
<span class="line"><span>f peri.GPIOB    = 0x40020400</span></span>
<span class="line"><span>f peri.USART1   = 0x40011000</span></span>
<span class="line"><span>f peri.SPI1     = 0x40013000</span></span>
<span class="line"><span>f peri.I2C1     = 0x40005400</span></span>
<span class="line"><span>f peri.TIM1     = 0x40010000</span></span>
<span class="line"><span>f peri.NVIC     = 0xE000E100</span></span>
<span class="line"><span>f peri.SCB      = 0xE000ED00</span></span>
<span class="line"><span>... (whole TRM)</span></span></code></pre></div><p>Then <code>. stm32f4_peri.r2</code> at session start. Combined with <code>tl</code> (link type to address, Chapter 8) using the CMSIS header, every peripheral access in the disassembly reads as a struct field.</p><h2 id="step-6-reload-with-a-recipe" tabindex="-1">Step 6: Reload with a recipe <a class="header-anchor" href="#step-6-reload-with-a-recipe" aria-label="Permalink to &quot;Step 6: Reload with a recipe&quot;">​</a></h2><p>After steps 1–5 you have a working load configuration. Save it:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span># load.r2</span></span>
<span class="line"><span>e asm.arch = arm</span></span>
<span class="line"><span>e asm.bits = 16</span></span>
<span class="line"><span>e asm.cpu = cortex</span></span>
<span class="line"><span>e cfg.bigendian = false</span></span>
<span class="line"><span>e anal.from = 0x08000000</span></span>
<span class="line"><span>e anal.to   = 0x08020000</span></span>
<span class="line"><span>e anal.depth = 64</span></span>
<span class="line"><span></span></span>
<span class="line"><span>o firmware.bin 0x08000000</span></span>
<span class="line"><span></span></span>
<span class="line"><span>. stm32f4_peri.r2</span></span>
<span class="line"><span>to stm32f4_flat.h</span></span>
<span class="line"><span></span></span>
<span class="line"><span>aaa</span></span>
<span class="line"><span></span></span>
<span class="line"><span>P+ stm32f4_fw_v1</span></span></code></pre></div><p>Run with <code>r2 -i load.r2 -</code>. From now on every session starts in the same correct state.</p><h2 id="when-the-blob-is-not-contiguous" tabindex="-1">When the blob is not contiguous <a class="header-anchor" href="#when-the-blob-is-not-contiguous" aria-label="Permalink to &quot;When the blob is not contiguous&quot;">​</a></h2><p>Some images are concatenations: bootloader + application + filesystem. <code>binwalk</code> finds the boundaries. Then split:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark-dimmed vp-code" tabindex="0"><code><span class="line"><span>$ binwalk firmware.bin</span></span>
<span class="line"><span>0          0x0          uImage header</span></span>
<span class="line"><span>64         0x40         LZMA compressed kernel</span></span>
<span class="line"><span>1572864    0x180000     Squashfs filesystem</span></span>
<span class="line"><span></span></span>
<span class="line"><span>$ dd if=firmware.bin of=kernel.lzma bs=1 skip=64 count=1572800</span></span>
<span class="line"><span>$ dd if=firmware.bin of=rootfs.squashfs bs=1 skip=1572864</span></span>
<span class="line"><span>$ unlzma kernel.lzma</span></span>
<span class="line"><span>$ unsquashfs rootfs.squashfs</span></span></code></pre></div><p>Now load each part separately into its own r2 session, or use multiple mappings within one session if they execute together.</p><h2 id="when-you-get-it-wrong-fast-checks" tabindex="-1">When you get it wrong: fast checks <a class="header-anchor" href="#when-you-get-it-wrong-fast-checks" aria-label="Permalink to &quot;When you get it wrong: fast checks&quot;">​</a></h2><p>After loading, four sanity checks:</p><ol><li><code>pdf @ entry0</code> produces a sensible function (not an instruction stream that decodes random bytes).</li><li><code>pdf @ entry0</code> ends with a return instruction (not an unconditional branch into nothing).</li><li><code>axt @ str.&lt;some-string&gt;</code> returns at least one xref (the string is referenced from code).</li><li><code>afl | head -20</code> shows function sizes that look plausible (40–500 bytes typical, not single-instruction or 50,000-byte monsters).</li></ol><p>If all four pass, your load is right. If any fail, revisit the choice they implicate (bad arch / wrong base / endianness / segment boundary).</p><p>The discipline of loading a blob right the first time — taking twenty minutes to sort out the recipe rather than diving into analysis on a wrongly-loaded image — saves hours of confusion later. Every line of disassembly you read against a misloaded image is a line you read in vain.</p>`,62)])])}const u=a(o,[["render",i]]);export{g as __pageData,u as default};
